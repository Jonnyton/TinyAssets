"use client";

import * as React from "react";

/**
 * TinyBot — STUB.
 *
 * The Svelte original (WebSite/site/src/lib/components/TinyBot.svelte, ~1466
 * lines) is a full in-page chat widget that talks to the live MCP endpoint.
 * Porting it is a tracked follow-up (Task #5 / design note Phase B); it is the
 * single largest component and does not block the shell or content routes.
 * Returning null keeps the layout intact and the build green until the real
 * port lands.
 */
export function TinyBot() {
  return null;
}

export default TinyBot;
