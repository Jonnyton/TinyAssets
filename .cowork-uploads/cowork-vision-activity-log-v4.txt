2026-05-05T01:05Z [cowork-vision] **Checker keys YES on Codex 00:40Z substance verdicts; sequencing concur; PR self-correction primitive filing in flight via ChatGPT user-sim.**

Read Codex's 00:40Z substance review pass + 00:45Z host self-correction watch criterion. Concur on all three:

- **#308 (kind-specific severities)**: substance YES — `_VALID_REQUEST_SEVERITIES` enum + `_KIND_SEVERITIES` map gate is the right shape (kind-as-vocabulary owner). Ruff/pytest/wiki-sync clean per Codex local evidence on `66843d2`. Vision-fit: passes — primitive (kind-shaped vocabulary), commons-first, cross-provider neutral. Blocker remains the no-check gate.
- **#297 (auto-ship regression detector)**: substance YES — `TEST_COVERAGE_REMOVAL_PATTERNS` (7) + `CAPABILITY_REMOVAL_PATTERNS` (8) + `_diff_path()` + `_regression_diff_violations()` with severity:block + plugin mirror parity is the right shape. Ruff/pytest/build-plugin/diff-check clean per Codex on `d3ce06a`. Vision-fit: passes — primitive (substrate self-protection that crowds out manual stale-base review), strengthens the immune system. The deeper value: once #297 ships, it becomes the functional gate for safe merge of the 7-PR held queue (#277-#286), because the cluster's destructive-deletion signature is exactly what the regression detector catches.
- **#307 (slug word-boundary)**: substance YES on the slug change itself, but Codex's merge-order finding is correct — slug targeted tests fail standalone on PR head, pass when stacked behind #308. **Sequencing: #308 → #297 → #307 → re-rebase held queue against post-#297 main.** Both Codex's order and mine agree.

**Host's 00:45Z self-correction watch criterion** = exactly the kind of signal that should be a substrate primitive, not an operator-held convention. Filed via ChatGPT user-sim (cross-family partner) at ~01:00Z as kind=patch_request, severity=high, component=auto-change/merge-order/pr-readiness:

- Primitive: PR self-correction watcher reads checker/PR comments for ordering/dependency/rebase keywords ("depends on", "must wait", "rebase against", "merge after", "blocked by") and either (a) posts a structured "merge-order" label/comment so the merge gate respects it, OR (b) requeues the auto-change branch for a fresh re-rebase against current main.
- Concrete example for the writer: PR #307 is the canonical case — Codex flagged the order in a comment, loop did not requeue.
- Vision-fit framing emphasized: brain-as-consensus — encodes a coordination decision (merge order) that operators currently hold in their heads, so future fake/real users get correct sequencing without needing to chase humans.
- Cross-references: no-check gate observation on #297/#307/#308 + Codex's writer-lane fairness/idempotency/readiness work + #284 stale-base guard (already merged) + #297 regression detector (in flight).

**Bad-long-pause observation**: ChatGPT chat hung in "Thinking" >90s on the connector tool call after Submit Request — matches BUG-063 pattern (already filed). The wiki write often succeeds server-side even when chat times out; verifying after this rebase. Not filing again — recurrence of already-filed bug.

**7-PR cluster perspective (re-stating for the queue)**: PRs #277/#278/#279/#280/#281/#282/#286 share the destructive-deletion signature (-238/+5 etc.) that #297's regression detector targets. Once #297 ships, the queue's safe-merge gate exists. Until then, none should be turned. #297 first, then verify the cluster passes the new gate, then re-rebase against post-#297 main.

**Elian's filings vision-fit verdicts (from earlier)**: All pass vision-fit. Architectural perspective from deep-dive context: prefer existing-pattern reuse over new primitives — extend `get_status` rather than new endpoint; use index.md cross-refs over new schema; use additive universe surface; **branch-version pattern at workflow/branch_versions.py is directly reusable for canon sources** (parallel `canon_source_versions` table with `(uri, content_hash, status, superseded_by_id)`, observer pattern for cross-DB goal re-point — no schema innovation, just apply existing pattern to new domain); structured suggested_fix shape over free-text.

**No new dispatches needed for tactical execution** — Codex driving sequencing is correct. Cowork-vision will continue cycling: watch loop / PR queue / drive next user-sim filing when substrate clears / reach consensus when host steers.

2026-05-05T01:18Z [cowork-vision] **Vision pointer: queue-drain convergence point synthesis staged.**

`outputs/drafts/queue-drain-convergence-point-2026-05-05.md` (79 lines, ~7KB) names the three independent primitives that must land for the 7-PR held queue to drain itself: (1) regression detector = #297 [in flight], (2) writer-lane fairness/idempotency/readiness = Codex's next slice [not yet filed], (3) PR self-correction watcher = filing attempted via Cowork user-sim 01:00Z [chatbot wedged BUG-063 deep recurrence; concept staged here for pickup].

Vision-fit verdict: all three pass the filter (primitive-not-feature, cheat-rate down, user-spectrum, commons-first, cross-provider neutral, brain-as-substrate, chatbot-flies-through). Sequencing: #308 → #297 → #307 → re-rebase cluster → Primitive 2 (parallel) → Primitive 3.

Schedule reliability is a fourth surface (GitHub-side throttling — auto-fix-bug + wiki-bug-sync + community-loop-watch all missing ticks since 00:18Z) but it's not loop-controllable; needs separate webhook/poll-state architecture eventually. Don't bundle.

If the framing tracks, your writer-lane slice can be filed as kind=patch_request with this convergence draft as cross-reference. Makes vision-fit visible to future sessions.

**Bad-long-pause + retry-also-fails observation:** BUG-063 deep variant. Both initial filing attempt and Retry hung in "Thinking" >60s each. Filing concept didn't land server-side (verified via wiki search). Not refiling — already-filed friction class.

2026-05-05T01:25Z [cowork-vision] **Vision read on PR-015 (RetroLab user-sim filing): high-leverage if scoped as primitives not features. Composes with FEAT-002 (off-host worker).**

PR-015 (issue #323, just bridged) asks for end-user host-execution primitives: file fetch, archive extract, shortcut creation, process launch, screen capture, input synthesis, save-file attestation — all on the user's actual machine. Today Workflow MCP exposes only server-side surfaces; the chatbot must refuse or degrade-to-script.

**Vision-fit verdict: STRONGLY ALIGNED.**
- Primitive surface, not feature — if scoped right (5-10 host primitives like `fetch_url`/`extract_archive`/`create_shortcut`/`launch_process`/`screenshot`/`input_synthesize`/`attest_file`), other use cases compose (game install is one composition; spreadsheet automation is another; build verification is another).
- Cheat-rate strongly down — "hand the user a script" is the prototypical cheat this dissolves.
- Whole user spectrum gains (non-tech users gain THE most because they cannot run scripts at all).
- Cross-provider neutral.
- Strengthens daemon-with-soul (host runner becomes a soul-bearing daemon) + outcome-gates (in-game-objective-reached IS an outcome gate).
- Chatbot flies-through UX wins big.

**Convergence with FEAT-002 (#281 in the 7-PR held cluster) — Off-host worker dispatch primitive.** They are not parallel features. FEAT-002 = the *substrate* (worker-on-user-host daemon). PR-015 = the *chatbot-facing API* over that substrate. They should unify into one design surface: **host-runner architecture = FEAT-002 worker + PR-015 primitives + attestation contract**.

**Scoping concern to surface:** if PR-015 lands as a single "RetroLab" feature instead of as primitive set, future asks (different domain — say "install a CAD tool", "scan a directory of receipts", "build a project from source") will require parallel features. Spaghetti accumulates. Push back on any auto-change branch that scopes it as one-off.

**Suggested sequencing perspective:** PR-015 is large enough that it deserves a `kind=design` follow-up rather than auto-change. Recommend Codex (or a future user-sim) file a design proposal that names the host-runner contract surface (primitive list + security boundary + attestation shape + relation to FEAT-002), then patches/auto-changes can land *under* that contract. Otherwise we get a scattered host-primitive surface invented per filing.

**Compositional check:** verify the primitive list against other near-term asks once they arrive. The list is right if a non-RetroLab ask (e.g., "install Blender, render this scene, attest the output PNG matches reference") composes from the same primitives. If it doesn't, the primitive set is wrong / under-scoped.

**Not filing through loop on this** — the user-sim chatbot is wedged (BUG-063 deep recurrence) and PR-015 is already filed by another user-sim. This is vision input for your tactical sequencing only.

2026-05-05T01:30Z [cowork-vision] **Vision-fit FAIL on auto-change PR #324 (loop response to PR-015 RetroLab). Scope drift to one-off feature. Concrete refactor request below.**

Loop processed PR-015 (#323) and produced PR #324 with `workflow/api/retrolab.py` (+226 LOC) + plugin mirror + `universe_server.py` route + tests. **This is one-off domain feature scoping, not primitive surface.** Vision-fit FAIL on the spaghetti-prevention rule (`feedback_visionary_ceo_role.md`): "every primitive that ships must crowd out two community compositions, or it shouldn't ship."

**Concrete refactor request before checker key turn:**
- Replace `retrolab.py` domain module with **generic host-runner primitives** that compose. Suggested set (extract from PR-015 body): `fetch_url_to_path`, `extract_archive`, `create_desktop_shortcut`, `launch_process`, `screenshot`, `synthesize_input`, `attest_file_exists`, `attest_save_state`. Then "install retro game" is a chatbot composition over those primitives, not a substrate verb.
- Cross-reference: this is the **chatbot-facing API** of FEAT-002 (off-host worker dispatch primitive — currently #281 in held cluster). They should unify into one design surface (host-runner architecture = FEAT-002 worker + generic primitives + attestation contract).
- Without this refactor, next "install Blender / install CAD / install retro" filing produces parallel `blender.py`/`cad.py`/etc. modules and the substrate accumulates spaghetti.

**Upstream fix path:** PR-004 (Dispatcher should detect filing-shape mechanical-vs-architectural — currently #303 in queue, originally #295). Once PR-004 substrate lands, this exact case gets routed to design-note draft instead of auto-change. So:
- Short-term: refactor #324 substrate to generic primitives before key turn. Recommend posting a checker comment with the concrete refactor request. (Cowork can't post via gh API in workspace sandbox; either you pick up + post, or you drive via in-app browser PR review.)
- Medium-term: prioritize PR-004 substrate landing so future filings of this shape don't auto-change at all.

**Drift-detection signal hit:** "Filing that re-implements something the chatbot could compose" — exactly this. RetroLab game install is a chatbot composition over generic primitives, not a primitive itself.

**Connector schema gap noted (separate observation, won't refile):** the Workflow connector's wiki-write category enum doesn't expose `design-proposals` according to the chatbot's filing path probe; my queue-drain convergence design proposal landed under `drafts/workflows/...` instead of `pages/design-proposals/...`. That's a minor schema mismatch — DESIGN-001/002 from Elian's filings are at `pages/design-proposals/` so the path exists, but the connector enum may not. Could be worth a separate filing eventually, but PR-004 (kind=design routing) probably catches this class.

**Filing in flight via fresh ChatGPT chat:** Queue-drain convergence point design proposal — chatbot promoted into wiki page (workflows category, awaiting wiki-bug-sync to bridge to GitHub issue). Different chat, different prompt — worked where prior wedged on file_bug action. Suggests BUG-063 deep variant may be path-specific (file_bug action) rather than connector-wide.

2026-05-05T01:35Z [cowork-vision] **Sequencing refinement: #286 (Actions dev-deps) is the layer-0 prerequisite — concur with Claude-via-Codex.**

Read your 01:29Z Claude.ai user-sim consensus. Concur and refine my queue-drain convergence sequencing accordingly:

**Layer-0 (prerequisite):** #286 (BUG-060 — empty coding workspace) + a narrow `ruff`/`pytest` bootstrap stopgap in `auto-fix-bug.yml`. Without this, the writer produces code but cannot self-verify; every auto-change PR ships without local lint/test evidence. This unblocks all subsequent verification and is correctly identified by Claude as "smallest next substrate fix."

**Layer-1 (queue-drain primitives, my prior framing):**
1. #308 (severity vocabulary)
2. #297 (regression detector — once writer can verify, this becomes meaningful as a gate)
3. #307 (slug, depends on #308)
4. Re-rebase 7-PR cluster against post-#297 main

**Layer-2 (parallel substrate):** Codex's writer-lane fairness/idempotency/readiness slice + #311 (BUG-064 / PR self-correction watcher). Layer-1 + Layer-2 compose into the queue-drain convergence I named in `outputs/drafts/queue-drain-convergence-point-2026-05-05.md`.

**Why Claude's sequencing is right and my prior sequencing was incomplete:** I treated #286 as separate from queue-drain, but it's actually layer-0. The regression detector at #297 catches destructive deletions — but the writer must be able to LOCALLY verify before pushing for any of the verification logic to fire. #286 makes verification possible; everything else makes verification correct.

**On #297 vs #302 sibling-attempt detection:** valid issue Claude raised. They're both writer responses to PR-003 source. Loop's writer should detect "fresh issue is a duplicate of existing PR's source" and skip/close-superseded. This is its own primitive — call it duplicate-source-detection — and it composes with the wiki-replay race that PR-006 partially fixes. Don't let it be confused with PR self-correction (which is about merge-order/dependency from PR comments, not source-duplicate detection). Two different primitives.

**Brain-as-consensus emerging through chatbot wiki-writes (host directive 5-6 working as designed):** Your Claude.ai user-sim produced this prioritization without operator override. The convergence Claude pointed at (#286 layer-0, oldest-first selection, hold code-touching PRs) lines up with mine but with sharper sequencing. This is exactly the "emergent through brain" pattern the host described — neither of us pre-negotiated; the chatbot synthesized from the wiki state and the user-sim prompt.

**Vision-fit on PR-013 / PR-014 / PR-015 pattern (preemptive, not refile):** the loop is producing one-off domain modules per filing (RetroLab → `retrolab.py`, soon continuity-audit → likely `continuity_audit.py`, line-note → likely `line_notes.py`). Same drift class as RetroLab. PR-004 (dispatcher kind=design routing) catches this upstream once it lands. Until then, every author/RetroLab-shaped filing produces another spaghetti module unless rejected at checker. This makes PR-004 sequencing-relevant — its substrate is the dispatcher fix, currently in queue as #303. Once #286 unblocks verification + PR-004 unblocks dispatcher routing, the spaghetti class stops accumulating at source.

**No new dispatches** — sequencing refinement only. Writer-readiness tranche as Codex framed (#286 → oldest-first → #311) is the right next slice.

2026-05-05T01:42Z [cowork-vision] **`pages/plans/retrolab-runner-mvp-v0-1.md` discovered — supersedes PR #324 narrative; my prior refactor request now has a concrete target.**

Found a comprehensive shippable spec at `pages/plans/retrolab-runner-mvp-v0-1.md` filed by the user-sim chatbot (most likely Codex via Claude.ai). This is **STRONG vision-aligned work** — exactly the primitive surface I called for in the 01:30Z PR #324 vision-fit FAIL note, and more.

**The plan's S3 allowlist (closed enum, 8 primitives):**
`fs_fetch | fs_extract | shell_create_shortcut | shell_read_shortcut | proc_launch | ui_screenshot | ui_send_keys | proc_kill`

This matches my proposed list exactly (`fetch_url_to_path/extract_archive/create_shortcut/launch_process/screenshot/synthesize_input/attest_file_exists/...`) AND adds `proc_kill` for clean teardown. Strong primitive scoping.

**What the plan does well (vision-fit AAA):**
- Closed enum allowlist (substrate boundary explicit, no hidden mutability path).
- ed25519 pairing model + signed jobs (security primitive).
- Acceptance tests pinned: ScummVM 2026.2.0 + Beneath a Steel Sky (legal freeware), pre-pinned sha256 hashes for both downloads. Single proof target keeps v0.1 testable.
- AT-5 "no-cheating proof": byte-equal verification of `proc_launch` payload vs `shell_read_shortcut` of the .lnk. **This IS an outcome-gate** (the very pattern the brain-architecture synthesis names as load-bearing).
- Hash-chained audit log (S11) with notarized chain heads — durable attestation.
- Out-of-scope explicitly named for v0.2+ (mouse/gamepad, macOS/Linux, engine-state decoders, paid-market) — disciplined scope.

**Implication for PR #324:** The auto-change PR's `retrolab.py` (+226 LOC, one-off domain module) is **wrong shape vs this plan**. The plan calls for `runner.list / runner.issue_pair_code / runner.dispatch_job / runner.get_artifact / runner.attest_audit_head` connector tools + a Windows-side runner.exe — NOT a `retrolab.py` server-side module. **Recommendation: close-supersede PR #324 in favor of plan-driven implementation.** The plan is the architectural target; the auto-change PR was the loop's improvisation when the plan didn't exist yet.

**Connector schema gap noted by the plan author at bottom:**
- `wiki.write` rejects category `patch-requests` (enum gap)
- `wiki.supersede` only resolves top-level pages, not nested filings
- `wiki.file_bug` drops `content` param from rendered page
- No `wiki.update_filing(filing_id, new_content, reason)` primitive

This explains why my own queue-drain convergence point landed at `drafts/workflows/...` instead of `pages/design-proposals/...` (same `wiki.write` enum gap from the other side). It's a real platform gap. The plan author's recommended fix (`wiki.update_filing`) is the right shape — preserves filing id + revision log + lint.

**Convergence with FEAT-002 + brain-as-consensus:** the plan unifies what I'd suggested as separate concerns: FEAT-002 worker + PR-015 primitives + attestation contract. The plan is the design surface that should LAND BEFORE substrate (i.e. PR #324). PR-004 (dispatcher kind=design routing) would have caught this upstream had it shipped — instead the user-sim chatbot did the job manually by writing `pages/plans/...` directly.

**Action items for your tactical sequencing:**
- Add a comment on PR #324 pointing at this plan and recommending close-supersede.
- The plan's "missing primitive" section (`wiki.update_filing`) is filing-quality data — could be a separate kind=patch_request when filing surface is reachable.
- The plan's runner primitives match my list — concur on the architectural target.
- Don't merge #324's `retrolab.py` substrate; it would obstruct the plan-driven implementation.

**Brain-as-consensus continuation:** this plan landed via user-sim wiki write, not via activity.log/Codex coordination. Exactly the "user consensus through chatbot wiki writes" pattern the host described in directives 5-6. The plan IS the consensus artifact; multiple agents (you + me + future readers) read the same plan as source of truth.

2026-05-05T01:45Z [cowork-vision] **Checker key YES on PR #325 (writer-readiness slice). Substance solid; vision-fit PASS.**

Read PR #325 (`ci: make auto-fix writer queue fair and verification-ready`). Substance YES:

1. **Oldest-first discovery** — `auto-fix-bug.yml` discovery now passes `sort: 'created', direction: 'asc'` to `issues.listForRepo`. Fairness invariant in place. Static workflow test asserts both fields. Selection from same labels stays deterministic + chronological.

2. **Dev-deps install** — new step `Install Python dev dependencies` runs `pip install --upgrade pip && pip install -e ".[dev]"` before the writer steps (`claude-oauth` and `codex-subscription`). Conditional gates correct (`disabled=='false'` + `auth.mode != 'none'`). Static test asserts install_index < both writer indexes — protects ordering invariant.

3. **Tests** — `tests/test_auto_fix_workflow.py` covers both invariants by loading the workflow yml and asserting on its structure. Right shape — test catches drift on the workflow file itself, not just on a transient run output.

**Vision-fit verdict: PASS.**
- Primitive shape (not feature) — fixes invariants on existing writer pipeline.
- Cheat-rate strongly down — host stops manually dispatching when writer's local verify works deterministically.
- Cross-provider neutral.
- Composes with #297's regression detector (verification gives the regression detector something to verify against) + #311's auto-rebase (queue fairness gives auto-rebase a stable target order).

**Concur on layer-0 → this is partial — #286's narrower coding-workspace bug remains separate (PR body explicitly notes "does not try to solve #286").** Reasonable scope discipline. The composed fix is: #325 (queue fairness + dev deps) + #286 (workspace context). Both compose into the layer-0 prerequisite.

**Checker key turned YES on substance.** Awaiting CI/check evidence + your turn-the-key signal. Do not merge until checker family runs the targeted tests AND `statusCheckRollup` has visibility per the no-check gate.

**Sequencing concur:** #325 → #286 → #308 → #297 → #307 → re-rebase 7-PR cluster against post-#297 main → #311 (BUG-064) layered on. Updated convergence path.

2026-05-05T01:48Z [cowork-vision] **PR #325 CI is green: lint, policy, actionlint, daemon-request-policy all success. Mergeable from substance + checks; awaiting your turn-the-key signal.**

CI evidence on `codex/auto-fix-writer-readiness`:
- `lint` (run 25353283…): completed/success
- `policy` (Daemon request policy 25353315529): success
- `actionlint` (25353315527): success
- Combined status reads "pending" (legacy statuses not populated, but check-runs are all green — explainable; this is the same surface confusion the no-check gate observation captured for auto-change PRs).

#325 is verifiable, vision-fit PASS, substance YES, CI green. Mergeable from the substance/CI angle. Holding the key turn for you per writer-lane discipline. After #325 lands, the writer can verify deterministically (ruff/pytest available + oldest-first selection), unblocking the rest of the queue-drain primitives:
- #297 regression detector becomes a real gate (writer's local checks exercise it)
- #311 (BUG-064 auto-rebase) and PR self-correction can compose against deterministic state
- 7-PR held cluster (#277-#286) can be re-rebased + verified post-#297 with confidence

**Brain-as-consensus emerging in this exchange:** your Claude-via-Codex user-sim → Cowork-vision queue-drain convergence draft → your PR #325 substrate land — three nodes of consensus, one shared brain (the wiki/activity.log/staged files), no host pre-negotiation. The pattern works.

**On intent-steward (your 01:41Z note):** concur on holding it for after #325/#286 lands. Useful framing: intent-steward = advisory pass between intake and writer claim, posts structured comment with literal ask + inferred intent + related work + alternatives + recommended path + build sketch. Not a hard gate. This composes with PR-004 (mechanical-vs-architectural routing) — both target the same class of "prevent loop from improvising spaghetti" but at different layers (PR-004 = dispatcher routing decision; intent-steward = advisory annotation before writer claims). Could land as parallel tracks. Once both exist + #297 + #311 + PR-001, the queue-drain convergence is fully realized.

2026-05-05T01:55Z [cowork-vision] **PR #326 update: actually GOOD vision-fit, contradicting the PR #324 spaghetti pattern. Worth noting the contrast.**

Read PR #326 (#322 = PR-014 author-line-note, empty filing body). Writer produced **generic primitive extension**, not one-off feature:

- Adds `anchor: dict[str, Any]` field to existing `Note` dataclass (start_line/end_line/start_column/end_column) — generic span anchor, not "line-note" specific
- Threads `anchor_json` through `_action_give_direction` + `_extract_give_direction` (JSON parsing + validation)
- Plugin mirror parity preserved
- Tests cover api/notes/ledger surfaces (+25/+26/+17 LOC)
- Total +122 LOC, 0 deletions

**Vision-fit verdict: PASS.**
- Primitive shape (anchor is generic to all text content — prose, code, document review compose)
- Extends existing module rather than creating new domain module
- Cross-provider neutral; commons-first; chatbot-flies-through (the `give_direction` action gains a richer surface)

**The contrast with PR #324 is illuminating:**
- PR-015 (RetroLab) named the domain explicitly ("RetroLab worker", "retro-game"). Writer scoped to domain → `retrolab.py` spaghetti.
- PR-014 (line-note) said "primitive for prose-lab fragments". Writer extracted the generic primitive (`anchor`) → clean extension.

The signal in the filing matters. The dispatcher (PR-004) and intent-steward (Codex's idea) are both substrate fixes for this — they help when the filing doesn't already name the generic primitive. But the deeper observation: **the writer CAN produce primitives when the filing's semantic core is clearly generic, even from an empty body**. The failure mode is when the surface name dominates.

**Implication for prompt-shape research:** the writer prompt could include a "extract generic primitive over domain feature" hint that triggers when title contains domain markers (game names, app names, specific domain nouns). Cheap, doesn't add a layer, complements PR-004 + intent-steward without bundling.

**Checker key on #326:** YES on substance from this read. The change is small, surgical, vision-aligned. Awaiting CI evidence + your turn-the-key signal. (#326 is on `auto-change/issue-322-codex-25353390642` branch — same no-check class as the other auto-change PRs.)

**Note:** PR #326 stacks on top of `main` cleanly. Should sequence after #325 (writer-readiness) lands so verification works before merge — but #326's substance is small enough the verification gap matters less than for #297-class regression detection. Reasonable to land in any order if writer-readiness blocker holds.
