> **Implementation status — RECLASSIFIED 2026-07-22 against `origin/main` @ `144eaba7`.**
> Full evidence table: `docs/audits/2026-07-22-universe-creation-task-landedness.md`.
> Of the 35 tasks previously unchecked and unannotated: **11 were already landed**,
> **4 assert behavior a later host directive reversed**, **10 are gated on a host
> live-proof gate**, and **10 are genuinely open**. Every box checked below carries a
> file:line or PR citation; every held item is now labelled `GATED (...)`; every
> reversed item is labelled `⛔ REVERSED` and **must not be built**.
>
> ⚠ **The previous header (2026-06-30) was stale.** It listed 2.7–2.8 as "held", but both
> landed when PR #1437 and PR #1462 merged 2026-07-15. An implementation-status note
> pinned to a branch goes stale the moment that branch merges — this note is pinned to a
> `main` sha instead. Re-verify before trusting it.
>
> **Two reversals govern this change.** (a) The 2026-07-02 relay reshape
> (`docs/design-notes/2026-07-02-universe-intelligence-relay-architecture.md`): the chatbot
> **relays** and does **not** speak as the universe (`tinyassets/universe_server.py:208`).
> (b) **AUTO-BIRTH**, host decision 2026-07-15 (`tinyassets/api/status.py:689`):
> `get_status` now *does* create a home universe on first contact.

## 1. Contract Tests

- [x] 1.0 Get opposite-provider source review of the OKF latest-main soul baseline before implementation uses the external standard. (Codex found → Claude reviewed; `docs/audits/2026-06-30-okf-soul-baseline-review.md`.)
- [x] 1.0a Add WorkOS resource-server auth tests proving a valid AuthKit bearer token resolves token `sub` to `founder_id`, invalid tokens do not create a principal, and anonymous callers cannot create universes. — landed #1437 (`tests/test_workos_provider.py:101` valid→identity; `:153,:159,:164,:168,:173` expired/missing-sub/anonymous-sub/wrong-issuer/bad-signature all rejected; `tests/test_predeploy_auth_hardening.py:65` anon create rejected)
- [~] 1.0b Add first-contact auth tests proving a founder home universe is resolved from authenticated founder identity, not from env, first directory, or root `.active_universe`. (Identity resolution + no-marker-write + no cross-founder serial leak covered by `test_first_contact::test_first_contact_births_and_binds_home`, `test_founder_create_does_not_write_active_universe_marker`, `test_readonly_founder_omitted_scope_does_not_leak_other_home`; explicit env/first-dir precedence assertions still to add.)
- [ ] 1.0c Add MCP write-boundary tests proving authenticated founders can write only to their own bound/home universe brain and cannot edit another universe's soul, identity, canon, wiki, runtime goals, body, org chart, files, or state. — OPEN (partial: `tests/test_multi_tenant_isolation.py:213` proves the core cross-founder write denial; the per-surface matrix this task enumerates is not covered)
- [x] 1.0c.1 Add anonymous-permission tests proving anonymous callers can read public surfaces only and cannot perform any write, create, run, costly, admin, ledger, sync, or state-changing action. — landed #1441 (`tests/test_anonymous_write_challenge.py:144–259`, 15 tests; `tests/test_predeploy_auth_hardening.py:58,65`; effect-classification gate `tinyassets/auth/middleware.py:396`; live `ui-test` passed 2026-07-14)
- [ ] 1.0c.2 Add universe-visibility tests proving ownership/admin grants do not change public/private readability, `public_read=false` blocks anonymous/non-granted reads, and public/private flips require explicit confirmation. — OPEN, **but ownership moved** to `openspec/changes/universe-visibility/` (tasks 3.1–3.2 there). That change's §1 model decisions (1.1–1.4) are still open; writing these tests here would fork the model. Coordinate, do not duplicate.
- [ ] 1.0d Add Branch interaction tests proving users can submit patch requests, Branch edit proposals, alternative Branch proposals, and Goal proposals without gaining write access to another universe brain. — OPEN (the patch-request surface landed at `tinyassets/api/universe.py:1439`; the negative "without gaining write access" assertion does not exist)
- [ ] 1.0e Add Branch-run tests proving Branches are run only by universes, every run is attached to that universe's runtime Goal, multiple universes can use the same Branch as separate instances, and a universe can remix a Branch into its own variant. — OPEN (universe-as-actor enforced at `tinyassets/api/runs.py:90`; no test for multi-universe instancing or remix. See also 2.0g — the goal-attachment half is unimplemented, so that clause is untestable today.)
- [~] 1.1 Add MCP creation tests proving blank create generates exactly one opaque lowercase-ULID `universe_id` and does not accept a creation-time persona name. (ULID format/uniqueness covered by `test_ids`; create takes no name. A create-route "generates when absent" assertion is still to add.)
- [x] 1.2 Add baseline tests proving creation writes `index.md`, `log.md`, `soul.md`, `soul.edit.md`, `identity.md`, `founder.md`, `orgchart.md`, `projects.md`, `goals.md`, `body.md`, `origin.md`, `soul_versions/index.md`, and `soul_versions/0001.md`, and does not create `self/`, `soul/`, `notes.json`, or `activity.log`. (`test_universe_bundle`, `test_universe_soul`.)
- [ ] 1.3 ⛔ **REVERSED** — Add tests proving `get_status` is idempotent for a newly created universe and does not first-create the soul bundle. **DO NOT BUILD the second clause.** AUTO-BIRTH (host decision 2026-07-15, `tinyassets/api/status.py:689–700`, *"supersedes the 2026-07-02 opt-in birth"*) means `get_status` **does** create and bind a home universe for an authenticated founder who has none. A test asserting "does not first-create" would assert retired behavior and fail against shipped code. The *idempotency* half is already covered (`tests/test_first_contact.py:148 test_auto_birth_is_idempotent`).
- [ ] 1.4 GATED (live-data migration; held for canary + chatbot `ui-test`, AGENTS.md Rule 11/12 — `scripts/rename_live_data_universes_to_serial_ids.ps1`, handoff `docs/exec-plans/2026-06-30-founder-identity-allslices-handoff.md:115`) Add tests proving existing descriptive-id universes are reset to
  serial-id roots and write/run/status operations use the serial `universe_id`.
- [ ] 1.4a Add universe-index tests proving creation inserts one row keyed by
  immutable `universe_id` and name learning updates that row from `identity.md`.
  — OPEN. First clause is implemented (`tinyassets/daemon_server.py:610`); the second is
  **untestable because the behavior does not exist** — see 2.11.
- [ ] 1.5 GATED (blocked by 3.1 — cannot assert the route does not create while it still creates) Replace HTTP create tests with assertions that `POST /v1/universes` does not create a universe.
- [x] 1.6 Add OKF-shape tests proving `soul.md` has parseable YAML frontmatter, `type: Universe Soul`, OKF latest-main source metadata, and local markdown links that resolve to generated files. (`test_universe_bundle`.)
- [x] 1.7 Add link-closure tests proving every baseline file is pointed to by `index.md`, `log.md`, `soul.md`, or `soul_versions/index.md`. (`test_universe_bundle::test_link_closure_every_file_pointed_to`.)
- [x] 1.8 Add `soul.edit` tests proving `soul.edit.md` exists as `type: Soul Edit Policy`, the execution path reads/follows it, changes are treated as learned updates, accepted edits update only governed soul files (`soul.md`, `identity.md`, `founder.md`, `body.md`, `origin.md`), `orgchart.md`/`projects.md`/`goals.md` are not listed as governed files, accepted edits append `log.md`, and a new `soul_versions/` snapshot is written. (Policy FILE + governed-list: `test_universe_bundle::test_soul_edit_policy`; execution path `universe action=soul.edit` landed via `tinyassets/soul_edit.py` + `tests/test_soul_edit.py` — reads governed list from the policy file, requires source+context, ACL/scope/ledger-gated.)
- [x] 1.9 Add projects/goals boundary tests proving `projects.md` is a one-line founder-project index with pointers as needed, and `goals.md` is runtime goals plus goal-attached Branch uses/runs. (`test_universe_bundle`.)
- [x] 1.10 Add body contract tests proving `body.md` starts not-learned, describes body as learned embodiment/personification, and does not claim live platforms, applications, voice, hands, or senses before those are built or observed. (`test_universe_bundle`.)
- [x] 1.11 Add orgchart contract tests proving `soul.md` links `orgchart.md` with the open questions, `orgchart.md` starts not-learned, states that the oath-confirmed founder is always the top anchor, and does not invent roles, teams, daemons, collaborators, delegations, or reporting lines. (`test_universe_bundle`.)
- [ ] 1.12 ⛔ **REVERSED in part** — Add first-connect tests proving an authenticated founder with no home universe gets a blank seed universe created and bound, the chatbot loads that seed soul/persona, and the first response speaks in first person as the universe rather than returning platform status as the main experience. **The birth/bind half is LANDED** (`tests/test_first_contact.py:124 test_first_contact_auto_births_home`, `:332 test_readonly_founder_gets_awaiting_not_birth`). **DO NOT BUILD the first-person-voice clause** — the 2026-07-02 relay reshape reversed it; shipped code at `tinyassets/universe_server.py:208–211` reads *"You do NOT speak as the universe … RELAY … you are the connector, not the universe."*
- [ ] 1.13 ⛔ **REVERSED in part** — Add first-connect tests proving an authenticated founder with an existing home universe loads that learned universe soul/persona and speaks as that universe. **Home resolution is LANDED** (`tests/test_first_contact.py:469 test_omitted_universe_routes_founder_to_their_home`). **DO NOT BUILD the "speaks as that universe" clause** — same relay reversal as 1.12.
- [x] 1.14 Add scope tests proving omitted universe scope resolves through authenticated founder home-universe context and never through root `.active_universe`. (`test_first_contact::test_founder_create_does_not_write_active_universe_marker`, `test_readonly_founder_omitted_scope_does_not_leak_other_home`.)
- [ ] 1.15 GATED (same live-data migration as 1.4) Add existing-universe reset tests proving universe roots are serial-id folders, data/canon files remain under the serial directory, and write/run/status operations use the serial `universe_id`.
- [ ] 1.16 Add universe-clearing tests proving serial universe directories and founder home bindings can be cleared while branch definitions, goals, run metrics, and outcome records remain intact unless branch commons are explicitly reset. — OPEN (partial: `tests/test_reset_universes.py:75` asserts dirs + index + ACL cleared and branch_definitions/goals preserved. The two clauses this task actually names — `founder_home` bindings cleared, and run metrics / outcome records intact — are **not** asserted, though `tinyassets/reset.py:36,51` supports both.)
- [ ] 1.17 GATED (PR #1438 "Android app" is OPEN/unmerged; no mobile code on `main`; task is explicitly conditioned on stable auth/read/confirmed-write surfaces + deploy) Add mobile-contract tests or fixtures proving an Android/iOS client path uses the same MCP auth/read/confirmed-write contract and cannot create a mobile-only universe id or brain shape.

## 2. Creation Implementation

- [x] 2.0 Implement WorkOS AuthKit Resource Server validation: Protected Resource Metadata, AuthKit metadata/JWKS loading, issuer validation, audience validation, and token `sub` to `founder_id`. — landed #1437 (`tinyassets/auth/workos_provider.py:52` endpoint derivation, `:103` JWKS, `:135–:143` issuer+audience decode, `:153` sub→founder; `tinyassets/auth/wellknown.py:63` PRM RFC 9728, `:108–140` routes mounted at root and `/mcp`)
- [x] 2.0a Implement resolve-always auth mode: public reads can be anonymous, while create/write/costly/admin actions require authenticated founder scopes through the existing action-scope registry. — landed #1437 (`tinyassets/auth/provider.py:703,1118` definitions; `tinyassets/auth/middleware.py:61,380` call sites; `:396` read passthrough; `:402` scope requirement)
- [x] 2.0b Enforce target-universe ownership for universe-brain writes through MCP. — landed #1437 (`tinyassets/api/universe.py:132–157` `_universe_acl_error`, the single ACL gate, with documented `list`/`create_universe`/`_DAEMON_SCOPED_ACTIONS` exemptions; `tinyassets/api/permissions.py:92–127`)
- [x] 2.0c Enforce anonymous public-read-only behavior for every MCP write surface, including ledger, auto-ship, wiki, run, and universe-brain writes. — landed #1441 (verified at call sites on all five surfaces: `tinyassets/api/auto_ship_actions.py:88`, `runs.py:100`, `status.py:893`, `universe.py:147`, `wiki.py:2493`; classification gate `auth/middleware.py:396–402`; live `ui-test` 2026-07-14)
- [x] 2.0d Implement explicit public/private visibility state using `public_read`, separate from owner/admin grants. — landed #1437 (`tinyassets/api/permissions.py:6–16` docstring naming the ownership/visibility conflation this replaces, `:59–90` `universe_public_read_allowed` fail-closed; schema `tinyassets/daemon_server.py:114`)
- [ ] 2.0e Add a confirmation-gated visibility action for public -> private and private -> public transitions. — OPEN (no universe-level visibility action exists; `tinyassets/api/branches.py:2523 set_visibility` is for **branches**. Coordinate with `openspec/changes/universe-visibility/`.)
- [x] 2.0f Route cross-universe branch/community interactions through request/proposal surfaces rather than direct universe-brain writes. — landed (`tinyassets/api/universe.py:1439,1498,1513,1521` patch-request classification/incentive; `tinyassets/api/wiki.py:1662,1795` `patch_request` kind; relay `tinyassets/universe_server.py:1014,1097` `_BRAIN_WRITE_RELAY_ACTIONS`; direct cross-universe writes blocked by 2.0b)
- [ ] 2.0g Enforce Branch-run authority so the runnable actor is a universe and each run is recorded as a goal-bound Branch-use instance. — OPEN (**half landed**). Runnable-actor half IS enforced: `tinyassets/api/runs.py:90–107` (`branch_run_requires_universe`, *"Branches are run by universes"*) and `:110–125` (actor `universe:<uid>`). The **goal-bound Branch-use instance** half is **not implemented** — `goal_id` occurs in `runs.py` only at `:1593`/`:1624` as an isolation-tier *diagnostic string*, never as a binding recorded on a run. Left unchecked deliberately: checking it would silently delete the goal-binding work.
- [x] 2.1 Add a small generated-id helper for new universe ids using `u-` plus lowercase ULID, keeping the serial generator isolated. (`tinyassets/ids.py`.)
- [x] 2.2 Update MCP `universe action=create_universe` so `universe_id` is optional on create and generated when absent.
- [x] 2.3 Ensure the MCP create route seeds one linked OKF soul bundle during creation. (`tinyassets/universe_bundle.py::seed_okf_bundle`.)
- [x] 2.4 Ensure the MCP create route writes the full baseline files required by the spec.
- [x] 2.5 Remove creation-time persona name handling; treat any display label as metadata only if still needed by a live reader. (Create takes no `name`; persona name is learned via `identity.md`.)
- [x] 2.6 Implement `soul.edit.md` as the real soul edit policy file and make the execution path read/follow it, with versioning and log updates. — landed (`tinyassets/soul_edit.py` + `tests/test_soul_edit.py`; MCP action at `tinyassets/api/universe.py:5161`; reads the governed list from the policy file, requires source+context, ACL/scope/ledger-gated). Already cited by the checked task 1.8 — this change contradicted itself.
- [x] 2.7 Implement founder home-universe resolution for MCP first contact: authenticated founder -> existing home universe or new blank seed universe -> loaded soul/persona -> first-person universe voice. — landed #1462 (`tinyassets/api/status.py:689–725` `_resolve_entry_universe`, `:737` `ensure_founder_home` with scope-gate-before-reserve; auto-birth + binding live in production). ⚠ **The trailing `-> first-person universe voice` clause is REVERSED** by the 2026-07-02 relay reshape (`tinyassets/universe_server.py:208`) — the resolution shipped, the chatbot-voice behavior must **not** be rebuilt.
- [x] 2.8 Remove root `.active_universe` from MCP default routing. Explicit universe choices are request/client/session scope, not shared host state. — landed #1437 at all three call sites, each citing this spec by name: `tinyassets/api/helpers.py:88–116` (`_request_universe` routes an authenticated founder to their bound home, never the marker; `_designated_public_universe` never returns another founder's serial), `tinyassets/api/universe.py:4665–4680` (authenticated `switch_universe` returns `scope: request` and does not mutate the marker), `:4747–4760` (authenticated create does not write it). ⚠ **Scoped, by design:** the **anonymous / dev single-tenant** path deliberately retains the marker — `_default_universe()` (`helpers.py:73`) still reads it and anonymous `switch_universe` still writes it, because the tray app watches that file. The cross-founder leak this task targets is closed; the marker itself still exists.
- [~] 2.9 Remove duplicate `self/`/`soul/` directories, brain archive folders, and empty `notes.json` / `activity.log` starter files from active universe roots, and stop creating `self/`, `soul/`, `notes.json`, or `activity.log` for new universes. (NEW-universe half done: create no longer seeds notes.json/activity.log/self//soul/. The EXISTING-universe removal is the held live-data migration — GATED, see 2.10.)
- [ ] 2.10 GATED (live-data migration; `scripts/rename_live_data_universes_to_serial_ids.ps1` + `scripts/migrate_live_data_okf_baseline.ps1` + `scripts/remove_legacy_brain_artifacts.ps1`, run as a separate reviewed step against a live-data snapshot per handoff `:115`; held for canary + chatbot `ui-test`) Bring universe roots to generated serial-id folders matching `universe_id`.
- [ ] 2.11 Maintain the root universe index: add new serial ids at creation and update the learned-name column when `identity.md` learns or changes the universe's name. — OPEN (**half landed**). Registration at creation IS implemented (`tinyassets/daemon_server.py:610` `ensure_universe_registered`, called from the create route). The **learned-name** half is **not**: nothing syncs a learned `identity.md` name into the index `display_name`. `tinyassets/soul_edit.py:203,254` writes the name into `identity.md` frontmatter and stops. Confirmed by reading the consumer — `_action_list_universes` (`tinyassets/api/universe.py:1195–1245`) walks the **filesystem**, never queries the `universes` table, and returns no name field at all. The index currently has no name reader.
- [ ] 2.12 GATED (PR #1438 OPEN/unmerged; explicitly conditioned on "after auth/read/confirmed-write surfaces are stable" + a production deploy) Prepare a basic Android test app after auth/read/confirmed-write surfaces are stable; it must use the same MCP contract and platform-standard credential storage/integrity hooks.

## 3. Remove Duplicate Route

> ⚠ **This whole section is unbuilt.** The BREAKING change at the heart of this proposal has not
> happened: `POST /v1/universes` is **live on `main` and still creates universes with slugified
> names** (`fantasy_daemon/api.py:755–810`), which is exactly the duplicate creation path and the
> creation-time-name identity semantics the proposal exists to retire. Held for the host live-proof
> gate (AGENTS.md Rule 11/12), not merely undone.

- [ ] 3.1 GATED (breaking public-surface removal; held for public canary + chatbot `ui-test`, AGENTS.md Rule 11/12 — handoff `docs/exec-plans/2026-06-30-founder-identity-allslices-handoff.md:115`) Remove or reject `POST /v1/universes` in `fantasy_daemon/api.py` so it cannot create universes. **Still fully live at `fantasy_daemon/api.py:755–810`** — `mkdir`, `ensure_universe_soul`, `canon/`, `output/`, `universe.json`.
- [ ] 3.2 GATED (with 3.1) Remove slug-name creation behavior and tests that treat slugified names as universe ids. **Still live** — `fantasy_daemon/api.py:356` (`_slugify`), `:770` (slug minted from `name`).
- [ ] 3.3 GATED (with 3.1 — nothing to preserve until the removal runs) Preserve non-create HTTP read/list behavior only where current tests or live clients still require it.

## 4. Verification

- [ ] 4.1 Run focused universe creation, universe soul, persona/soul-model, and affected HTTP API tests. — OPEN (cannot complete while 3.1/3.2 are unbuilt; the task names "affected HTTP API tests")
- [ ] 4.2 Run an MCP-level create/status smoke test against a temporary data dir. — OPEN (no recorded run for this change)
- [ ] 4.3 GATED (blocked by 3.1/3.2 — docs cannot stop teaching a route that still works; e.g. `docs/historical/ARCHITECTURE_PLAN.md:255`) Update docs or prompt examples that still instruct users to create universes through HTTP or creation-time names.
