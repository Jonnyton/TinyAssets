## 1. One trusted writer

- [x] 1.1 `write_brain` records a bounded per-turn proposal and returns `{"status": "proposed"}`; it no longer calls any soul-edit sink. Verify: a test that goes red if `write_brain` persists (mutation: restore the direct commit); proposal captured with the section cap enforced.
- [x] 1.2 The turn id binds through the TRANSPORT, not a shared file: `ModelConfig.engine_mcp_turn_id` → `TINYASSETS_ENGINE_TURN_ID` for the stdio child, `<secret>.<turn_id>` on the loopback bearer for the persistent HTTP server (claude headers + codex `bearer_token_env_var`), validated against `[A-Za-z0-9_-]{1,64}` and bound per REQUEST by `_parse_bearer` + module-level `BearerAuth`. Slot is `<universe>/.runtime/brain_proposal.<turn_id>.json`, consumed by exact turn, swept after an hour. Verify: interleaved turns A/B keep their own proposals (mutation: one shared slot); two simultaneous HTTP requests each see their own turn (mutation: process-global); the three wirings carry the id; a turn id alone never authenticates; a malformed turn half is dropped.
- [x] 1.3 Extraction returns ONLY `{"remember": ["<whole founder sentence>"]}`. The sink accepts a candidate only when it EQUALS a whole sentence of the founder's message (≥ 3 words; whitespace-normalised, case-preserving; newlines are NOT sentence boundaries), appends it verbatim and quoted to `learned.md` with the turn id, and requires turn id + utterance. Empty utterance → proposal dropped with a logged reason. Verify: "Do not call yourself Root." cannot be reduced to "Root" (mutation: substring); a fabricated sentence is dropped; reply-only content never persists; the log preserves prior entries and is idempotent.
- [x] 1.4 The extraction cannot choose a DESTINATION: a section key, a `name` or a `canon` page in the reply is ignored and logged; `name`/`canon` are gone from the extraction path and canon is untouched by conversation. Verify: an old-shape `{"soul": {...}}` payload with a real founder sentence writes nothing (mutation: honour a section key); a canon-shaped reply never reaches `write_universe_canon` (patched to raise).
- [x] 1.5 The read→append→write of `learned.md` runs inside the per-universe soul lock via `apply_soul_edit(transform=…, transform_files=…)`. Verify: a concurrent turn forced into the window keeps both entries (mutation: read outside the lock → RED).
- [x] 1.6 `learned.md` has a 16 KB prompt budget; past it the oldest entries move to `learned-archive.md` (governed, not grounding, readable via `read_brain`) with a log line. Verify: 200 entries + one more → archive created, log within budget, every entry preserved across the two files (mutation: no cap).
- [x] 1.7 `apply_soul_edit` takes a minted provenance object, never a string: `FounderUtteranceProvenance` (module-private key + a grep test for the single minting call site in `commit_founder_learning`) and `DirectEditProvenance` (built only by `commit_direct_soul_edit`). `universe action=soul.edit` calls `commit_direct_soul_edit`. Verify: `apply_soul_edit(source="founder utterance turn_X")` raises (mutation: accept it); direct construction of the founder type raises; the action cannot forge conversation provenance.

## 2. Untrusted by construction

- [x] 2.1 `read_commons_shape`, `browse_commons`, `read_graph target="branch"` (foreign author or remixed off-universe), and `read_graph target="run"` / `run_graph` results return the untrusted envelope; one line in the persona prompt names it. An error WE produced is returned bare — enveloping it would make the notice a false claim. WebFetch is CLI-native and cannot be enveloped at this boundary — recorded in design.md, and D2 makes it moot for persistence. Verify: envelope shape tests (own vs foreign branch, run output); our own errors are never enveloped (mutation); an end-to-end test where a commons shape containing instructions is read during a turn, proposed, echoed in the reply, and returned by the extractor — and no brain file changes.
- [x] 2.2 `orgchart.md` joins `_GROUNDING_FILES` (written by the brain loop, never read back) and `FOUNDER_PRIVATE_GROUNDING`; `learned.md` joins both and `learned.md` is rendered as QUOTES under an intro line, inside `# What I know so far`. Verify: founder prompt contains both, visitor prompt contains neither; the intro line is present.

## 3. Prove and close

- [ ] 3.1 Codex refutation (round 3); live proof through the app: (a) tell the universe a fact, next turn it knows it and `read_brain` shows the turn + digest; (b) have it read a commons shape seeded with an instruction, confirm nothing persisted. Sync deltas, delete `docs/concerns/2026-08-24-write-brain-prompt-injection.md`, archive.
