## 1. One trusted writer

- [x] 1.1 `write_brain` records a bounded per-turn proposal and returns `{"status": "proposed"}`; it no longer calls the soul-edit sink. Verify: a test that goes red if `write_brain` persists (mutation: restore the direct commit); proposal captured with the section cap enforced.
- [x] 1.2 The turn id binds through the TRANSPORT, not a shared file: `ModelConfig.engine_mcp_turn_id` → `TINYASSETS_ENGINE_TURN_ID` for the stdio child, `<secret>.<turn_id>` on the loopback bearer for the persistent HTTP server (claude headers + codex `bearer_token_env_var`), bound per REQUEST by `_parse_bearer`. Slot is `<universe>/.runtime/brain_proposal.<turn_id>.json`, consumed by exact turn, swept after an hour. Verify: interleaved turns A/B keep their own proposals (mutation: one shared slot); the three wirings carry the id; a turn id alone never authenticates.
- [x] 1.3 `extract_learning` takes the founder utterance and the proposal only (reply and tool output removed), and returns SPANS. `commit_founder_learning` accepts a span only when it is verbatim founder wording (`verify_spans`: whitespace-normalised substring, case-preserving), appends it as a delta under `## Learned` with the turn id, and records `source="founder utterance <turn_id>"` + the utterance digest; it REQUIRES turn id + utterance. Empty utterance → proposal dropped with a logged reason. Verify: a malicious extractor's unsupported span lands nowhere (mutation: drop the substring check); candidate wording never persists; reply-only content never persists; a delta preserves prior facts (mutation: replace-body); `read_brain` shows provenance.
- [x] 1.4 `commit_direct_soul_edit` is the separately named non-turn entry point, recording `source="founder direct edit (<actor>, <surface>)"`; `universe action=soul.edit` DERIVES that source instead of accepting one from the caller. Canon spans are verified, appended, and carry a provenance line + the turn id in the wiki log. Verify: the action cannot forge conversation provenance (mutation: accept `claimed`); the founder path refuses without provenance (mutation: drop the guard).

## 2. Untrusted by construction

- [x] 2.1 `read_commons_shape`, `browse_commons`, `read_graph target="branch"` (foreign author or remixed off-universe), and `read_graph target="run"` / `run_graph` results return the untrusted envelope; one line in the persona prompt names it. WebFetch is CLI-native and cannot be enveloped at this boundary — recorded in design.md, and D1/D2 make it moot for persistence. Verify: envelope shape tests (own vs foreign branch); an end-to-end test where a commons shape containing instructions is read during a turn, proposed, echoed in the reply, and returned by the extractor — and no brain file changes.
- [x] 2.2 `orgchart.md` joins `_GROUNDING_FILES` (written by the brain loop, never read back) and `FOUNDER_PRIVATE_GROUNDING` (it names the founder's collaborators). Verify: founder prompt contains it, visitor prompt does not.

## 3. Prove and close

- [ ] 3.1 Codex refutation (round 2); live proof through the app: (a) tell the universe a fact, next turn it knows it and `read_brain` shows the turn + digest; (b) have it read a commons shape seeded with an instruction, confirm nothing persisted. Sync deltas, delete `docs/concerns/2026-08-24-write-brain-prompt-injection.md`, archive.
