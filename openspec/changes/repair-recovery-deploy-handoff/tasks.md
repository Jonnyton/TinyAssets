## 0. Evidence And Contract

- [x] 0.1 Record the production-reproduced writer/sidecar collisions, failed
  candidate and terminal-receipt contracts, audited recovery provenance,
  transient systemd mismatch, bounded handoff design, and executable deltas.

## 1. Test-Driven Repair

- [x] 1.1 Carry exact finalized-recovery writer provenance through normal
  preflight; durably retire/replay only the exact stopped generation, and admit
  only an exact proved partial canonical target to unsafe recovery.
- [x] 1.2 Bind fixed-name sidecars by exact ID, finite audited project/service,
  pinned image, exact read-only bind mounts, and restart posture; safely replay,
  retry, re-fence, reboot-reconcile, and hand them to canonical Compose.
- [x] 1.3 Capture failed candidate startup evidence with fixed classifications,
  repository-proved paths, hard deadlines, exact image/revision binding, safe
  publication gates, and no raw host/log/environment disclosure.
- [x] 1.4 Preserve forward, rollback, cleanup, and running-container terminal
  truth independently, including a safely fenced post-forward cleanup failure.
- [x] 1.5 Close review findings for configuration spoofing, same-name
  substitution, interrupted replay, mixed ownership, writer-first fencing,
  finite historical authority, and exact frame parsing.
- [x] 1.6 Reproduce run `30672569902`'s watchdog and daemon transitions; capture
  only settled unit state before mutation, refuse a nonsettling unit, normalize
  daemon startup to active, and preserve exact saved enablement on restore.

## 2. Verification And Release

- [ ] 2.1 Pass the combined focused suite, Ruff, strict OpenSpec validation,
  clean diff, and independent exact-head fail-closed/security review for the
  integrated stable-unit successor.
- [ ] 2.2 Land the repair, deploy one immutable image through the normal fence,
  and record exact-five canonical fleet plus public MCP canary evidence.
- [ ] 2.3 Resume the OAuth diagnostic deployment only after the repaired normal
  deploy completes from a finalized recovery generation.
- [ ] 2.4 Sync the deltas into their canonical specs, archive this change, and
  retire its STATUS row after live acceptance evidence is durable.
