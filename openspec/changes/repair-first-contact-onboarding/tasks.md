## 1. Ownership and review gates

- [ ] 1.0 Before this lane authors production SQL, claims a live Postgres adapter, or deploys a catalog write, identify or propose a separate OpenSpec baseline for the production Postgres migration runner, role/RLS model, recovery, stock-Postgres exit, and first-write approval. Obtain host and opposite-provider acceptance and an allocated exact branch-catalog migration filename. This lane consumes that substrate and MUST NOT create a second runner, schema history, service credential, or Supabase-only authority.
- [ ] 1.1 Refresh current main and obtain file-specific landed commits or explicit releases from active universe-creation/visibility, `control_station`, scoped-wiki-canary, identity/secret-catalog, personification, main-red, and other owners surfaced by the exact future Files check; preserve newborn-BYOC behavior landed in #1759. Retire-legacy caller inventory v4 landed in #1772; retain the `publish_version` and `approve_source_code` gates now recorded in retire-legacy tasks 2.3a/4.0 and STATUS, and require any future retirement owner to accept them before implementation. Inherit landed prompt text and exact-handle tests without inheriting unrelated dirty response-guidance expansion.
- [ ] 1.2 Obtain Claude Opus 5 opposite-provider review of the exact spec/evidence head; resolve every Critical/Important or ADAPT item and re-review.
- [ ] 1.3 Keep V1 commons-only. Reject non-public visibility before persistence; choose no private-data custody mode and rule none in or out. Any private authoring route requires a separate PLAN-approved change that names its selected custody mode, trust boundaries, storage, and routing.
- [ ] 1.4 Treat a visibility-safe `read_graph(target="branch")` projection and every shared exact alias/helper as a hard runtime, create/publish-mode, catalog, deploy, prompt, and rendered-acceptance prerequisite; obtain the universe-visibility owner's accept/adapt.
- [ ] 1.5 Add `TINYASSETS_BRANCH_CRYPTO_KEYRING` to the deployment host-action lane with named rotation owner, current/prior retention, and validation receipt; no catalog/create/publish runtime claim proceeds without it.
- [ ] 1.6 After tasks 1.0/1.1 release every file, re-run file-collision and provider-context checks and create a runtime STATUS claim whose Files name the single allocated Postgres migration filename plus exactly `tinyassets/storage/postgres_catalog.py`, `tinyassets/universe_server.py`, `tinyassets/daemon_server.py`, `tinyassets/branch_versions.py`, `tinyassets/rollback.py`, `tinyassets/api/{branches,evaluation,selector_dispatch,prompts,wiki,universe,market}.py`, `scripts/secrets_keys.txt`, `docs/reference/environment-variables.md`, `WORKFLOW_DESIGN_HANDOFF_FOR_POLSIA.md`, `packaging/claude-plugin/plugins/tinyassets-universe-server/runtime/tinyassets/**`, `tests/postgres/test_branch_catalog_postgres.py`, `tests/postgres/test_branch_catalog_migration.py`, `tests/test_branch_authoring_catalog.py`, `tests/test_mcp_instruction_surfaces.py`, and `tests/load/test_branch_catalog_concurrency.py`. Treat the shared Postgres migration runner/history and requester-authority owners as Depends/read-only files. If migration, lifecycle scheduling, rollback, or projection integration requires another production file, replace this exact claim before editing; never claim broad directories or broad `tests/`.
- [ ] 1.7 Reconcile owner truth before any runtime claim. For universe-creation and universe-visibility, a STATUS actor/date is not a release: require an `origin/main` landed SHA, an explicit file-specific release/handoff recorded in STATUS/worktree metadata, or stale-claim reaping/reassignment; a remote-gone branch with unmerged commits is neither landed nor released. Require the `control_station` owner to land/release its prompt-only slice and exact-handle invariant and to split or fully declare its dirty response-guidance Files before handoff. At minimum obtain explicit releases for direct overlaps `tinyassets/api/evaluation.py` and `tests/test_mcp_instruction_surfaces.py`. Record `worktree_status.py`, exact `claim_check.py --check-files`, provider-context build feed, and owner-head divergence evidence with consumed SHAs.
- [ ] 1.8 After runtime/load gates land, create a separate deploy/acceptance STATUS claim with Files exactly `output/user_sim_session.md`, `docs/ops/2026-07-25-first-contact-rollback-rehearsal.md`, and `STATUS.md`; Depends exactly on the landed runtime SHA, exact-branch visibility proof, provisioned branch keyring, production deployment receipt, retained prior image, canonical ChatGPT connector registration, owned `scope=commons` route, and retire-legacy tasks 2.3a/2.3b/4.0. Canaries, production logs, and deployment configuration are read-only dependencies unless the claim is explicitly revised before editing.

## 2. Failing public-contract tests

- [ ] 2.1 Add failing exact-surface tests for `read_graph(target="branches")`, closed `write_graph(target="branch")` create/patch/publish discrimination, target-scoped idempotency docs, and no legacy/eighth registration.
- [ ] 2.2 Add failing duplicate-member and positive-schema tests for every typed top-level/node/edge/conditional-edge/state-field key, reserved `START`/`END` directionality, every-node-reaches-`END`, prompt-placeholder declaration, and the literal one-node `step1 -> END` successful definition; also cover exact `str|int|float|bool|list|dict|any` compiler semantics, null-versus-omitted defaults, every protected/unknown nested key, body/collection/string/numeric/output caps, prompt-template-only enforcement, minimal projections, and exact error enums.
- [ ] 2.3 Add failing authority tests proving verified-principal authorship/publisher identity, no environment fallback or MCP `force` bypass, no graph-membership authority, no caller/generated-ID overwrite, V1 rejection of fork/Goal/source/private shapes, and shared patch-helper rejection of author/approval fields with source approval cleared on mutation.
- [ ] 2.4 Add failing real-Postgres catalog tests for `published|mine`, verified actor/tenant binding, forced-RLS and direct-DML denial, legacy free-text omission, actionable anonymous-mine error, commons predicate, exact normalization, Goal-key non-disclosure, default 30/max 100, immutable pagination, bounded query plans, startup-readiness/no-request-write/post-fork/restart/rotation/counter-exhaustion AES-GCM nonce leases across real issuer processes, closed cursor failure, active canonical version/publication verification, immediate patch refresh, and zero restricted/private projection. Inject stale, missing, forged, and conflicting SQLite/YAML rows and Postgres/pool outage; prove no fallback, dual-write, or reverse reconciliation. Inject a lost/delayed projection event and prove named bounded maintenance repairs it idempotently after restart without read-path writes or revision regression.
- [ ] 2.5 Add failing Postgres transaction tests for create, patch, and publish commit points; same-body replay; historical publish receipt without current eligibility restoration; key conflicts/rotation/expiry; 100 identical retries; forced pre-commit failure returning `branch_write_failed`; response loss after commit; UUID collision; validation rollback; and one canonical result. Inject SQLite outage and consumer crashes between pre/post snapshot roles plus repeated delivery; prove canonical success remains committed, the outbox stays retryable, and exactly one logical operational snapshot exists per event role.
- [ ] 2.6 Add failing deliberate-publication tests proving stored-row public eligibility, create starts unpublished, patch/evaluation/selector `intent="snapshot"` creates no publication, default-selector bootstrap remains principal-free and non-publishing, the known-risk legacy publisher creates no catalog authority, service `catalog_publish` is rejected, owner publish is idempotency-bound, and operational metadata is never authority. Race two distinct idempotency keys publishing different validated snapshots of one Branch and race owner publication against rollback; prove unique strictly increasing revisions, at most one active publication, highest committed revision eligibility, no regression when an older/superseded version rolls back, and a fresh owner publication after rollback creates a newer active version/publication.
- [ ] 2.7 Add failing catalog/create/publish-to-exact-read tests proving a returned Branch ID cannot disclose restricted related-wiki metadata, top-level `goal_id`, or Goal-derived `gate_claims` through `read_graph(target="branch")`, `get_branch`, `describe_branch`, or shared helpers. Cover public, private, deleted, missing, unauthorized, and resolver-error Goals; missing/private/deleted/nonexistent responses must be indistinguishable, and all three new modes stay unavailable while any exact path is unsafe.

## 3. Branch authoring and catalog owner

- [ ] 3.1 Implement the versioned closed request/result/error DTO boundary, RFC 8785 digests, exact compiler type vocabulary, numeric/default-value caps, prompt-template-only nested allowlists, reserved-sentinel endpoint positions, declared-node entry/reachability-to-`END`, prompt-placeholder declaration, and protected-field rejection before staging, persistence, or existing-handler dispatch.
- [ ] 3.2 After the approved Postgres baseline allocates the exact migration filename, add one production-native branch-catalog migration and connection-accepting adapter owning canonical Branch definitions/versions, verified owner, projection, publication/revision, idempotency alias/result, nonce-lease, and outbox rows under forced-RLS-compatible least privilege. Never infer authority from legacy/free-text author or environment fallback; implement the post-fork issuer-readiness lifecycle outside request handling, retain idempotency state at least 24 hours, and do not modify the shared migration runner/history owner.
- [ ] 3.3 Implement `TINYASSETS_BRANCH_CRYPTO_KEYRING` parsing/HKDF/catalog docs, retained-key aliases across rolling rotation, lease-before-current key activation with prior keys decrypt-only, same-body replay, changed-body conflict, pre/post-commit crash recovery, collision retry, and stable outbox-event replay; optional consumers transact effect+dedupe by `event_id` or use inherently idempotent effects.
- [ ] 3.4 Harden full-definition staging to derive branch/node authorship, reject fork/Goal/source/private shapes, avoid attempted-definition echo, and return the minimal create result.
- [ ] 3.5 Harden shared patch staging reached by canonical and retained aliases so caller authorship/approval/IDs and MCP `force` bypass are rejected, new-node author is derived, admitted source mutation clears approval, and `set_published` directs callers to publish mode. Commit the canonical Branch revision, projection fields, and immutable pre/post operational-snapshot outbox events in one Postgres transaction. Remove synchronous SQLite snapshot writes from the request path; patch failure rolls back only the authoritative transaction and never changes publication state.
- [ ] 3.6 Give `publish_branch_version` a closed `snapshot|catalog_publish` discriminator in the same Postgres authority. Route `branches.py`, `evaluation.py`, and `selector_dispatch.py` operational intents through `snapshot` without a request principal or publication; reserve `catalog_publish` for verified owner mode and preserve selector bootstrap. Commit canonical snapshot, publication/current revision, idempotency state, and outbox events together. Integrate `tinyassets.rollback.execute_rollback_set` into that transaction so each affected active publication becomes `rolled_back` with exactly one next ineligible revision/event; repeated/concurrent rollback is safe and a superseded rollback cannot regress a newer pointer. Implement the SQLite operational-snapshot consumer with same-transaction `(event_id, snapshot_role)` dedupe.
- [ ] 3.7 Implement catalog filtering, immutable ordering, cursor issuance, and active version/publication verification against Postgres only with one bounded query/transaction snapshot and no local mirror as positive authority. Add named `reconcile_branch_catalog_projection_once` maintenance with bounded batch/high-water mark, runtime startup plus periodic trigger, concurrent/restart-safe locking, non-secret health telemetry, and all writes outside MCP read handling.
- [ ] 3.8 Build a reviewed one-way legacy import/cutover: quiesce shared legacy writes, preserve a source snapshot and hashes, import idempotently, reconcile rows/content, shadow-read until divergence is zero, obtain the baseline's first-write approval, and disable legacy shared mutation. Do not dual-write. Local-only OSS namespaces remain local until explicitly imported.

## 4. Canonical MCP routing

- [ ] 4.1 Repair every exact Branch projection and shared helper so page/universe visibility and the canonical Goal visibility predicate run before projection. Omit hidden `goal_id`; filter `gate_claims` before deriving items, counts, booleans, or truncation metadata; fail closed to no Goal-derived metadata when the predicate is unavailable. Keep new create/catalog/publish modes unavailable until both restricted-page and hidden-Goal proofs pass.
- [ ] 4.2 Add `definition_json` create and explicit `publish=true` modes to `write_graph`, reject mixed/incomplete modes before dispatch, and preserve all-or-none patch staging without falsely claiming patch idempotency or CAS.
- [ ] 4.3 Add `read_graph(target="branches")` scope/domain/cursor parameters without changing existing target defaults; reject non-default `goal_id` plus every other unrelated shared-handle field and delegate only to the hardened catalog owner.
- [ ] 4.4 Re-scope the `idempotency_key` docstring/metadata to create and publish only, update publish/definition parameter descriptions, allowed-target errors, structured envelopes, and the packaged runtime through `python packaging/claude-plugin/build_plugin.py`; prove mirror parity and add drift coverage.

## 5. Prompt, help, and repository guidance truth

- [ ] 5.1 After the active `control_station` prompt-truth lane lands or explicitly hands off its write boundary, inherit rather than overwrite its `control_station` text and exact-handle invariant test; then reconcile all four registered prompt bodies/docstrings so first contact starts with `converse`, the starter uses the literal one-node `step1 -> END` definition, and prompt-template V1 discovery/create/inspect/publish/rediscovery uses canonical target shapes. Treat `universe-creation` 2.0 and 4.1–4.7 plus R2-1a/R2-1b as named hard dependencies for executable run guidance: before they land, prompts end at rediscovery; afterward, add only requester-authority-gated optional run wording and inherit the owner's exact held/error envelopes.
- [ ] 5.2 Remove or replace retired-tool advice from first-contact user-facing strings in `tinyassets/api/{prompts,branches,wiki,universe,market}.py` and `tinyassets/universe_server.py`; where no canonical equivalent exists, remove the advice instead of inventing one.
- [ ] 5.3 Remove source-code/fork/Goal-binding instructions from first-contact guides; broader source guidance must disclose the missing canonical approval route and current non-OS-isolated execution boundary. Excluding fork/remix from V1 guidance does not authorize retirement of the existing remix/lineage seam; depend on retire-legacy task 2.3b.
- [ ] 5.4 Correct or mark historical `WORKFLOW_DESIGN_HANDOFF_FOR_POLSIA.md`, regenerate the packaged runtime, extend the prompt-truth lane's exact-handle invariant instead of duplicating it, and add drift tests scanning prompts/help for retired invocation syntax, stale "five tools" text, opaque-ID-only onboarding, excluded V1 shapes, false OS-sandbox claims, and both run-guidance states. Without accepted requester authority the four prompts expose no executable run journey; with it they require the resolved requester bundle and contain no maintainer/founder/environment fallback.

## 6. Focused and concurrent verification

- [ ] 6.1 Run the new first-contact contract suite plus existing branch read/edit/composite/authoring, handle metadata, exact-registration, anonymous-challenge, and public-canary suites.
- [ ] 6.2 Mutation-test branch/node/publisher spoofing, stored-row visibility bypass, one raw hidden `goal_id`, one unfiltered hidden-Goal claim, one pre-filter claim count, one visibility-resolver fallback, self-approval patch, `force` bypass, caller/generated-ID overwrite, private/fork/Goal/source create, duplicate JSON members, multi-instance nonce/tag tampering or reuse, cursor starvation, false/out-of-order/rolled-back publication, removed caps, definition echo, mixed write mode, idempotency/keyring bypass, one reintroduced hidden registration, and one stale/BYOC-unsafe prompt.
- [ ] 6.3 Run the declared 500-client/1,000-operation 750-read/200-create/50-publish fixture against the approved production-shaped Postgres topology, application role/RLS boundary, pool, migration history, and at least two real cursor issuers. Record pool/query plans/counts, p50/p95/p99, deadlocks/retries, exact-surface result, restricted-data assertions, full 400-candidate window, completed pagination, nonce uniqueness, canonical commit/publication-race outcomes, and one-winner results. Run the SQLite consumer healthy, delayed, unavailable, and crash-restarted; inject Postgres outage plus stale/forged/missing local projections and prove fail-closed behavior, zero fallback/dual-write, bounded outbox lag, convergence, and no duplicate logical snapshots.
- [ ] 6.4 Run scoped Ruff, plugin build/parity, `git diff --check`, strict validation of this change, and the repository-required test gate; obtain independent whole-diff spec and quality review.

## 7. Deploy and rendered acceptance

- [ ] 7.1 Deploy only after every catalog-to-exact alias proves restricted-page and hidden-Goal non-disclosure, the keyring is provisioned, and Postgres create/catalog/publish gates fail closed; record source SHA/image/release receipt and run `scripts/mcp_public_canary.py --assert-handles` against `https://tinyassets.io/mcp`.
- [ ] 7.2 Complete the real browser-rendered chatbot journey: browse without a prior ID, create and inspect a public prompt-template V1 branch, explicitly publish it, rediscover it, and save prompt/result plus trace or screenshot to `output/user_sim_session.md`. Do not run unless the exact `universe-creation` authority/isolation dependencies are deployed and the trace proves the admitted requester-owned/accepted-market receipt; otherwise stop at rediscovery and record run as intentionally unavailable.
- [ ] 7.3 Check for post-fix organic use; if none is visible, say so and leave a concise STATUS monitoring item.
- [ ] 7.4 Before declaring rollback available, retain and boot the exact prior image against a copy of the post-migration databases; prove startup/read compatibility, new catalog/create/publish modes fail closed, required hidden legacy registrations return, no new publication/projection state mutates, and exact-surface plus retirement canaries report the expected rolled-back state. Record prior/new SHA, database snapshot identifier, commands, results, and restore-forward procedure in `docs/ops/2026-07-25-first-contact-rollback-rehearsal.md`; failure blocks deployment and retirement.

## 8. Live wiki serialized hash-guard lane

- [ ] 8.1 Re-read every direct page in the evidence manifest, refresh the exact full SHA-256, and expand the lower-bound inventory with privileged `scope=all` or volume export before claiming exhaustive coverage.
- [ ] 8.2 Depend on the accepted `scope=commons` implementation owned by `reconcile-universe-personification-relay` and on a separately reviewed serialized/locked single-writer boundary. Require owned tests to prove authenticated `write_page(scope="commons", ...)` writes only commons while omitted scope continues to resolve/relay to the founder universe and cannot silently patch commons. Do not duplicate or bypass that owner.
- [ ] 8.3 After both owned gates land, run each correction as authenticated `write_page(scope="commons", dry_run=true, expected_sha256=..., old_text=..., new_text=...)`; record target, pre-image, proposed post-image, audience, and exact-one-match proof, use supersession banners for historical pages, and stop on any scope/hash mismatch.
- [ ] 8.4 Only after 8.2/8.3 plus repository/runtime and opposite-provider gates pass, apply authenticated `write_page(scope="commons", dry_run=false, ...)` one page at a time; reread explicitly from commons, record guarded-write and post-image hashes, and stop on any relay response, scope mismatch, or conflict.

## 9. Foldback

- [ ] 9.1 Do not sync while canonical truth still contains the legacy-registration requirement. Reconcile the baseline and concurrent MODIFIED `Canonical Advertised Handle Set` deltas to this reviewed target, prove sync idempotence, and strict-validate the full tree:
  - Merged requirement: the live server registers and advertises exactly `read_graph`, `write_graph`, `run_graph`, `read_page`, `write_page`, `converse`, and `get_status`; each stays a thin shape/target router to its existing `tinyassets.api.*` handler; target validation/authority/hardening lives at shared handler/staging boundaries inherited by canonical, internal, and temporarily retained aliases; the public canary requires exact equality, `get_status`, and no additional or hidden registration.
  - 1. **Live surface advertises exactly the seven handles** — middleware-applied `tools/list` equals the exact set and `converse` is present, preserving the `tests/test_universe_server_five_handles.py` attribution.
  - 2. **Middleware-bypassed registry contains exactly the seven handles** — the raw registered set equals the same seven and contains no hidden/deprecated registration.
  - 3. **A canonical handle routes to its existing API handler** — `write_graph(target="goal", ...)` followed by `read_graph(target="goals", ...)` uses the same goals handler and returns the proposed goal.
  - 4. **A canonical branch target routes through the shared hardened owner** — canonical and retained branch aliases apply identical protected-field, verified-authority, visibility, and bounded-result invariants.
  - 5. **run_graph completes a live MCP-to-storage round trip** — an authenticated deterministic no-provider run returns a durable ID that registered `read_graph(target="run")` reads back without replacing registration/storage with direct calls, AST/schema checks, or stubs.
  - 6. **Branch onboarding extends targets and parameters, not handles** — enabling catalog/create/publish keeps raw and advertised sets at seven and registers no standalone list/build/publish tool.
  - 7. **An unknown router target is reported, not silently accepted** — `read_graph(target="bogus")` returns `error="unknown_target"`, `handle="read_graph"`, and allowed targets.
- [ ] 9.2 Archive the completed change and retire its STATUS row only after runtime, load, deployed canary, rendered chatbot, and live-wiki evidence are truthful.
