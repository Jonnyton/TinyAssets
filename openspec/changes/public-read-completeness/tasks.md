## 0. Contract and claim gates

- [x] 0.1 Validate the proposal, design, and all three delta specs with `openspec validate public-read-completeness --strict` and `openspec validate --all --strict`; record the exact current-base evidence without claiming runtime behavior. (2026-07-27 Windows, `86c85083` base: change valid; full tree 59/59.)
- [x] 0.2 Obtain an exact-current Claude Opus 5 review of `proposal.md`, `design.md`, all delta specs, and this task map; fold every blocking finding and retain the final verdict. (2026-07-27: rounds 1–17 `ADAPT` were folded; exact-current round 18 returned `APPROVE` for safe local commitability and slice-0A implementation authority while the tracked publication gate remains in force. Evidence: `output/public-read-completeness-review/public-read-completeness-opus5-r18-output.md`.)
  Round 12 `ADAPT` replaced public global sequence leakage with contiguous visibility-partition sequences, separated visibility rebaseline from actor-context rebound, generalized cross-mode fail-loud behavior and normalized redundant-boundary matching, corrected shipped Goal defaults, allowlisted public Goal visibility, and made Goal digests payload-safe.
  Round 13 `ADAPT` reconciled exact-read ambient filters with fail-loud routing, made the Goal digest implementable, froze event partition membership at commit with transition-only widening, scoped sequence contiguity to unfiltered traversal, narrowed the Worker header exception, made actor quotas cross-surface, covered universe visibility and current-path backfill, documented handle-qualified scope enums, and required `--assert-handles`.
  Round 14 `ADAPT` aligned routing/backfill scenarios with their requirements, excluded capture-time withdrawals from the sequence-contiguity oracle, corrected secrets-hash wording, added dedicated digest-key lifecycle, pinned query-routing migration, declared the Worker non-hop-by-hop boundary, made digest asymmetry and scope enums testable, and retained explicit staging safety for review scratch.
  Round 15 `ADAPT` corrected rebaseline/rebound descriptions, kept host-gated scope out of slice 1A, split an ungated public-Goal list/search leak hotfix into slice 0A with a live STATUS Concern, made capture-exclusion gaps explainable, completed Worker merge/scenario fidelity, added the wiki per-principal forward guard, and closed boundary-scenario/documentation gaps.
  Round 16 `ADAPT` extended slice 0A to exact Goal lookup, made its public-Goal requirement independently syncable, blocked public publication until closure or host acknowledgment, assigned exact-read category to 1A, restored 1C in the slice sequence, added capture-rule and actor-context forward pointers, and clarified rendered-matrix and STATUS-janitor evidence.
  Round 17 `ADAPT` moved all exploit-mapping review scratch outside the repository, promoted publication/staging safety into this tracked task map, narrowed exact-read filtering to the public action path while protecting shared `get_goal` consumers with regression suites, and documented temporary loss of private-author connector read-back.
- [ ] 0.3 Obtain a host decision specifically on newly advertising anonymous `scope=coordination|all` on canonical public `read_page` before slice 1B; legacy `wiki` never exposed scope and slice 1A/removal are not gated by this choice. If approved, retain the scope clauses/scenarios in the `wiki-commons` and `live-mcp-connector-surface` deltas and build 1B. If denied, amend those two delta specs plus `proposal.md`, `design.md`, and this task map before sync. Before slices 2/3, obtain host approval or revision of the proposed public snapshot TTL/rate/count/byte envelope. Separately, resolve the Goal storage substrate and dedicated versioned snapshot-digest key provisioning gates before slice 3, and obtain the already-open host decision for an isolated production-load traffic envelope before task 4.4; numeric thresholds do not authorize production load.
- [ ] 0.4 Before each runtime slice, run `claim_check.py --check-files` for the exact canonical files, matching packaged mirrors under `packaging/claude-plugin/plugins/tinyassets-universe-server/runtime/tinyassets/` (`universe_server.py`, `api/wiki.py`, `api/market.py`, `daemon_server.py` as applicable), `packaging/claude-plugin/build_plugin.py`, and for anonymous quota work `deploy/cloudflare-worker/worker.js` plus its tests and origin provenance seam; wait for or receive handoff from active owners, update STATUS Files/Depends, run the provider context feed for build, and reconcile concurrent `live-mcp-connector-surface` changes before wrapper edits. Wiki snapshot work also waits for the canonical commons topology from `build-brain-canonical-store` and reconciles every withdrawal/restoration predicate and partition-membership transition from `moderation-and-abuse-response` and `universe-visibility` before inventory-transition/tombstone emission; Goal snapshot work reconciles `establish-postgres-control-plane` and `per-user-goal-canonicals`; load execution waits for the future `implement-production-load-harness`.
- [ ] 0.5 Before task 1C.1 or this change's sync, reconcile this change's full-body `Cloudflare Worker Public Front Door` delta with the competing full-body delta in `reconcile-external-connector-manifests`; this change SHALL NOT sync until that owner records a reciprocal task/Depends edge and both owners agree one merged requirement preserving its accepted manifest/front-door behavior plus the shipped non-hop-by-hop request-header boundary, the intentional exact `X-Forwarded-For` exception and authoritative overwrite, missing-value strip-and-proxy, origin `anonymous_partition_unavailable` denial, and every then-canonical clause/scenario not intentionally modified by either accepted change.

Publication/staging guard: keep all provider-review scratch outside the repository, use explicit path staging only, never stage unrelated `.claude/.fleet_*` state, and do not push or open a PR until slice 0A is deployed/synced or the host explicitly acknowledges earlier public disclosure.

## 0A. Public Goal visibility hotfix (gate independent)

- [ ] 0A.1 Add RED focused tests that pin all live anonymous leaks without publishing exploit instructions: empty-query Goal listing, ranked `target=goals` search, and exact `target=goal` lookup can disclose non-public Goals; then require all three canonical public modes to include or return only exactly `visibility=public`, with private, deleted, unrecognized, and missing exact lookups sharing a non-disclosing missing-result shape and no non-public IDs, fields, counts, or ranked results regardless of author/tag/query/actor inputs.
- [ ] 0A.2 After narrow runtime/test claims release, implement the exact-public allowlist in the owning list/search storage predicates and canonical wrappers, but scope exact-read filtering only to the public `read_graph target=goal` action path—never the shared `get_goal` primitive used by owner updates, gates/claims, bind semantics, canonical dispatch, selector dispatch, or quality evaluation. Regenerate applicable packaged `daemon_server.py`, `api/market.py`, and `universe_server.py` mirrors with `build_plugin.py`; run focused Goal list/search/exact-read, MCP structured-envelope, visibility, bind/update/gate, canonical-dispatch, selector-dispatch, quality-leaderboard, and plugin-parity suites. This hotfix has no dependency on pagination, snapshot storage, Worker requester partitioning, Goal digest keys, host quota/scope choices, Postgres, or `per-user-goal-canonicals`.
- [ ] 0A.3 Independently review, land, deploy, run `python scripts/mcp_public_canary.py --url https://tinyassets.io/mcp --assert-handles`, and inspect fresh traces for clean list/search/exact-read use. In the same landing lane, sync only the self-contained `Public Goal read modes expose only public Goals` requirement and its three scenarios into canonical `openspec/specs/shared-goals-and-convergence/spec.md`; keep the remaining pagination delta in flight. Retire the live private-Goal STATUS Concern only after deployed proof closes all three modes, leaving rendered multi-window acceptance to the later catalog slice.

## 1A. Exact public page continuation (scope-decision independent)

- [ ] 1A.1 Add RED focused schema and handler tests proving canonical `read_page` advertises/forwards `offset`, `max_chars`, and `expected_sha256`, preserves shipped exact `page+changed_since`/`query` ambient-feed behavior, newly advertises and forwards exact-read `category`, does not advertise the host-gated `scope`, enforces the 4,000/32,000 connector default/cap through strict public-window validation while the in-process 128,000 default/256,000 clamp remain unchanged, preserves authority-first filtering, and returns zero results plus a structured parameter/mode error for every cross-mode combination, including exact `page`+enumeration `cursor` and search/changed-since enumeration with `offset`, `max_chars`, or `expected_sha256`.
- [ ] 1A.2 Add RED chunk-integrity tests with a non-prefixed draft fixture proving promoted and draft content/hash/offsets use raw canonical text, draft state is a sibling field, ranked-search draft title labeling remains unchanged, `next_offset` is stable, concatenation is exact, invalid bounds stay safe, and hash mismatch returns `stale_read`. Replace the now-vacuous `test_read_draft_does_not_duplicate_existing_draft_marker` fixture/expectation rather than leaving it green.
- [ ] 1A.3 Implement hash-bound exact-page chunks plus canonical exact-read `category` schema/forwarding without synthetic draft/truncation prose in the owning handler's in-process or canonical exact-read `content`; update the draft-marker assertions in `tests/test_wiki_tools.py`, truncation/prose assertions in both `tests/test_api_wiki.py` and `tests/test_wiki_tools.py`, add literal numeric assertions `== 128_000` and `== 256_000` in `tests/test_api_wiki.py` because imported symbolic constants are insufficient to pin the legacy contract, and update the affected fixture in `tests/test_wiki_canary.py`; regenerate the packaged `api/wiki.py` and `universe_server.py` mirrors with `build_plugin.py`, then run focused wiki, MCP schema, structured-envelope, visibility, and plugin-parity suites.
- [ ] 1A.4 Independently review and land slice 1A after its narrow claims release; it satisfies the legacy `wiki` exact-page continuation dependency and independently authorizes removal without waiting on the new-scope decision.

## 1B. Public relevance scope (host-gated)

- [ ] 1B.1 After task 0.3 approval, add RED schema/routing tests proving canonical `read_page` advertises `scope`, routes it to ranked search, changed-since enumeration, and exact-read ambient feeds, keeps `discovery` as search/enumeration default and source audience as exact-read default, and applies universe/page visibility before relevance filtering.
- [ ] 1B.2 Implement the minimal canonical-wrapper changes so anonymous root-commons `scope=coordination|all` works only as approved, universe visibility still wins, invalid scope returns no results, and no cursor/offset parameter is accepted outside its defined mode; regenerate the packaged `api/wiki.py` and `universe_server.py` mirrors in the same slice and prove parity before landing.

## 1C. Trusted anonymous requester transport key (snapshot-quota prerequisite)

- [ ] 1C.1 After task 0.5, and before task 2.3 or 3.3 enables anonymous snapshot quotas, add RED Cloudflare Worker tests that replace the shipped `preserves caller-supplied X-Forwarded-For` expectation with unconditional overwrite from authoritative `CF-Connecting-IP` and, when it is absent, stripping caller XFF while continuing the proxy so the origin returns in-band `anonymous_partition_unavailable` rather than `502`; claim and update `deploy/cloudflare-worker/worker.js` and `worker.test.js`, delete the set-if-absent preservation branch, run the Worker suite, deploy through its owning pipeline, require `python scripts/mcp_public_canary.py --url https://tinyassets.io/mcp --assert-handles` green immediately after deploy, and retain a partition canary proving caller-selected forwarding headers cannot mint requester partitions.
- [ ] 1C.2 After task 1C.1, claim the exact origin provenance/partition seam and focused tests; require authenticated tunnel/front-door provenance before trusting XFF, derive one keyed privacy-safe requester partition without persisting raw addresses, and return zero rows, `complete=false`, `anonymous_partition_unavailable`, plus bounded retry guidance for missing/unverified provenance as an in-band tool result rather than an OAuth challenge or gateway error. Prove direct-origin and caller-selected headers cannot mint partitions while distinct authoritative clients remain distinct.

## 2. Complete wiki changed-since enumeration

- [ ] 2.1 After `build-brain-canonical-store` resolves physical-bundle versus union-view topology, require its Brain commit protocol or compatibility adapter to assign server-controlled monotonically non-decreasing `inventory_committed_at`, stable globally unique `inventory_event_id`, and globally unique monotonically increasing server-internal causal `inventory_sequence` in the same serialization that makes a content revision or ordinary-discovery withdrawal/restoration transition durable/read-visible; at commit, freeze the event's visibility-partition membership and independently assign a stable monotonically increasing contiguous `partition_sequence` across only events committed visible to each applicable partition. Any later change to the set of partitions where a page is inventory-visible commits a fresh withdrawal/restoration event in every affected partition; pre-membership events never enter a newly eligible manifest. Source views capture a monotonically advancing visibility fence no earlier than incoming/visible clocks, and accepted-pending revisions/transitions remain unclocked/unidentified/unsequenced until visibility then receive the internal triple plus every applicable partition sequence with a clock no earlier than that fence. Atomically backfill one current event per current page path with one server-selected migration timestamp independent of content/mtime and no later than the first live commit plus distinct event IDs, deterministic contiguous path-ordered internal sequences, and deterministic contiguous path-ordered sequences independently per visibility partition; do not retroactively import multiple historical revisions from a successor store. Reconcile the committed source view/index/inclusive watermark plus durable authority-safe deletion and all page/moderation/declared-universe partition-transition representations with the Brain, moderation, and universe-visibility owners; design one ephemeral retained-snapshot adapter without creating a second canonical content store.
- [ ] 2.2 Add RED tests that first pin the shipped normalized ISO-Z `(updated DESC, path DESC)` order, author-controlled frontmatter-or-mtime clock, exclusive `updated_at > changed_since` boundary, literal in-process `max_results` clamp `== 100`, and shipped `changed_since and not query and not category` enumeration discriminator, then require server-controlled total `(inventory_committed_at DESC, tagged path_sort_key ASC, partition_sequence DESC)` retained windows with strict public 1–100 rejection, deterministic pathless ordering, query/scope/category/universe/grant-partition/actor-context binding, exact event-based capture totals, emitted/pathless-signal/withheld and all-withheld-window invariants, immutable identical-continuation replay with withheld ordinals advancing position, terminal/full-page completion, malformed/expired paging and poll tokens, terminal-only actor/partition-bound `next_changed_since`, visibility-partition-change rebaseline with server-provided earliest ISO boundary, same-partition actor-context rebound preserving the fence, abandoned-traversal non-advancement, monotonically advancing empty-view fences, inclusive boundary deduplication by globally unique `inventory_event_id`, redaction-stable manifest digests containing only event ID/partition sequence/clock/disposition, one-current-event-per-path initial per-partition backfill coverage, accepted-before-view/durable-after-view coverage, every page/moderation/declared-universe partition-membership withdrawal and restoration without content revision, no retroactive admission of pre-membership events, same-clock live/deletion causal ordering, ascending partition-sequence reducer/supersession-chain semantics across windows, ordinary/moderation-redacted/secrets-safe authority-filtered durable deletion tombstones, a post-watermark commit with backdated frontmatter `updated`, no-page changed-since plus scope/category enumeration routing and no-page changed-since plus query conflict failure after slice 1B, writes during traversal, hidden-mutation non-observability including contiguous sequence only for an epoch-bootstrap `scope=all`/no-category/no-capture-exclusion/no-emission-withholding partition traversal and explicitly permitted filtered or excluded-candidate gaps, and hidden-only first-window snapshot reuse.
  Acceptance for task 2.2 also proves every capture-excluded partition-member candidate has an emitted ordinary/pathless superseding transition in that partition, or remains a withheld-and-counted ordinal when no such transition exists.
- [ ] 2.3 After task 1C.2, implement server-assigned `inventory_committed_at`/`inventory_event_id`/server-internal `inventory_sequence` plus commit-time frozen visibility-partition membership and contiguous `partition_sequence` for content revisions and every ordinary-discovery partition withdrawal/restoration transition, with one-current-event-per-path atomic backfill using path-ordered internal and independent per-partition sequences; then implement authority-filtered/actor-bound retained snapshot materialization from one committed canonical commons source view, with keyed privacy-safe anonymous partitions, 15–30 minute expiry, 64 MiB per-snapshot, a combined wiki-and-Goal four/256 MiB and six-creations/minute limit per actor/requester partition, 2,048/8 GiB anonymous aggregate and 4,096/16 GiB service-wide limits with authenticated reserve, immutable total-ordered live/visibility-transition/deletion rows carrying safe event/supersession/hash/display evidence, redaction-stable identity/partition-sequence/clock/disposition manifest digests that never expose the global sequence, filtered public revision, visibility-fence watermark independent of hidden membership, capture/expiry evidence, and no per-window source rescan or global write invalidation.
- [ ] 2.4 Implement bounded versioned positional wiki cursors and the complete receipt in `tinyassets/api/wiki.py`, with a hard 1–100 public `max_results` range, fixed contiguous processed-ordinal windows, and stateless identical continuation replay; accept explicit ISO bootstrap boundaries or bounded authenticated `next_changed_since` tokens binding fence/enumerator/universe/visibility-partition/actor context, return `poll_rebaseline_required` plus the server's earliest supported ISO `rebaseline_changed_since` only when the visibility partition changes, return `poll_context_rebound` plus a same-fence `rebound_changed_since` token when only actor context changes, and emit a non-null ordinary poll token only on terminal completion; make `read_page.cursor` alone select changed-since enumeration using its bound boundary while redundant `changed_since` must equal the decoded normalized inclusive boundary, keep no-page `changed_since` as the routing discriminator even with slice-1B `scope` or enumeration `category`, make non-empty no-page `query` conflict with `changed_since` or enumeration cursor instead of silently discarding enumeration state, preserve exact-page `changed_since`/`query` plus the slice-1A `category` and slice-1B `scope` ambient filters, expose/forward enumeration `category` and `cursor`, and add no action or handle; regenerate the packaged `api/wiki.py` and `universe_server.py` mirrors in the same slice.
- [ ] 2.5 Add cursor/poll-token fuzz, path/SQL-injection, cross-principal/context/partition token rejection, identical first-window plus continuation retry replay, invalid-window caps, expiry, all-withheld positional windows, post-capture restriction/deletion/grant-revocation, page/moderation/declared-universe partition withdrawal/restoration without content revision, operational-redaction-before-body-deletion behavior, ordinary/pathless-moderation-redacted/secrets-safe deletion/withdrawal polling, same-clock/same-path and pathless total-order fixtures, ascending reducer/supersession-chain application across windows, post-capture redaction-stable digest fixtures, abandoned traversal, single- and multi-partition adversarial fan-out, quota cleanup, and concurrent reader/writer tests; prove every retained `inventory_event_id` is emitted or withheld exactly once in deterministic public `partition_sequence` order, retries reuse/replay one immutable positional snapshot window without extra quota, ordinary later writes do not abort them, newly restricted or authority-revoked rows are withheld with no identifying fields, restoration receives a fresh poll-visible event and wins over its withdrawal after causal reduction, pre-membership events never enter a newly eligible partition, old-visibility-partition poll tokens force rebaseline using the returned earliest ISO boundary, same-partition actor rotation receives a same-fence rebound token without rebaseline, deletion/withdrawal never deduplicates against its superseded live event, redacted removal signals expose no path/hash/descriptive fields, secrets-class tombstones retain or emit no recoverable hash, manifest digests exclude all path/hash/display/relevance payload and the global sequence before and after redaction, emitted rows including pathless signals plus withheld positions equal capture total, hidden activity cannot create sequence gaps in an unfiltered epoch-bootstrap `scope=all`/no-category/no-capture-exclusion/no-emission-withholding partition traversal while filtered or excluded-candidate gaps are allowed and reveal nothing, hidden activity cannot alter visible totals/validity/revision evidence or force a new snapshot charge, capacity rejection leaves no partial snapshot or unbounded retained state, and anonymous exhaustion cannot block authenticated reserved capacity or valid continuations.

## 3. Complete Goal catalog enumeration

- [ ] 3.1 Resolve the host-gated Goal storage substrate after reconciling `establish-postgres-control-plane` and `per-user-goal-canonicals`; design one transactional, actor-context-bound ephemeral snapshot adapter while the exhaustive catalog is the public-only constant `goal-public-commons`, and provision a dedicated versioned Goal snapshot-digest key through the accepted credential-vault/control-plane secret path without reusing identity or request-idempotency keys.
- [ ] 3.2 Add RED tests that pin the shipped no-tiebreak ordering, first-tag-only SQL-LIKE behavior, in-process absence of an upper `limit` clamp, and internal `production_only` overfetch `max(requested_limit * 10, 100)`, including its explicit `production_only=true` default-limit case (`limit=50` yields 500); separately pin that `production_only` defaults false, the canonical default fetches 30, and the legacy default fetches 50. Then require canonical-wrapper-only strict 1–100 rejection plus `(updated_at DESC, goal_id ASC)` retained windows capped at 100 processed ordinals, author/exact-all-tag/public-partition and actor-context binding, exact tag treatment for `%` and `_`, allowlist-only `visibility=public` rows/totals, post-capture public-to-non-public withholding, immutable identical-continuation replay with withheld ordinals advancing position, non-public-only first-window snapshot reuse, exact totals, terminal/full-page completion, ranked-search incompleteness, malformed/expired tokens and invalid limits, and ordinary writes during traversal; exclude internal `production_only` from this exact-count surface.
- [ ] 3.3 After task 1C.2 and on the accepted storage engine, add the covering `(updated_at DESC, goal_id ASC)` index and exact author/all-tag/public-visibility predicates, then implement transactional immutable filter/actor-context-specific snapshot materialization with dedicated-key public-manifest-digest deduplication whose cursor binds a key version retained for at least 30 minutes and until every bound snapshot/cursor expires, plus the same 15–30 minute, 64 MiB per-snapshot, combined wiki-and-Goal four/256 MiB and six-creations/minute per-actor limit, 2,048/8 GiB anonymous aggregate, and 4,096/16 GiB service limits with authenticated reserve, without cross-actor deduplication or changing exact `target=goal` behavior.
- [ ] 3.4 Expose/forward `cursor` through canonical `read_graph target=goals`, reject it on unsupported targets/search modes, enforce the hard 1–100 public `limit` range, preserve current filters plus the complete receipt, implement positional replay without a mutable continuation pointer, and replace the misleading “narrow filters” text with faithful `next_cursor` guidance; regenerate the packaged `api/market.py`, `daemon_server.py`, and `universe_server.py` mirrors in the same slice.
- [ ] 3.5 Add cursor fuzz, query-injection, wrong-partition/actor-context, invalid-window caps, identical first/continuation retry replay, expiry, digest-key rotation/overlap and retired-key rejection, post-capture public-to-any-non-public visibility transitions, non-public-only mutation deduplication, single- and multi-partition adversarial fan-out, quota cleanup, and concurrent reader/writer tests; prove retained snapshots have no duplicate/gap, identical retries reuse/replay one immutable positional snapshot window across an allowed key rotation, only `visibility=public` Goals ever enter or emit from the public catalog or influence its digest, unrecognized/non-public visibility cannot force another public snapshot charge, ordinary later Goal mutations do not abort traversal, emitted plus withheld equals capture total, capacity rejection leaves no partial snapshot or unbounded retained state, and anonymous exhaustion cannot block authenticated reserved capacity or valid continuations.

## 4. Connector, load, and live acceptance

- [ ] 4.1 Audit the already-regenerated packaged Claude-plugin runtime from every landed vertical slice and prove byte parity, exact seven advertised handles, discoverable handle-qualified parameter descriptions including disjoint `read_page.scope=discovery|coordination|all` versus any separately accepted `write_page.scope=commons`, read-open auth, stable read-only/idempotent hints, and unsupported-cursor errors.
- [ ] 4.2 Extend the public canary to traverse more than one wiki/Goal window and one large-page chunk while validating structured/text receipt fidelity, hard enumeration-window caps, identical continuation replay including an all-withheld window, cursor-only wiki continuation routing, normalized redundant-boundary equality, exact-page ambient filtering, no-page query/enumeration conflict, cross-mode fail-loud errors, handle-qualified disjoint scope enums, terminal-only actor/partition-bound wiki `next_changed_since`, visibility-partition-change `poll_rebaseline_required` plus usable earliest ISO boundary, same-partition actor-context `poll_context_rebound` with a usable same-fence token, page/moderation/declared-universe withdrawal/restoration without content revision, deterministic same-clock/pathless total order and ascending partition-sequence supersession reduction across windows, contiguous sequence only in an epoch-bootstrap `scope=all`/no-category/no-capture-exclusion/no-emission-withholding partition traversal plus allowed filtered/excluded-candidate gaps, abandoned-traversal non-advancement, event-based emitted/pathless-signal/withheld totals, distinct live/deletion event IDs and public partition sequences, redaction-stable payload-free digest behavior excluding the global sequence, secrets-safe and pathless-redacted removal fields, stale/mismatched continuation rejection, authoritative front-door requester partitioning, caller-header overwrite, and missing-authoritative-header in-band denial.
- [ ] 4.3 In this change's owner-local `tests/load/public_read_completeness/` lane, implement and register two scenarios through the future `implement-production-load-harness` protocol, one owned by `wiki-commons` and one by `shared-goals-and-convergence`. Each registry entry names owning capability, scenario ID/version, applicability, required/optional classification with justification, substrate requirements, adapter and oracle references, fault declaration, and threshold references. Each scenario independently uses 10,000 records, 1,000 concurrent clients, and 1,000 distinct authoritative actor/requester rate partitions for five minutes, exactly 100 full traversals plus 900 ordinary first-or-continuation reads, and 20 relevant writes/second; the wiki run includes non-destructive visibility withdrawal/restoration, same-clock/pathless events, causal reduction, a dedicated epoch-bootstrap `scope=all`/no-category/no-capture-exclusion/no-emission-withholding sequence-privacy fixture distinct from filtered/excluded-candidate gap-acceptance fixtures, visibility-partition rebaseline, and same-partition actor rebound while both runs include ordinary writes. Then prove one client cannot mint partitions by varying forwarded headers, distinct authoritative clients remain distinct, one partition exceeds the combined cross-surface per-context bounds, and many anonymous partitions reach the anonymous aggregate cap while an authenticated client starts from reserved capacity. Instrument false-complete count, duplicate/omitted event count, qualifying unfiltered partition-sequence gap/privacy violations, false filtered/excluded-candidate gap alarms, total-order/reducer violations, actor-rebound and partition-rebaseline violations, p99 cold snapshot-plus-first-window latency, p99 continuation latency, completed traversals, class/global capacity rejections, authenticated reserve success, partial-snapshot count, retained bytes/count, continuation success at capacity, and cleanup. Do not modify `harden-production-load-evidence`.
  The wiki load oracle also proves each capture-excluded partition-member event is explained by an emitted superseding transition in the same partition or retained as a withheld-and-counted ordinal; only unexplained gaps count as privacy violations.
- [ ] 4.4 After the shared harness exists and the host approves an isolated traffic envelope whose declared substrate class is exactly `real`, run both per-surface scenarios and require for each: zero false-complete receipts, cold snapshot plus first window below two seconds at p99, continuation below 500 milliseconds at p99, at least 99 of 100 traversals complete within five minutes, zero partial snapshots, hard per-context/anonymous-class/global quota enforcement, authenticated reserved-capacity success, existing-continuation success at capacity, and cleanup within the 30-minute maximum TTL; `shaped` and `mock` classes are barred, and the `not_run` verdict cannot satisfy a §14 done-claim; optimize or materialize the shared snapshot projection before acceptance if either surface misses the bound.
- [ ] 4.5 Provision and receipt the dedicated versioned Goal snapshot-digest key through the accepted secret path, land through the normal reviewed main pipeline, prove the deployed source SHA/release receipt, and rerun `python scripts/mcp_public_canary.py --url https://tinyassets.io/mcp --assert-handles` plus route-absence, auth, key-rotation continuation, and ordinary continuation canaries against the same live endpoint.
- [ ] 4.6 Run `ui-test` in separate real browser-rendered Claude.ai and ChatGPT conversations with the installed connector; record multi-window/chunk traversals reaching terminal `complete=true`/`truncated=false` without using the web app, leaving any untested host matrix cell explicitly open rather than claimed.
- [ ] 4.7 Inspect fresh production traces/support history for clean post-fix organic use; if none exists, leave a freshness-stamped STATUS monitoring row rather than claiming proven adoption.

## 5. Independent review and foldback

- [ ] 5.1 Obtain exact-current opposite-provider review of implementation, security boundaries, cursor concurrency semantics, tests, load evidence, and rendered acceptance; resolve every blocking finding and rerun affected evidence.
- [ ] 5.2 After `build-brain-canonical-store`, `moderation-and-abuse-response`, `universe-visibility`, the host-approved Goal substrate and dedicated versioned snapshot-digest key provisioning, `per-user-goal-canonicals`, `implement-production-load-harness`, the four concurrent `live-mcp-connector-surface` changes, task 0.5's reciprocal merged front-door body, and applicable retire-legacy ordering are reconciled, sync the remaining implemented deltas into `wiki-commons`, `shared-goals-and-convergence`, and `live-mcp-connector-surface`; verify the merge is idempotent over the already-synced slice-0A public-Goal requirement and retains the authoritative-XFF/missing-header clauses plus every then-canonical clause/scenario not intentionally modified by an accepted change, strictly validate the full tree, archive the change, and retire the STATUS row in the same landing lane.
