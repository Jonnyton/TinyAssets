## 1. Approve the acquisition boundary

- [ ] 1.1 Obtain independent cross-family shape approval, resolving account-deletion/lifecycle and concurrent-acquisition guards before runtime implementation.

## 2. Implement one recovery path

- [ ] 2.1 Add the exact bounded same-origin deposit_key operation using the installed free preset and existing complete_bootstrap pipeline; preserve owner, empty-setup and lifecycle fences.
- [ ] 2.2 Connect the prominent shortcut and sole secure input to explicit manual setup mode, clear-before-await, login fencing, honest progress and the existing approval UI without generic inference or credential replay.
- [ ] 2.3 Correct signup/dashboard/error guidance without changing the hosted OAuth journey or claiming automatic return.

## 3. Verify and hand off

- [ ] 3.1 Prove route authority, lifecycle races, exact preset/no-paid boundaries, approval reuse and secret-free failures with focused tests.
- [ ] 3.2 Prove actual controller/DOM submission, timeout/login fencing, one input and no URL/storage/log leakage; capture isolated phone/desktop browser proof.
- [ ] 3.3 Run focused combined onboarding/model/auth regressions, lint, brand and plugin mirror parity; commit and submit a draft PR with exact-head independent review.
- [ ] 3.4 Lead verifies deployment and ordinary live owner approval/model connection; sync the as-built delta and archive in the same delivery lane.
