## 1. Contract promotion

- [x] 1.1 Audit current-main branch reads, source reuse, lineage, mutation, deletion, related-wiki projection, live/version/canonical execution, goal/gate attachments, private-filtered projections, dry inspection, active visibility ownership, and existing tests against the connector-first product boundary.
- [x] 1.2 Define target requirements for authenticated-subject authorship, not-found read equivalence, source reuse, lineage, mutation/deletion authority, action-scope defense in depth, and cross-surface wiki visibility.
- [x] 1.3 Obtain Opus 5 review of the drafted proposal, design, both delta specs, tasks, and sibling-lane boundary; resolve every Critical and Important finding. — 2026-07-25: initial `ADAPT` findings applied; narrow Opus 5 re-review returned `APPROVE` with no Critical or Important findings.
- [x] 1.4 Run strict OpenSpec validation and land this target change active and unsynced. — 2026-07-25: 47/47 strict validation passed; PR #1778 squash-merged as `26d267eb`.

## 2. Dependency and claim gates

- [ ] 2.1 After the in-flight `test-identity-and-reset` lane lands, consume the as-built `tinyassets.api.permissions.current_request_actor_id()` seam, assert that `anonymous` carries no branch authority, and mutation-probe the absence and environment-only cases; PR #1691 is not a dependency.
- [x] 2.2 Record current-main evidence that blank-universe page-grant evaluation is fail-closed without changing `visibility.py`: `list_universe_acl` returns `[]` before storage access for `universe_id=""`, and a 2026-07-25 temporary-database raw-DML forge probe showed an injected empty-ID ACL row still cannot make a private root page visible.
- [ ] 2.3 Coordinate `retire-legacy-live-mcp-tools` tasks 4.2/4.4 so every branch mutation and deletion stays non-read and missing metadata denies during registry migration. Current-main evidence already proves edits are `write`, approval/deletion are `admin`, and an absent row returns no metadata and is rejected; the remaining gate is lockstep preservation through that migration.
- [ ] 2.4 Wait for broad `tests/` claims to release, then claim `tinyassets/api/branches.py`, the narrow `tinyassets/daemon_server.py::search_nodes` seam, `tests/test_branch_read_authority.py`, `tests/test_related_wiki_visibility.py`, and `tests/test_branch_mutation_authority.py` before implementation.
- [ ] 2.5 Inventory the 189 `UNIVERSE_SERVER_USER` test references and 54 branch-creating test files, add one credential-subject fixture compatible with `test-identity-and-reset`, and migrate every core branch-authority test away from environment identity without breaking the Tier-3 no-auth/dev test path.
- [ ] 2.6 Run the Tier-3 fresh-clone/local setup with a real locally issued credential/session subject and create a branch through the canonical connector; fail the gate if local bootstrap still depends on anonymous or `UNIVERSE_SERVER_USER` authorship.

## 3. Wave 1 — reads, projections, and lineage

- [x] 3.1 In `tests/test_branch_read_authority.py`, add RED tests for server-bound create/build authorship, authoring-receipt actor, and global branch-action ledger actor, plus `anonymous` denial and an environment-only actor; each test must fail against the current `_current_actor()`/caller-author behavior and prove no environment actor reaches the public ledger. — 2026-07-25: RED proved env/caller attribution; GREEN binds create/build/node/receipt/ledger actors to the credential subject and denies anonymous before persistence.
- [x] 3.2 In the same file, add RED listing tests proving an environment-only actor exposes no private row/count, unauthenticated `scope=mine` is `[]`/`0`, and an authenticated author still sees their own private branch. Add reusable-node search tests proving public-plus-own-private candidates/counts and zero foreign/environment-private influence. Add RED table tests for get/describe/validate/fork-tree private-or-missing byte equivalence, plus a private-name case proving environment identity cannot resolve or replace the original selector with the canonical ID; keep explicit public and owner controls. — 2026-07-25: RED exposed env-private listing, global-search leakage, and unguarded selector reads; GREEN covers anonymous/owner/public controls and byte-identical original-selector errors.
- [x] 3.3 In `tinyassets/api/branches.py`, add `_request_branch_actor() -> str | None`, `_resolve_readable_branch(selector: str, base_path: str) -> tuple[str, dict[str, Any]] | None`, `_resolve_readable_version(version_id: str, base_path: str) -> tuple[str, dict[str, Any]] | None`, and canonical branch/version not-found helpers. The version helper resolves parent `branch_def_id`, fails closed on a missing parent, applies the same read boundary, and is the only version-authority primitive successors may consume. — 2026-07-25: helpers consume `current_request_actor_id()`, treat anonymous as absent, preserve original selectors, and fail closed through the persisted version parent.
- [x] 3.4 Server-bind branch create/build authorship, authoring-receipt actor, and `_append_global_ledger(actor=...)` through `_request_branch_actor`; use it as the only `list_branches` viewer, add/pass `viewer` through `daemon_server.search_nodes` before aggregation, wire `_resolve_readable_branch` before get/describe/validate/fork-tree output, and omit unreadable `fork_from` pointers from get/describe. — 2026-07-25: focused credential-backed tests pass; forced env fallback, open selector, and unfiltered search mutations each turned their guards RED.
- [x] 3.5 In `tests/test_related_wiki_visibility.py`, add RED tests for blank-root-context filter-before-match/sort/cap/count, zero hidden fields/counts, all-hidden `[]`/`0`, public-page controls, and related-path subset equality against the root wiki list. — 2026-07-25: RED showed hidden cap/count/path/body influence; public stable-field control stayed green after correcting its pre-existing `pages/` prefix.
- [x] 3.6 Pass `universe_id=""` to `page_visible_in_listing` inside `_related_wiki_pages` before matching, without deriving per-page grant context, adding audience filtering, or changing stable keys. — 2026-07-25: filter now runs immediately after frontmatter parsing; forced-open predicate mutation produced 3 focused failures.
- [x] 3.7 Add RED lineage cases for an unreadable ancestor, unreadable stored `fork_from` pointer, owner-private descendant, and foreign-private descendant; authorize each ancestor through `_resolve_readable_version`, filter pointers through the same helper, and pass `_request_branch_actor()` as the descendant viewer. — 2026-07-25: GREEN omits unreadable pointers/ancestors and includes public plus owner-private descendants only; forced-open version and descendant mutations each failed.
- [x] 3.8 Run `python -m pytest tests/test_branch_read_authority.py tests/test_related_wiki_visibility.py tests/test_universe_visibility.py -q`; require all tests to pass, then independently mutation-probe selector resolution, page filtering, and ancestor/descendant gates before the Wave 1 commit. — 2026-07-25: 75 passed in 13.35s; request actor, selector, search viewer, page filter, version-parent, and descendant-viewer probes all produced expected RED failures, then 75/75 re-passed after restoration.

## 4. Wave 2 — private source reuse

- [x] 4.1 Add RED `node_ref.source` tests that snapshot destination bytes and prove a missing source and foreign-private source return byte-identical original-selector envelopes; source code, prompt, tools, description, and approval provenance are neither returned nor persisted. Include public and owner-private controls proving authorized copied provenance is preserved. — 2026-07-25: RED copied a foreign private source into the destination; GREEN proves byte-identical denial/no byte change and preserves public/owner-private source authorship plus approval provenance.
- [x] 4.2 Gate node-reference lookup through `_resolve_readable_branch` before `_lookup_node_body` reads any source field; run the Wave 2 node-reference cases to green and commit independently. — 2026-07-25: branch selector is authorized before `_lookup_node_body`; a forced skip copied the source and failed the atomicity test.
- [x] 4.3 Add RED clone and `set_fork_from` tests proving missing and foreign-private version selectors return byte-identical original-selector envelopes and leave no `node_defs`, metadata, version, lineage edge, or partial destination; include public and owner-private controls. — 2026-07-25: RED materialized foreign snapshots/lineage; GREEN returns canonical original-version envelopes with unchanged branch rows/version lists and keeps both authorized controls.
- [x] 4.4 Gate fork snapshot loading and `set_fork_from` through `_resolve_readable_version` before clone materialization or lineage persistence; run `python -m pytest tests/test_branch_read_authority.py -q`, mutation-probe all three reuse/lineage gates, and commit independently. — 2026-07-25: 21 passed in 3.45s; forced-open node-ref, clone, and set-fork gates each produced the expected RED failure before restoration.

## 5. Wave 3 — mutation and deletion authority

- [ ] 5.1 In `tests/test_branch_mutation_authority.py`, add RED non-author and owner controls for add-node, connect, entry-point, and state-field mutations, asserting byte-identical branch state after denial; foreign-private ID/name cases must match missing selectors, readable-public non-owner cases must expose no stored author, and a caller-supplied new-node author must be replaced by the authenticated subject.
- [ ] 5.2 Add `_branch_authorized(branch: dict[str, Any]) -> bool` using `_request_branch_actor`; resolve targets through `_resolve_readable_branch`, gate the four structural mutations before target expansion or persistence, server-bind new-node/git/receipt/ledger attribution, and emit one generic no-author-details denial for readable non-owned branches. Run only those cases to green and commit.
- [ ] 5.3 Add RED non-author and owner controls for update-node, patch-nodes (including empty selection), and approve-source-code, asserting no source/provenance or batch expansion changes after denial, private-or-missing response equivalence, and authenticated-subject approval/new-node attribution.
- [ ] 5.4 Resolve targets through `_resolve_readable_branch`, then gate update-node, patch-nodes, and approve-source-code through `_branch_authorized` before selection expansion or persistence; server-bind new provenance through `_request_branch_actor`, run those cases to green, and commit.
- [ ] 5.5 Add RED patch-branch tests proving private-or-missing equivalence, generic readable-public non-author denial, identical denial under `force=true`, no stored-author/retry-with-force guidance, and preserved authorized conflict recovery.
- [ ] 5.6 Resolve patch-branch through `_resolve_readable_branch`, move `_branch_authorized` ahead of force/conflict handling, and server-bind version publisher/git/receipt attribution; run the patch cases to green and commit without changing authorized force semantics.
- [ ] 5.7 Add RED delete tests proving private-or-missing equivalence and that a readable-public non-author cannot remove the branch, versions, or backing files while the author still can.
- [ ] 5.8 Resolve the target through `_resolve_readable_branch`, then gate delete through `_branch_authorized` before any deletion call and run the delete cases to green.
- [ ] 5.9 As an as-built defense-in-depth regression guard (not a new requirement owned here), assert that structural/node/patch actions remain `write`, approval/deletion remain `admin`, and missing metadata is denied; coordinate those existing effects with retirement tasks 4.2/4.4.
- [ ] 5.10 Run `python -m pytest tests/test_branch_mutation_authority.py tests/test_branch_read_authority.py -q`, surrounding branch/storage suites, and mutation probes for every authority call site before the Wave 3 commit.

## 6. Core concurrency, load, public acceptance, and completion

- [ ] 6.1 Run a deterministic concurrent cross-actor proof with two credential-distinct request contexts interleaving listing/search, selector reads, lineage, source reuse, mutation, and deletion against one private branch and restricted page set; prove zero cross-actor disclosure or mutation. This is a focused race/security proof, not the §14 load evidence in 6.2–6.3.
- [ ] 6.2 After `harden-production-load-evidence` provides the shared protocol, register graph-execution-owned scenario ID `branch-authority-isolation` with `scenario_version=1`, applicable/required status, release-blocking justification, real connector/storage substrate requirements, adapter reference, invariant-oracle references, no-fault declaration, and threshold references. Exercise at least 1,000 concurrent canonical `/mcp` sessions, 100 credential-distinct subjects, and 10,000 admitted mixed core branch operations; require authorization-decision p99 under 3 seconds, zero unexplained 5xx, exact-once reconciliation within 60 seconds, and zero unauthorized disclosure/reuse/mutation/deletion.
- [ ] 6.3 Run that scenario through the shared production-load-evidence manifest/oracle protocol; retain recomputable raw evidence and reject shaped/mock execution as `not_run` rather than passing.
- [ ] 6.4 Run focused tests, surrounding suites, Ruff, strict OpenSpec validation, and independent correctness/security/concurrency/load-evidence/diff review; resolve every Critical and Important finding.
- [ ] 6.5 After deploy, pass canonical MCP handle canaries and a rendered two-actor chatbot conversation: actor A creates a private branch and restricted page; actor B receives not-found and cannot list/search, reuse, attach lineage, mutate, delete, or derive related-page metadata; actor A retains every authorized path.
- [ ] 6.6 Record dated post-fix clean-use evidence from real connector activity, or leave a monitoring row explicitly stating that organic evidence is not yet available.
- [ ] 6.7 Sync only this change's graph-execution-substrate and wiki-commons requirements and archive after its core runtime, deterministic concurrency, required real load evidence, and public acceptance pass. Successor changes are not completion dependencies and own their own sync/archive gates.

## Successor ownership references (non-blocking)

These findings remain required platform work but are not requirements or tasks of this change. Each named successor MUST file its own delta under the listed owning capability before implementation and MUST NOT restate these requirements under graph-execution-substrate:

- `harden-background-branch-execution-authority`: `daemon-runtime-and-dispatch` for schedules/subscriptions and `universe-lifecycle-and-soul` for universe-loop binding, receipt migration, inventory, re-authorization UX, and zero-host execution.
- `harden-run-branch-access-authority`: `graph-execution-substrate` MODIFIED deltas for run start plus persistent run read/write/output authority, explicitly replacing conflicting as-built visibility scenarios.
- `harden-branch-evaluation-access-authority`: `evaluation-outcomes-and-attribution` (and any graph version requirement it explicitly modifies) for version publication/read and node evaluation/rollback.
- `harden-branch-adjacent-access-authority`: `shared-goals-and-convergence` for Goal/canonical/protocol/gate/projection behavior, `evaluation-outcomes-and-attribution` for remix/provenance/conformance behavior, and `paid-market-economy` for bonus lifecycle privacy without changing settlement.
- `harden-outcome-and-gate-event-authority`: `evaluation-outcomes-and-attribution` for run/outcome read authority and server-bound independent gate-event attestation actors; any monetary event lifecycle remains with `paid-market-economy`.
