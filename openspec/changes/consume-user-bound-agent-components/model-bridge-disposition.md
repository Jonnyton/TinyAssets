# Model DATA bridge disposition — 2026-09-19

Fable 5.1 session3331, exit0 after427s, reviewed exact2bd644d3: **ADAPT**.
Lead and builder read the complete artifact in model-bridge-review.md. No tests,
edits or children were performed by the reviewer. Root accepts these corrections
before bounded implementation; this is shape disposition, not exact-code approval.

1. Reuse the existing same-journal coordinator capacity frontier. Work inference
   may stage only the exact finite next DATA candidate through a pure identity
   prelude, then `_authorize_attempt` supplies ALL fresh run authority. Replace
   current carrier/selection only after that succeeds. Never relax strict
   `_check_agent_authority`, allow FAILED reservations there, or add a relaxed
   shared check mode. Observer and tool checks remain strict. No automatic
   advancement after local arm refusal, authentication or unknown effects.
2. Capture validated preference documents before session construction; discovery
   is outside author/runs writers. No old admission rereads mutable defaults.
3. Finite arithmetic: C is the minimum existing accepted member invocation
   ceiling, NOT catalogue size. For each graph policy group g, w_g is the sum
   of its existing compiler attempt multipliers (1 for no policy; otherwise
   1+len(_POLICY_PROVIDER_RETRY_BACKOFF_SECONDS)) over its prompt nodes. Each
   explicitly chosen primary/fallback list fixes its full eligible order L_g;
   incompatible explicit entries refuse rather than disappear. Explicit empty
   fallback means primary only. Only genuinely Automatic discovered groups may
   shorten: start each at one candidate, then extend prefixes deterministically
   by canonical policy-group order while sum(w_g * len(L_g)) <= C. Groups sharing
   the same effective policy share a prefix, avoiding caller/node ambiguity.
   If even mandatory minima exceed C, refuse before arming. No discovered model
   increases C, token or money allowances. Work-agent receipt stays at existing
   C; non-agent receipt uses the computed finite minimum. The SAME fitted orders
   are used by actual dispatch. Compiler retries do not replenish this budget.
4. Session exhaustion persists across compiler re-entry and parallel nodes;
   exclusions are evaluated directly against the frozen finite order rather
   than feeding a stale exhausted source into a refreshed catalogue that raises
   ValueError. Removed sources never resurrect or restart the list. Fresh arm
   remains authoritative and can hold on revocation/catalogue changes.
5. Preserve configured-vs-reported model distinction. A producing carrier's
   selected model can support configured metadata, NEVER a claimed reported
   answer identity. Codex lacking reported_model remains unknown. The strict
   ExecutionReceipt normalizer stays unchanged. Graph attribution must be
   node-local and demonstrably associated with the final reply field; no global
   last-provider slot or first graph response can supply it.

Required regression list is the review's six cases plus graph-node parallel
observer isolation and honest unknown output combinations. Public activation
still needs completed adapter/model/controls, exact integrated code review and
ordinary rendered acceptance. Prompt-only dark staging is not full completion.
