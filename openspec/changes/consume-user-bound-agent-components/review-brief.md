# Bounded Fable shape review — receiver-owned agent consumer

Status: prepared, NOT dispatched. Release lead assigns the sole external peer
slot after cloud integration and the file-delivery shape review. This is a
platform proposal, never permission to implement or execute a user's harness.

Read `proposal.md`, `design.md`, `primitive-evidence.md`, both delta specs and
`tasks.md` and `turn-seam-evidence.md` in this change. Inspect only the cited current runtime seams needed
to answer the questions. Full PLAN was read by the author; review its Scoping
Rules 1/2, Daemon Platform public-definition/private-binding/no-ceiling clauses,
Providers current-user authority and no-platform-actor constraints as needed.

Concrete question: is one receiver-selected immutable Branch handling the
canonical foreground turn the smallest useful governed consumer beyond the
already deployed four-surface layout, without a parallel runtime/catalog/store
or source-author privilege? It is a candidate, not an approved runtime shape.

Answer separately with AGREE / DISAGREE_EVIDENCE (code citation) /
DISAGREE_CONCERN:

1. Existing primitive reuse and scope: identify any already-working consumer
   the proposal would duplicate. Does this adapter permit user-authored cognitive
   strategy, or merely rename the fixed writer? Do not propose implementing the
   user's PR #3840/#3842 or arbitrary scene/design content. Explicitly evaluate
   unsupported arbitrary executable UI/foreign harness/full setup cases.
2. Basic one-user/second-owner safety: receiver-owned/latest-updater identity,
   exact public pin, explicit install independent of layout application,
   current model/provider/effect/admission authority, no credentials/grants from
   creator, no ambient interpreter execution, and trusted disable/rollback.
   Determine whether existing binding update semantics suffice for install
   consent or require the existing owner approval path; do not invent a grant.
3. Canonical turn feasibility: same history/actor, current model display and
   fallback policy, immutable executable closure, cancellation, transport loss,
   exactly one terminal reply and no effect replay. Name any irreducible missing
   primitive/storage boundary before implementation, rather than blessing an
   internal helper merely because it has a callable signature.

If the shape is too broad, recommend the smallest functional subset which
preserves the eventual extension boundary and state what it would NOT complete.
If a genuine PLAN conflict appears, cite it; the author must file a concern,
not change PLAN. Distinguish blockers to this first adapter from later hardening.

Concrete choices from fresh seam inspection: (a) self-contained immutable graph
first, rejecting nested/unresolved executable dependencies, versus larger pinned
closure; (b) minimal durable canonical request/turn/run reservation and terminal
commit home (conversation rows are written afterwards; provider journals are
not canonical admission; run_lineage is comparison history, NOT ancestry);
(c) trusted preference DATA bridge into current run admission because foreground
authority explicitly rejects substituted served agent_model_plan. Give a narrow
choice and exact missing fields/transaction obligations, not broad framework.
Read the dated agent-spec drift concern without treating old Codex/WebFetch-only
as a mandate to regress current tools. This review remains proposal-only.

Constraints for eventual dispatch: read-only; no subagents or peer recursion;
no new worktree; no whole-repository audit/full test suite; no network/browser,
accounts, live incidents, user workflows or production mutations. Do not alter
any file. Budget will be bounded by the dispatch and reserve its final minute
for substantive verdict. No tests are needed to pretend this unbuilt shape is
implemented; source inspection is sufficient for design review.

Return concise release blockers and optional follow-ups, then end with
`VERDICT: APPROVE`, `VERDICT: ADAPT` or `VERDICT: REJECT`. An APPROVE clears only
this proposal's shape; implementation exact-head tests/review/live proof remain.
