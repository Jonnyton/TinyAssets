# Design

Add remix_shape to the canonical explicit SERVED_ENGINE_MCP_TOOLS tuple imported by both provider families. Retain all handler identity, universe, admission, capability and visibility checks. Do not replace the allowlist with a wildcard or modify the wake prompt. Verify registered engine catalog equality by AST and provider inclusion using existing config tests. Retain raw-secret deposits in the founder request flow. Broader connector access requires universe-bound adapters; do not alias unbound connector tools.
