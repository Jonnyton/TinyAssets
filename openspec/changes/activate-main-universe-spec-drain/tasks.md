## 0. Review and dependency admission

- [x] 0.1 Obtain independent review of the proposal, design, and `user-owned-cloud-automation` delta; on a documented hard opposite-provider limit use the host-approved fresh-context independent-review fallback; fold every blocking finding and record an exact-head `APPROVE` verdict before runtime implementation. The original 2026-07-29 review passed. Ringer follow-up on 2026-07-30: authenticated Claude Code 2.1.220 reported the hard monthly spend limit; fresh-context Codex review returned `ADAPT` on aggregate authority, unsafe tenant-code execution, and unavailable GitHub reconciliation. All findings were folded, strict validation passed, and independent exact-head review approved `53d44236`.
- [x] 0.2 Revalidate current-main owners for requester-owned cloud provider authority, background user/universe/Branch authority, persisted continuation, Branch version access, GitHub effects/receipts, and canonical connector actions; record exact landed prerequisites and stop on any unresolved collision or contradictory spec. Completed 2026-07-29 in `docs/audits/2026-07-29-cloud-drain-current-main-prerequisites.md`: reusable seams and exact gaps were initially audited at `eff97a1e` and reconciled after rebase at `71cf1a82`; runtime activation stopped on the explicit epoch-1/file-lock versus epoch-2/transactional PLAN decision. Host approval on 2026-07-29 selected epoch 2 as the sole-authority target and cleared the design-choice gate only. Epoch 1 remains the live bridge and epoch 2 remains dark until task 4.1 implements and proves fail-closed cutover; the collision-free exact-destination production capability repair and dark authority work remain admissible.

## 1. Generic definition and BYOC single-flight cloud slice

- [x] 1.1 Test-first, define one immutable generic repository-to-accepted-spec work definition and a read-only operational projection over existing authority records; reject user-authored runtime authority and any tenant-code evaluator while confinement is unavailable. Bind Jonathan's values only as an acceptance fixture. Completed 2026-07-30 at `fed9159d`: closed server-owned deterministic evaluator policy, full canonical `AcceptanceScenario` freeze, exact cross-record identity/source/executor/inherited-envelope projection, 167 focused tests, Ruff/strict OpenSpec/diff clean, independent exact-head `APPROVE`.
- [ ] 1.2 Test-first, bind that inactive private-universe Branch version to requester-owned cloud-consumable provider authority and an exact destination grant, then persist continuation through the existing activation, epoch-2 task, background-attempt, and provider/effect owners; prove missing/revoked prerequisites fail closed and concurrent triggers or restart never claim a second lane. Progress 2026-07-31: the first dark slice added the non-bearer provider binding and exact repository-grant preflight; the second persisted one prepared continuation against stopped activation and exact background/provider/destination snapshots (#2052). `docs/audits/2026-08-01-provider-work-ledger-core.md` added dark single-winner `universe_work` receipt/claim/reservation persistence with conserved invocation/token/cost ceilings. Progress 2026-08-01: `docs/audits/2026-08-01-cloud-drain-provider-resolution.md` adds resolver-gated production binding issuance and a canonical-store resolver that narrows one live cloud activation plus claimed background attempt into one bounded restart-safe provider-work receipt. `docs/audits/2026-08-01-cloud-drain-epoch2-assignment.md` repairs the canonical request/admission/task ID contract and adds a fail-closed resolver from one activation-bound epoch-2 task plus server-selected worker to one restart-safe background attempt. `docs/audits/2026-08-01-cloud-drain-claim-custody.md` transactionally binds the claimed epoch-2 task, exact worker, claim instant, and lease to the reserved background attempt. `docs/audits/2026-08-01-cloud-drain-runtime-compositor.md` restart-safely converges prepared authority through the active epoch, deterministic epoch-2 admission, and reserved attempt, with eight-way single-winner proof and the consumer still dark. PR #2082 landed as `5ba557d0` with exact-head opposite-provider `APPROVE`; exact-head desktop-release rerun 2 completed the Windows lifecycle job successfully in 43 seconds on 2026-08-01 after one runner attempt became non-terminal and was cancelled without uploaded logs. `docs/audits/2026-08-01-cloud-drain-provider-launch-arm.md` adds the dark exact-current durable pre-launch arm, and `docs/audits/2026-08-01-cloud-drain-provider-carrier.md` threads that armed tuple through the existing provider carrier/sinks without a live caller. Production carrier minting, effect reconciliation, and cutover remain absent, so this task stays unchecked.

## 2. Reviewable delivery and evidence

- [ ] 2.1 Test-first, freeze one existing `AcceptanceScenario` policy and input digests, execute one bounded isolated task workspace and one PR at most, and preserve a content-addressed commit/diff before cleanup; require independent review and CI with only the documented hard-limit fallback. Permit at most one same-workspace evaluation retry with fresh target/provider generations and budgets. Route `(universe_id, automation_id, claim_id, repository, intended_head_sha, effect_kind)` exclusively through outbound-boundary reconciliation; block activation while exact remote matching remains unsupported. Independently verify merge, then perform OpenSpec sync/archive and STATUS retirement. The dark outbound-owned exact-matching prerequisite landed 2026-07-30 in #1951/#1953/#1954; this task remains unchecked until the automation consumes it through a server-authored identity and current scoped proxy.
- [ ] 2.2 Test-first, emit bounded terminal receipts and cloud-authoritative health containing immutable version, claim, useful-progress timestamp, authority source, budgets, retry/continuation policy, blocker, evidence handles, and next action; alarm when liveness continues without useful progress.

## 3. Phone-only ownership and evolution

- [ ] 3.1 Test-first, expose inspect, pause, resume, and stop through existing canonical handles with principal derived from authenticated request context and ownership resolved server-side; prove caller-supplied `owner_actor` grants no authority, a phone-chatbot user needs no desktop/filesystem/CLI, and pause fences future slices without misreporting an already committed effect as cancelled. Reprioritization is deferred.
- [ ] 3.2 Test-first, expose complete definition read/diff/edit, external-effect-free dry test, immutable version publication, explicit activation bind, and rollback-by-rebind through existing canonical handles, with non-owner access denied non-oracularly.

## 4. Single-active cutover

- [x] 4.0 Test-first, land the dark server-authoritative automation activation record/store with exact-record CAS over epoch, executor class, immutable version, lease, and state; prove stop/rebind fencing, competing-version single-winner behavior, and stop-before-executor-switch semantics without wiring either queue epoch live. Completed 2026-07-30 with 11 focused activation tests, 192 related focused tests, Ruff, mirror parity, and strict OpenSpec validation; neither queue epoch is wired to the store.
- [x] 4.0a Test-first, bind the optional exact activation tuple into epoch-2 admission and revalidate it atomically at claim against the trusted worker executor; preserve unbound legacy tasks and keep the consumer dark. Completed 2026-07-30 with a cross-record admission discriminator plus stale epoch/version/lease, stopped activation, alternate identity, full-tuple erasure, tray/cloud mismatch, populated-schema migration, tamper, and mirror-parity coverage; `EPOCH2_QUEUE_CONSUMER_READY` remains false.
- [ ] 4.1 Test-first, implement one server-authoritative `(universe_id, automation_id)` activation record with CAS transitions over epoch, executor class, immutable version, lease, and state; prove every claim rejects competing cloud versions, alternate activation identities, stale/partitioned tray epochs, cloud activation while the tray can claim, and tray restoration until cloud is durably stopped.

## 5. Acceptance and foldback

- [ ] 5.1 Run focused authority, security, collision, restart, receipt-reconciliation, and §14 concurrency/load tests plus the public canary; preserve dated commands, environment, revision, and artifacts.
  - [x] 5.1a Accept the generic runtime's shaped local prerequisite at reviewed head `dde16358`: cross-process identity/restart and provider single-flight races pass with explicit local-SQLite/provider-double classification. See `docs/audits/2026-08-02-cloud-agent-production-shaped-load.md`. This does not satisfy the live cloud, public-canary, or production-environment portions of 5.1.
- [ ] 5.2 Through a rendered chatbot conversation, supply a repository plus spec or patch request, create/import/remix and activate the ordinary Branch composition, then—with the tray disabled and Jonathan's computer off—run at least 24 hours of useful cloud progress including a worker restart; capture phone-chatbot inspect/control/evolve/rollback proof and check for post-fix organic use evidence.
- [ ] 5.3 After all acceptance evidence and independent review are green, sync the delta idempotently into the main spec, archive the change, retire the STATUS activation row, and leave any unmet organic-use proof as a dated monitoring item.
