> **Historical reconciliation (2026-07-22).** All boxes below record disposition, not permission to
> sync the obsolete delta. Current behavioral truth is canonical
> `live-mcp-connector-surface`; archive this change with `--skip-specs`.

## 1. Recovered Foundation

- [x] 1.1 Record `cfd724bd` / PR #617 as the recovered directory-router source.
- [x] 1.2 Record `tinyassets/directory_server.py` as the current descendant and intentional directory surface, not a retirement target.
- [x] 1.3 Preserve the recovered fold-map and `tests/test_directory_server.py` provenance.

## 2. Landed Live-Surface Evolution

- [x] 2.1 Record PR #1335 / `7fa67fd6` as the five base-handle forward port, PR #1340 / `33c32624` as the dotted-to-underscore connector fix, and PR #1437 / `b91a6b07` as the later `converse` addition.
- [x] 2.2 Correct the router description to the current primarily `target=` dispatch boundary over existing handlers.
- [x] 2.3 Preserve `run_graph` as the only run-producing advertised handle and page routing through the existing wiki handlers.
- [x] 2.4 Record hidden legacy registration/logging as landed historical behavior; transfer actual removal to `retire-legacy-live-mcp-tools`.
- [x] 2.5 Preserve historical plugin mirror-parity evidence without reopening runtime implementation.

## 3. Verification Truth

- [x] 3.1 Correct the advertised-set record to exactly seven live underscore handles: `read_graph`, `write_graph`, `run_graph`, `read_page`, `write_page`, `converse`, and `get_status`.
- [x] 3.2 Record that existing surface tests cover advertisement, status, unknown-target, and goal write/read, but not a genuine `run_graph` invocation; transfer that proof gap to `retire-legacy-live-mcp-tools`.
- [x] 3.3 Preserve the hidden-legacy dispatch/deprecation-log proof.
- [x] 3.4 Record that PR #1337 / `624b627a` fixed the cited schema-label failure; do not preserve it as a current baseline failure.

## 4. Canary and Deployment

- [x] 4.1 Record the as-built canary as six required core handles plus optional `get_status`; transfer exact-seven enforcement to `retire-legacy-live-mcp-tools`.
- [x] 4.2 Preserve the post-deploy canary wiring as landed.
- [x] 4.3 Fresh 2026-07-22 production probes passed: live `/mcp` listed seven handles and the versioned directory catalog listed five.

## 5. Administrative Closure

- [x] 5.1 Record that PR #1335 merged; no canonical position-record artifacts were found, so do not manufacture triple-key evidence.
- [x] 5.2 Treat the historical triple-key task as administratively obsolete rather than reopening landed work.
- [x] 5.3 Preserve current deployment evidence via the fresh public canary, not the obsolete five-handle claim.

## 6. Residual Promotion

- [x] 6.1 Record registry metadata repair as closed by PR #1595; move MCPB/host-manifest and substantive Polsia handoff reconciliation to `reconcile-external-connector-manifests`.
- [x] 6.2 Move hidden live-tool removal, exact-seven canary proof, and real `run_graph` invocation coverage to `retire-legacy-live-mcp-tools`; explicitly retain `tinyassets/directory_server.py`.
- [x] 6.3 Validate the historical change, create both successor changes, and archive this change with `--skip-specs` so no `mcp-five-handle-surface` canonical capability is created.
