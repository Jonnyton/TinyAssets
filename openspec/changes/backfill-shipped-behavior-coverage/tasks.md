## 1. Ground the wave

- [x] 1.1 Confirm the four gaps against the PR #1616 full-coverage audit and current canonical headings.
- [x] 1.2 Inspect the owning source and focused tests; record contradictions and current limitations.
- [x] 1.3 Decompose the remaining thirteen groups into exact staged successors; promote only the two proposal-safe literal STATUS rows.

## 2. Draft additive deltas

- [x] 2.1 Add cumulative multi-shot ASP validation to `constraint-evaluation`.
- [x] 2.2 Add the installed `tinyassets` GUI command to `desktop-host-runtime`.
- [x] 2.3 Add guard/mirror checks and machine-readable diagnostics to `development-coordination-runtime`.
- [x] 2.4 Add Branch-slug and episodic-coordinate registries to `domain-plugin-runtime`.
- [x] 2.5 Write the durable source/test/limitation map without changing runtime or canonical specs.

## 3. Verify the proposal

- [x] 3.1 Run `openspec validate backfill-shipped-behavior-coverage --strict`.
- [x] 3.2 Run repository-wide `openspec validate --all --strict`.
- [x] 3.3 Run focused non-Windows-forbidden tests and direct source assertions for all four contracts; report the known stale GUI-name test separately.
- [x] 3.4 Run `git diff --check` and the STATUS line-budget check.

## 4. Independent review and publication

- [x] 4.1 Obtain independent source-to-requirement review for constraint/desktop and coordination/domain deltas.
- [x] 4.2 Rebase on current `origin/main`, re-check active-change overlaps, and update evidence freshness.
- [x] 4.3 Push a draft PR with explicit no-runtime scope and validation evidence.

## 5. Landing lifecycle

- [ ] 5.1 Merge corrective PR #1621 first, then rebase and preserve its reviewed STATUS/legacy-authority corrections plus this lane's two intentional successors.
- [ ] 5.2 After the broader `tests/` claim clears, promote and land the exact test-only `tests/test_desktop.py` GUI-entrypoint repair; require the focused assertion to pass.
- [ ] 5.3 After approval and 5.1-5.2, sync all four deltas into canonical specs and strict-validate the synced tree.
- [ ] 5.4 Archive the change and retire the Wave 1 STATUS row in the same landing lane.
- [ ] 5.5 Re-run the full-coverage audit and promote the next safe staged successor.
