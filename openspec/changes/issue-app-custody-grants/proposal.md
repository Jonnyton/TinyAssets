# Change: issue-app-custody-grants

Add a server-owned, dark authority that converts freshly authenticated external-app evidence into one exact, signed private-universe conversation custody grant handoff after resolving the existing founder-owned app mapping. The existing custody domain remains the owner of opaque in-process minting until its packaged mirror lane is available.

This is the custody issuer handoff for the V1 app conversation path. It does not add an MCP handle, ingress route, Slack reply adapter, runtime invocation, or workflow authoring behavior.
