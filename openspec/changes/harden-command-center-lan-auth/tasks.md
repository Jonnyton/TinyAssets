## 1. Red Security Proofs

- [x] 1.1 Add CLI/config tests proving the default listener and generated token, environment-or-generated token resolution, removal of secret-valued CLI flags, URL-safe 20-128 character validation, literal IPv4/IPv6 loopback acceptance, and invalid host/port/token rejection.
- [x] 1.2 Add production-server tests proving actual post-bind IPv4/IPv6 authority formatting (including `port=0` and port 80), backlog at least 64, and bad configuration or occupied-port failure before cache start, collector/external calls, worker creation, or durable effects.
- [x] 1.3 Add real-HTTP tests proving inert static assets remain anonymous while every API rejects missing, duplicate, malformed, wrong, and query-only tokens; matching singleton header tokens use constant-time authentication and expose no secret in URLs.
- [x] 1.4 Add Host/DNS-rebinding tests for GET, POST, OPTIONS, HEAD, and a default unsupported method proving missing, duplicate, comma, whitespace, hostname, forwarded, wrong-family, wrong-port, and hostile-domain authorities fail before routing, asset serving, authentication, cache reads, or collector calls.
- [x] 1.5 Add mutation tests proving deterministic Host → authentication → route → singleton Origin → singleton media → framing → UTF-8 JSON-object → strict-schema order, including absent/duplicate/wrong Origin, absent/duplicate/text/form/JSON media, transfer encoding, absent/nondecimal/duplicate/zero/negative/65,536/65,537 content lengths, five-second partial-body timeout, malformed/array/scalar/null JSON, unknown fields, wrong field types, NUL, and unpaired surrogates.
- [x] 1.6 Add a barrier-started 32-way production-server proof whose declared mix includes every anonymous/query-token read, cross-origin/simple-form/oversized/ambiguous-framing write, rebinding authority, and valid-auth dispatch-disabled hire class across GET and POST; assert deadline completion, no 5xx/timeouts, bounded bodies, zero cache/collector/provider calls or threads, and unchanged inbox/config/filesystem state.
- [x] 1.7 Add talk/hire tests proving dispatch-off talk permits only its inbox write, non-preset hire rejects before discovery/lookup/write/thread creation, `_hire_dispatch` rechecks the gate, preset-only updates remain available, and one shared atomic pool bounds eight mixed talk/hire provider process trees with the specified over-cap semantics.
- [x] 1.8 Add browser-source and response tests proving fragment-to-session-storage bootstrap, synchronous legacy-query/fragment removal without legacy promotion, header-only API calls, fragment reconstruction for sharing, and the exact CSP/referrer/no-sniff/frame/cache headers on representative successes and every error class with no CORS allow headers.
- [x] 1.9 Add fake-peer process probes proving Village/MCP bearers are absent while a provider-auth marker remains, the 540-second provider timeout is below the 600-second wrapper timeout, complete descendant process trees are terminated/reaped on inner and outer timeout paths, and shared reservations release only after normal exit or verified cleanup.

## 2. Fail-Closed Implementation

- [x] 2.1 Make configuration resolve `TINYASSETS_VILLAGE_TOKEN` or generated credentials, remove secret-valued Village/MCP CLI flags, validate token/listener/port inputs, and default to literal IPv4 loopback.
- [x] 2.2 Build a production IPv4/IPv6 loopback server with backlog at least 64, derive canonical authority from its actual bound address/port, and complete binding before starting the state cache or any observation.
- [x] 2.3 Reject invalid Host values before route handling, require constant-time singleton header-only authentication for APIs, keep static assets state-free, and centrally emit the exact security/cache headers on success and error responses.
- [x] 2.4 Enforce deterministic authenticated POST routing, singleton exact Origin/media headers, strict framing/UTF-8-object/schema and safe-scalar validation, connection close on framing failures, and secured non-successful OPTIONS/HEAD/unsupported methods with no CORS path.
- [x] 2.5 Gate non-preset hire dispatch before universe/provider discovery unless `--dispatch` was enabled, recheck at `_hire_dispatch`, and use one shared atomic pool to cap talk plus hire provider process trees at eight in flight without changing inbox-only talk or the non-spending preset path.
- [x] 2.6 Move the browser token from URL fragment to current-tab session storage, synchronously strip fragment and legacy query tokens without promoting legacy values, send the secret only in `X-Village-Token`, and reconstruct fragment-only share URLs.
- [x] 2.7 Update the README and CLI help to remove phone/LAN safety claims and secret CLI flags, document secure loopback/environment/generated token handling, and state that remote access awaits an authenticated-confidential HTTPS/tunnel design.
- [x] 2.8 Launch peers with Village/MCP bearers scrubbed but provider auth preserved, pass a shorter explicit provider timeout, own process groups at both launcher layers, and terminate/reap every descendant before returning from a timeout or releasing capacity.

## 3. Verification And Foldback

- [ ] 3.1 Run the focused command-center and peer-launcher suites, full `python -m pytest tests/ -q`, full `python -m ruff check`, Python compilation, diff/secret scans, and the 32-way hostile concurrency proof using fake peers without invoking any provider/model.
- [ ] 3.2 Run strict validation for this change and the complete OpenSpec tree.
- [ ] 3.3 Exercise the loopback UI in a real browser: fragment bootstrap, authenticated polling/chat/preset, secret-free requests/history, failed hostile Host/Origin probes, and dispatch-off behavior.
- [ ] 3.4 Obtain independent security and code-review approval for startup ordering, listener authority, browser secret transport, mutation framing/schema, hostile concurrency, bearer scrubbing, process-tree cleanup, and quota-spend boundaries.
- [ ] 3.5 Sync the modified requirement into the canonical spec, archive the change, update coordination/reflection records, and publish the reviewed PR.
