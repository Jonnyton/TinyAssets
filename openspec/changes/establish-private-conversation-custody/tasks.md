## 1. Contract Admission

- [x] 1.1 Strict-validate this proposal/design/spec/task set, obtain an independent exact-head architecture and security review, and fold every blocking finding before production-code implementation. Completed at approved exact head `21d5bbae935a8afc9f8648656f3c816f43318153`: seven review rounds closed every custody, authority, path, canonicalization, idempotency, deletion, concurrency, export, task, and packaging blocker; the independent Codex fallback recomputed every vector and approved with no Critical/Important finding after two Claude CLI attempts failed with exit 1 and empty stderr. `openspec validate establish-private-conversation-custody --strict`, all 70 strict validations, flow admission, and `git diff --check` passed.

## 2. Typed Authority and Custody Contract

- [x] 2.1 Test-first, add the one-use operation-grant consumer seam and detached evidence validation to `tinyassets/conversation_custody.py`. Acceptance: evidence binds action/request digest, exact canonical 32-byte-key wire/full-string digest or read/export null, exact scope, live selection generation, registered directory, and trusted platform root; noncanonical pad bits, aliases, and mismatches fail; absent first-use files and sidecar transitions are permitted while existing-primary identity is stable; randomness/provider-ID origin remains an upstream issuer invariant; same-account races are inside this mode's trust boundary; no production issuer/constructor ships. Correction: inherited grants are PID-bound, the registry/key reset after POSIX fork, the synthetic PID-mismatch regression passes, and an actual WSL fork probe reports `parent=accepted child=rejected exit=0`. The full focused suite passes with 69 tests and one Windows-only POSIX skip.
- [x] 2.2 Test-first, add immutable records plus canonical JSON, exact node/type/Unicode/integer/escape/byte rules and vectors, 256-byte ref/64-byte kind grammars, six-fraction UTC times, exact deleted-target preimage/vector, exact create/append/read/export/delete request mappings and digest vectors, reply rules, and exact export envelope/digest. Acceptance: malformed/ambiguous/oversized inputs fail before grant consumption; insertion order canonicalizes identically; bounded members round-trip; unsupported modes fail without fallback. Verification: RED was captured for all missing domain symbols and for custom-container coercion; 38 focused tests now pass with exact canonical, key, operation, deleted-target, immutable-record, reply/ordinal, and deterministic export vectors. Ruff and diff checks pass.

## 3. Private-Universe Persistence

- [x] 3.1 Test-first, add private `tinyassets/storage/conversation_custody.py` persistence derived only from consumed registered-path evidence, with universe-local idempotency, immutable thread creation, exact authorized reads, append-only identified messages, earlier-same-thread replies, and one `BEGIN IMMEDIATE` order. Acceptance: concurrent identical create/append retries create one result; changed retries conflict; deliberate cross-universe key reuse is independent; distinct appends are contiguous; reply-first/cross-thread targets fail without ordinal consumption; raw storage is not publicly exported. Correction: every database is transactionally bound to exactly one universe; alias/rebind attempts fail with `storage_universe_mismatch`; legacy databases infer the binding from canonical thread/deletion state before admitting a universe; and genuinely separate universe paths remain independent. Targeted regressions and the full 69-test focused suite pass.
- [x] 3.2 Test-first, add deterministic export, operation-scoped idempotency, the unique content-independent deleted-target index, exact two-value reason domain, tombstone transition, pending cleanup intent, secure-delete/WAL truncation, and receipt recovery. Acceptance: append/read/export versus delete follows transaction order; premature/wrong-scope deletion is atomic; same/different keys and reasons plus post-delete retries follow exact semantics; corrupt content is deletable without disclosure; interrupted cleanup resumes; successful quiescent deletion leaves no private sentinel in primary/sidecars and states backup limits. Correction: retention deletion now reconstructs the immutable canonical thread before using its boundary; create/append tombstones retain only operation/key identity with no target correlation; logical deletion and cleanup completion each take their own trusted clock reading. The four reviewer repros and all 67 focused tests pass.

## 4. Packaging and Dark Boundary

- [x] 4.1 Mirror both canonical modules into the packaged universe-server runtime and prove no public MCP handle, app/provider import, network call, binding mutation, definition/lineage mutation, credential field, or production construction path was added. Acceptance: a focused test explicitly asserts both packaged paths exist and are byte-identical to their canonical files, and the exact-seven public-handle assertion is unchanged. Both correction modules are byte-identically remirrored; the full focused suite, Ruff, format, and dark-boundary assertions pass.

## 5. Verification and Foldback

- [ ] 5.1 Run the focused suite plus relevant custom-agent, runtime-manifest, storage, security, race, Ruff, mirror, strict OpenSpec, and bounded-flow gates; obtain independent exact-head code/security review and fold every blocking finding. Acceptance: fresh command output is green, the reviewer approves the exact head, and review evidence names any same-provider fallback if opposite-provider review is unavailable.
- [ ] 5.2 Sync `conversation-custody` into `openspec/specs/`, archive this completed change, publish/merge the single PR, retire the STATUS/worktree claim in the landing lane, and hand the fixed-universe context/store interfaces to `connect-custom-agent-app-conversations` without activating app ingress or production storage. Verification: merged current main contains the synced spec and modules, no active change or STATUS claim remains, and the successor handoff records all remaining authority prerequisites.
