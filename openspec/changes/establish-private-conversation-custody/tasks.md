## 1. Contract Admission

- [ ] 1.1 Strict-validate this proposal/design/spec/task set, obtain an independent exact-head architecture and security review, and fold every blocking finding before production-code implementation. Acceptance: the review explicitly covers custody placement, authenticated context, cross-scope disclosure, idempotency/races, export, deletion residue, and the dark integration boundary. Verification: `openspec validate establish-private-conversation-custody --strict`, `openspec validate --all --strict`, and `python scripts/openspec_flow.py check-change establish-private-conversation-custody --provider codex-gpt5-desktop` all exit zero at the reviewed head.

## 2. Typed Authority and Custody Contract

- [ ] 2.1 Test-first, add the one-use operation-grant consumer seam and detached evidence validation to `tinyassets/conversation_custody.py`. Acceptance: evidence binds action/request/key digest, exact scope, live selection generation, and registered absolute path; missing/replayed/expired/revoked/mismatched grants, caller paths, platform-root placement, symlinks, and Windows reparse points fail before storage; no production issuer or constructor ships. Verification: focused tests demonstrate RED then pass with only an explicit test issuer.
- [ ] 2.2 Test-first, add immutable thread/message/receipt records plus `tinyassets-canonical-json/v1` validation, exact structural/type/Unicode/integer/byte bounds, IDs, request digests, reply rules, and deterministic private export. Acceptance: malformed/ambiguous/oversized inputs fail before grant consumption; different mapping insertion orders canonicalize identically; arbitrary bounded members round-trip; unsupported custody modes fail without fallback. Verification: focused domain tests demonstrate RED then pass.

## 3. Private-Universe Persistence

- [ ] 3.1 Test-first, add private `tinyassets/storage/conversation_custody.py` persistence derived only from consumed registered-path evidence, with immutable/idempotent thread creation, exact authorized reads, append-only identified messages, earlier-same-thread replies, and one `BEGIN IMMEDIATE` order. Acceptance: concurrent identical create/append retries create one result; changed retries conflict; distinct appends are contiguous; reply-first/cross-thread targets fail without ordinal consumption; raw storage is not publicly exported. Verification: focused persistence plus cross-process create/append/reply races pass with WAL, foreign keys, and exact path assertions.
- [ ] 3.2 Test-first, add deterministic export, the operation-scoped idempotency ledger, deletion tombstone transition, pending cleanup intent, secure-delete/WAL truncation, and content-free receipt recovery. Acceptance: append/read/export versus delete follows the documented transaction order; premature/wrong-scope deletion is atomic; competing keys/reasons and post-delete create/append retries follow exact semantics; corrupt content is deletable without disclosure; corrupt scope fails closed; interrupted cleanup resumes; successful quiescent deletion leaves no unique private sentinel in primary/sidecars and the receipt states backup limits. Verification: focused race/export/deletion/corruption/recovery tests and direct byte scans pass.

## 4. Packaging and Dark Boundary

- [ ] 4.1 Mirror both canonical modules into the packaged universe-server runtime and prove no public MCP handle, app/provider import, network call, binding mutation, definition/lineage mutation, credential field, or production construction path was added. Acceptance: canonical/package files are byte-identical and the existing exact-seven public-handle assertion is unchanged. Verification: `python scripts/check_packaged_runtime_parity.py`, focused custom-agent/runtime regressions, and repository searches for forbidden integration paths pass.

## 5. Verification and Foldback

- [ ] 5.1 Run the focused suite plus relevant custom-agent, runtime-manifest, storage, security, race, Ruff, mirror, strict OpenSpec, and bounded-flow gates; obtain independent exact-head code/security review and fold every blocking finding. Acceptance: fresh command output is green, the reviewer approves the exact head, and review evidence names any same-provider fallback if opposite-provider review is unavailable.
- [ ] 5.2 Sync `conversation-custody` into `openspec/specs/`, archive this completed change, publish/merge the single PR, retire the STATUS/worktree claim in the landing lane, and hand the fixed-universe context/store interfaces to `connect-custom-agent-app-conversations` without activating app ingress or production storage. Verification: merged current main contains the synced spec and modules, no active change or STATUS claim remains, and the successor handoff records all remaining authority prerequisites.
