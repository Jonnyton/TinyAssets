# Shared background self

Replace briefing-only background prompts with an opt-in instance of the same universe conversation harness. Founder request: another me, with shared knowledge and tools, guided only by the cofounder direction.

Scope: immutable owner-authored Branch capability, fresh persona/current founder-thread reconstruction, and lossless live retrieval of retained conversation via the existing pinned engine tool. No new credential grants, provider changes, synthetic founder messages, or automatic learning from run output.
