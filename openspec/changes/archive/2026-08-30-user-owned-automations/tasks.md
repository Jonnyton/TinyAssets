## 1. Retire the fleet

- [x] 1.1 Remove the four `worker*` services and fleet env from `deploy/compose.yml`; fetch the droplet's live compose, reconcile, and only then let `deploy-prod.yml` ship the file. Verify: `docker compose config --services` on the droplet lists daemon/cloudflared/logs only; `deployed_sha` green.
  Verified 2026-08-29T21:10Z on the droplet (`sudo docker compose --env-file /etc/tinyassets/env -f /opt/tinyassets/compose.yml config --services` -> `daemon cloudflared logs`; `docker ps` -> tinyassets-daemon, tinyassets-tunnel, tinyassets-logs; no `cloud_worker` process). Repo half landed as #2677 + #2678. Shipping `deploy/compose.yml` from the repo on deploy landed afterwards as the deploy bundle transaction (#2685/#2696, concern deleted); the first real run reported `deploy_result=deployed` on 2026-08-29.
- [x] 1.2 Delete `tinyassets/cloud_worker.py`, `cloud_worker_healthcheck.py`, `.github/workflows/reconcile-stale-fleet.yml`, the `DEFAULT_HOST_USER` import in `fantasy_daemon/__main__.py`; relocate `_worker_model_for_provider` and `supervisor_heartbeat_filename`; update the named tests and `scripts/check_background_authority_inventory.py`, `scripts/retire_cheat_loop_deploy_fence.py`; rebuild the plugin mirror; sync the REMOVED requirements into `daemon-runtime-and-dispatch`. Verify: full pytest + `mirror-parity` green.

## 2. Schedules obey the rule

- [x] 2.1 Registration requires the authenticated owner with admin ACL, refuses `scheduler_unavailable` when the tick thread is not running, and the run actor is `universe:<id>`. Verify: tests that go red on the old code (anonymous accepted, actor mismatch), then a live registration refusal through the app.
  Landed #2690 (620b19e7, deployed 3434cdde). Live 2026-08-30T02:22Z, founder session in the web app (`MCP.callTool("extensions", {action:"schedule_branch", branch_def_id:"44d9cf94ee25", interval_seconds:60})`) -> `{"error":"trigger_invalid","reason":"interval_below_floor","interval_seconds":60,"minimum_interval_seconds":300}`. Boot log on the same deploy flags the founder's fleet-era row: `scheduler: schedule 1508d5dc-… is legacy (universe='' principal='') — never fired; the owner must re-register it`.
- [x] 2.2 Split the scheduler lifecycle from `TINYASSETS_INBOUND_ENABLED`; owner pause/delete on the schedule row. Verify: the founder's heartbeat schedule fires on `u-01kxm1vszd8hwp7em418asq8h9` and appears in the run list.
  Landed #2690. Live: founder re-registered the heartbeat at 2026-08-30T02:24:45Z -> `schedule_id 9a331b73-b28b-41b6-b900-8019e5eef446` (interval 300, universe `u-01kxm1vszd8hwp7em418asq8h9`); the scheduler started on serving with `TINYASSETS_INBOUND_ENABLED` unset. It fired on the first tick after the 02:24:51Z boot: `last_fired_at 02:24:58Z`, run `ab48eff7bc2c4722` (`scheduled:9a331b73`, actor `universe:u-01kxm1vszd8hwp7em418asq8h9`, 02:25:00.2Z → 02:25:09.5Z, `completed`) listed by `extensions action=list_runs branch_def_id=44d9cf94ee25` next to the automation's run. The founder's fleet-era row `1508d5dc` shows `legacy: true, recent_reason: legacy_row`. Owner pause/delete is built but unreachable from the app: `docs/concerns/2026-08-30-owner-cannot-pause-or-delete-own-schedule-from-app.md`.

## 3. User-owned automations

- [x] 3.1 Automation row + owner controls on `write_graph target=automation` (create/pause/resume/delete/list), universe-scoped, authenticated, fail-loud per D4. Verify: tests red→green; live create and list from the app.
  Landed #2693 (d08a82ce) + #2699 (3434cdde). Live 2026-08-30T02:18:56Z from the founder's web-app session: `write_graph target=automation operation=create payload_json={"name":"heartbeat","branch_def_id":"44d9cf94ee25","interval_seconds":300}` -> `automation_id d25a356811d0405c8bc92ebc9601c68f`, `desired_state active`, `owner.is_you true`; `read_graph target=automations` lists it beside the fleet-era rows as `{"legacy":true,"status":"retired_fleet_era"}`. The universe's own served `write_graph` refuses `target=automation` by design (SHAPES only), so an automation is registered by its owner, not by the universe.
- [x] 3.2 Consumer executes due automations on the CURRENT assignment through the served carrier with foreground admission/budget; `(automation_id, due_at)` fence; recorded refusal per skip; no runtime pin, no enrollment manifest. Verify: restart-survival and provider-switch tests; live run on the founder's universe with a receipt.
  Live receipt 2026-08-30 on the droplet (`/data/.automations.db`): attempt `(d25a356811d0405c8bc92ebc9601c68f, 2026-08-30T02:23:56+00:00)` claimed 02:23:56, `status completed`, `reason ok:ran:c500e5d9f7e0451a`; row `last_reason ok`, `consecutive_failures 0`. Run `c500e5d9f7e0451a` (`tiny_heartbeat_line_v1`, actor `universe:u-01kxm1vszd8hwp7em418asq8h9`) started 02:23:56.9Z, finished 02:24:06.4Z, node `heartbeat` ran on `provider_served codex` (the founder's own subscription) and wrote `still here 2026-08-30T02:24:03Z`. A deploy recreated the container at 02:24:51Z; the row and attempt survived it (they live on `/data`).
- [x] 3.3 Retire the fleet-era activation layer and the old-principal `cloud_automation_controls` / `background_branch_bindings` (explicit retired rows with reason). Verify: the pump ignores retired rows; refusal table stops growing.
  Partially true today, the rest re-proposed. Verified 2026-08-30T02:29Z on the droplet: `assigned_queue_refusals` is a keyed upsert (12 rows, one per refusal key) — it does not grow; the legacy automation rows list as `{"legacy":true,"status":"retired_fleet_era"}` and the legacy schedule row as `legacy_row`; neither fires. The deletion itself (−18k production lines in four PRs, measured map) is `openspec/changes/retire-activation-layer/` — split out so this change archives on its live proof rather than idling on it.

## 4. Prove and close

- [x] 4.1 Codex refutation of 3.x, live proof through the app (`ui-test`), spec sync, archive this change and `execute-assigned-queue-consumer`.
  Codex: three rounds each on automations (`automations-verdict` 1–3, final ADAPT folded in #2699), schedules (`schedules-verdict` 1–3, folded in #2690) and the compose transaction (#2685/#2696/#2700) — cap reached, remaining findings carried to the founder notes. Live proof 2026-08-30 through the founder's web-app session (2.1, 2.2, 3.1, 3.2 above). `execute-assigned-queue-consumer` archived 2026-08-29. Spec deltas synced into `openspec/specs/` in this close-out; this change archived with it.
