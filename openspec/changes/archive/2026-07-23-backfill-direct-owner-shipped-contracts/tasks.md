## 1. Draft As-Built Contracts

- [x] 1.1 Draft requirement deltas for a full `MODIFIED` live-endpoint prompt owner plus tool metadata, status identity variants, universe switching, and four uptime controllers
- [x] 1.2 Strictly validate the complete draft change without modifying canonical specs or runtime code

## 2. Verify Current Evidence

- [x] 2.1 Run focused metadata, status-shape, and universe-switch tests for representative public/interface paths; source-review the exact four-prompt catalog, thirteen-tool table, config-error shape, early omission, and substring false-positive edges — 97 passed; three stale assertions exposed the retired `workflow` tag and early-response `session_boundary` mismatch
- [x] 2.2 Run focused DNS, LLM-binding, release-reconcile, disk-watch, transcript-rotation, and auto-prune evidence without running `tests/test_uptime_canary_layer2.py` through Codex on Windows — 118 passed plus release structural proof; forbidden file not run
- [x] 2.3 Obtain independent requirement-to-source review and resolve every overclaim, omission, and active-delta collision — both focused reviews approved after corrections

## 3. Clear Requirement-Level Owners

- [x] 3.1 Isolate the credential requirements behind PR #1606 in `backfill-credential-vault-shipped-contracts`
- [x] 3.2 Compare prompt/status/switch requirements against connector, legacy-tool, identity/reset, and universe lifecycle deltas; preserve their distinct as-built owners
- [x] 3.3 Compare release reconciliation against the event-trigger lane; require that future behavior to modify the synced owner
- [x] 3.4 Retain the bounded Forever Rule section 14 limitation: workflow groups serialize running jobs but may replace pending runs, and this spec-only foldback claims no launch-scale load proof

## 4. Fold Back Canonical Truth

- [x] 4.1 Broaden the STATUS Files boundary only for dependency-cleared canonical owners
- [x] 4.2 Sync approved deltas while preserving every untouched canonical requirement block
- [x] 4.3 Update the full-coverage audit and legacy disposition (no canonical spec index file exists); strictly validate the full tree and run the drift self-test
- [x] 4.4 Archive the synced change
- [x] 4.5 Land through reviewed PR #1626 and replace the completed broad STATUS claim with the credential remainder
