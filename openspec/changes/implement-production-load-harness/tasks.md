## 1. Review And Ownership Gates

- [x] 1.1 Map this dependent change to `harden-production-load-evidence` tasks 2.1-2.5 and preserve capability-local ownership of workloads, thresholds, adapters, faults, predicates, and activation.
- [x] 1.2 Confirm the target OpenSpec write set is collision-free while `tests/load/_protocol/**`, `tests/load/README.md`, CI, and adapter files remain unclaimed.
- [ ] 1.3 Obtain independent correctness, security/privacy, concurrency, evidence-integrity, and simplicity review of the exact target change; resolve every Critical and Important finding.
- [ ] 1.4 Obtain a fresh current-main Claude Opus 5 review before push or apply; do not treat a model-rate reset as proof while account spend/credits still block inference.
- [ ] 1.5 Re-run STATUS/worktree/provider-context and exact prospective runtime file collision checks before adding any implementation Files.

## 2. Shared Protocol Kernel

- [ ] 2.1 After overlapping `tests/` claims release or narrow, claim only `tests/load/_protocol/**`, `tests/load/README.md`, and focused conformance tests; do not claim capability adapters.
- [ ] 2.2 Implement the bounded Python surface in `schema.py`, `artifacts.py`, `environment.py`, `rollup.py`, and `__init__.py`; add only a protocol README and focused test module beyond that package. V1 has no CLI.
- [ ] 2.3 Implement duplicate-rejecting guarded JSON decoding and RFC 8785 encoding/canonical comparison with exact schema versions, integer units, ASCII identifiers/keys, closed fields, stable validation codes/paths, exact limits/error precedence, and golden valid bytes/digests for every V1 scenario/command/argv/blocker registry, selection, pre-traffic gate set, dispatch-attempt/ref sets, bundle-index, manifest/nested record, aggregate, validation/safety result, trust-set, authenticated-receipt, event, fault, and reconciliation shape plus malformed, percentile, and mutually exclusive `invalid_type|invalid_value|invalid_identifier|invalid_root|invalid_path|artifact_linked|artifact_not_regular` reachability fixtures.
- [ ] 2.4 Implement history-aware versioned scenario-registry publication plus context-free loading; require the prior accepted registry for a non-genesis publication and validate predecessor/version/classification changes, ownership, applicability, required/optional status, justification, substrate, command, adapter, oracle, fault, threshold, scenario custody mode, accepted backend-policy digest, and shared/owner-namespaced blocking-code references without interpreting owner-local semantics.
- [ ] 2.5 Implement rooted content-addressed JSON/JSONL raw-artifact storage, canonical per-run bundle indexes, and exclusive atomic manifest publication; reject absolute/parent/alternate-stream paths, duplicate logical names, symlinks, junctions/reparse points, unexpected hard links, digest/length mismatch, manifest/index membership mismatch, mutation, and address collision without scanning unrelated CAS objects. Opaque artifacts cannot satisfy required privacy/recomputation gates without an accepted scanner.
- [ ] 2.6 Implement immutable supersession with explicit prior canonical-manifest input and exact digest/schema/run-ID resolution, full digest-tree reload/verification, allowlisted environment-fingerprint collection, stable error precedence, idempotent identical publication, typed owner-result interfaces, and exact failure-first scenario/aggregate rollup without executing or embedding owner predicates, thresholds, fault/reconciliation semantics, or baseline comparisons.

## 3. Independent Recomputation And Anti-Fabrication

- [ ] 3.1 Recompute universal declared attempted/offered/sent/admitted/rejected/timed-out/cancelled/committed/claimed/delivered/settled/lost/duplicate populations from raw events and reject omitted or success-only denominators. Preserve retry identity with logical-operation pseudonym, contiguous attempt index, per-attempt pseudonym, retry/backoff and planned/actual timing; offered counts logical operations while attempted/sent count attempts.
- [ ] 3.2 Recompute p50/p95/p99/max from integer raw samples with named operation, units, sample count, and denominator; record planned-send and actual-send/receive timing so coordinated omission and client queueing remain visible.
- [ ] 3.3 Validate source/image/rollout/config identity, clean/dirty state and diff digest, clocks, topology, service tiers, database/pool/queue/Realtime/network facts, participant resources, arrival model, phase timings, and the closed generator-health record with fixed offered/achieved, queue-delay, CPU, memory, network, socket/connection, drop, skew, sampling, and raw-digest fields. Missing health uses `generator_health_missing`; malformed health uses `generator_health_record_invalid`; owner rules retain saturation thresholds.
- [ ] 3.4 Validate typed, digest-bound owner-supplied oracle, threshold, fault, reconciliation, and baseline-result records without executing or interpreting their semantics. Reject unknown verdicts; shaped/mock `passed`; `not_run` without versioned blockers; empty required/applicable pass; unjustified classification downgrade; unevaluable-as-held; empty/unverifiable required fault timelines; reconciliation discrepancies outside `expected|loss|duplicate|unexplained`; or an aggregate missing any constituent digest/substrate. For missing required fingerprint, denominator, fault, reconciliation, or baseline evidence, prove a structurally valid blocked manifest evaluates `not_run`, while a claimed pass is refused and separately proven required violations remain `failed`.
- [ ] 3.5 Prove offline protocol conformance can pass while every unavailable real production scenario remains independently `not_run`; golden-rollup cases cover optional failed/not-run, inapplicable failed/not-run, an empty required/applicable set, and mixed required failed plus not-run without letting optional/inapplicable constituents alter the verdict.

## 4. Isolation, Privacy, Authorization, And Cleanup

- [ ] 4.1 Enforce closed evidence schemas with no user-authored content, raw identities, bearer/cookie/token/grant/credential values, provider homes, arbitrary environment values, or local paths; require deployment-scoped pseudonyms and access-classified counts/hashes/distributions.
- [ ] 4.2 Validate typed, digest-bound owner receipts for sanitized child environments, client and server pre-dispatch authority/tripwires, allowed egress, and non-secret attempt evidence. Support only `provider_free`, `requester_byoc`, and `accepted_market`; require exact authority/exclusion digest agreement, full-interval coverage, zero attempts for provider-free, and zero unauthorized or maintainer-authority attempts for BYOC/market. Do not launch processes, inspect ambient authority, constrain networks, select authority, spend quota, or execute tripwires in the shared kernel.
- [ ] 4.3 Validate explicit issuance/current trust-set and revocation-review inputs plus authenticated envelopes for authorization, nonce-consumption/append-only replay witness, distinct operator-domain local opt-in, capture, URL/DNS/TLS identity, provider attempts, backend policy, traffic start/stop, exhaustive dispatch receipts, abort/canary, cleanup, recovery, and lockout. Cross-bind every receipt-ref field and authorization digest, validate keys at receipt issue time, preserve historical validity across ordinary expiry/prospective revocation, apply reviewed retroactive compromise, recompute the pre-traffic gate-set digest, verify pinned environment/envelope/identities/registry/reset/cleanup bindings, reject dispatch sends or send intervals that reach beyond authorization expiry, require traffic stop and provider/canary coverage through every resource release, aggregate overlapping run-wide overshoot budgets, and require distinct trust domains for independent capture.
- [ ] 4.4 Encode the dependency fields by which a future connector-owner adoption proves accepted scoped reset, two ordinary real test subjects, private alias roster, fingerprint key, provider-attempt receipts, provider-free path, host-approved isolated envelope, and canary coordination.
- [ ] 4.5 Validate immutable owner-supplied cleanup/lockout receipts containing reset plan, inventory/roster revisions, lease/fence, namespace, pre-run witness, commit witness, planned/deleted/preserved/residual counts/digests, post-run inventory, foreign-state stability, and monotonic durable environment lockout state. Emit a closed validation result that keeps protocol status, nullable capacity verdict, and non-aggregating `safe|failed_locked|unknown_locked|unresolved` safety disposition separate. After authenticated traffic start, missing/invalid stop, coverage, cleanup, required abort, or lockout-on-failure proof yields protocol-invalid, null capacity, `unknown_locked`; unidentifiable malformed input yields `unresolved`; do not perform cleanup or lockout in the shared kernel.
- [ ] 4.6 Conformance-test forged/replayed authorization and nonce-consumption evidence, operator opt-in forgery, trust-set revocation, redirect/DNS/TLS mismatch, missing/attempted server tripwire, provider-free attempt, contradictory provider-free authorized count, valid requester-BYOC/accepted-market attempts, authority-digest mismatch, maintainer-authority attempt, pre-run-only provider receipt, canary/provider interval gaps, malformed dispatch-attempt/ref sets, dispatch/event one-to-one reconciliation gaps, empty dispatch stop, a pre-expiry dispatch with a post-expiry send or send-end, split-receipt cumulative/overlap/overshoot-cap evasion, zero-length/disjoint claimed intervals over overlapping sent-event times, concurrency/connection drain overlap, traffic stop or provider/canary coverage ending before resource release, missing traffic stop, scenario/leaf custody or backend-policy mismatch, the one valid null-policy `not_run` manifest with zero leaves, rejection of every null-policy manifest retaining a leaf, same-trust-domain capture, missing access enforcement, malformed or contradictory traffic-start/abort/cleanup/lockout receipts, valid cleanup failure versus missing/invalid cleanup proof, cleanup omission after traffic, forged/stale/process-local lockout clear, restart persistence, retry amplification, generator saturation/clock loss, path/link swaps, tenant bleed, supersession run/digest mismatch, exact `traffic_started=yes|no|unknown` derivation, and exact pre/post-traffic capacity/safety transitions without injecting faults or controlling a workload.

## 5. Owner Adoption Handoffs

- [ ] 5.1 File a separately accepted `live-mcp-connector-surface` adoption change for the current single-origin connector/storage baseline. It owns generators, target/DNS/TLS resolution, isolation and server/client tripwire execution, egress, abort/canary control, scoped cleanup, durable lockout, workload, thresholds, predicates, baseline comparison, and live traffic.
- [ ] 5.2 File owner-local adoption changes for operator admission, distributed execution, PostgreSQL, Realtime, market, and later capabilities; each owns execution and emits typed results and `not_run` packets without moving semantics into the shared package.
- [ ] 5.3 Keep every unavailable or unadopted PostgreSQL/RLS/pooling/CAS/outbox, Realtime, fleet, settlement, failure-control, provider, and connector scenario `not_run`; the shared change does not run them.

## 6. Completion Gates

- [ ] 6.1 Before runtime implementation lands, either rehabilitate an unconditional required-test gate or separately claim a secretless focused protocol workflow. Run protocol conformance on Windows and Linux, focused security/path/property tests, the full applicable suite, Ruff, and strict OpenSpec validation with dated environment and exact commands; never run live scenarios in CI.
- [ ] 6.2 Obtain independent code-to-requirement, security/privacy, concurrency, evidence-integrity, and diff-simplicity review against the exact landing SHA.
- [ ] 6.3 If public behavior changes, run canonical `/mcp` canaries, rendered chatbot `ui-test`, and post-fix clean-use evidence; protocol-only offline code does not fabricate this proof.
- [ ] 6.4 After the protocol kernel, cross-platform conformance suite, one approved real owner adapter, immutable evidence, and all applicable gates pass, use one landing lane to archive/sync `harden-production-load-evidence` first, strict-validate, then archive/sync `implement-production-load-harness`, strict-validate canonical `production-load-evidence` and all OpenSpec, and never archive either change alone.
