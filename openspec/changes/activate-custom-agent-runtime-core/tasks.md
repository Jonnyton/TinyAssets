## 1. Review and Exact Owner Handoff

- [x] 1.1 Run strict OpenSpec and bounded-flow checks, fold an independent exact-head architecture/security review, then obtain exact current-main activation, provider-work, continuation, grant-resolver, health, packaged-mirror, and test handoffs before broadening STATUS Files; explicitly fence the Branch-only background-attempt owner out of agent invocation. Completed 2026-08-02 in `docs/audits/2026-08-02-agent-runtime-invocation-handoff.md`: both changes strict-valid, bounded-flow audit clean, exact current owners and missing invocation/health seams recorded, Branch-only attempt owner fenced out, and independent exact-head Codex architecture/security review approved `e6382212` after the Claude and first Codex attempts hit bounded provider timeouts.

## 2. Immutable Subject, Manifest, and Compilation

- [x] 2.1 Test-first, add the shared typed execution subject to canonical activation, derive the sole agent automation key from `(universe_id, agent_binding_id)`, and prove concurrent aliases cannot create a second row while every Branch CAS/epoch/executor/lease guard remains unchanged. Completed 2026-08-01 in PR #2082: canonical and packaged activation/admission/task owners now use `ExecutionSubject(kind, ref, digest)`; generic creation cannot enter the reserved agent namespace and activation/rebind/claim enforce kind-to-namespace coupling; the reserved agent key is deterministically derived from the binding within the universe; legacy stopped rows migrate transactionally while active legacy rows fail closed; Branch admission refuses agent subjects; eight-way alias/migration races, 383 broader authority regressions, Ruff, strict OpenSpec, and 303-file mirror parity pass. Opposite-provider review approved exact runtime head `e8b50d2f`; final current-main integration binding gates landing.
- [x] 2.2 Test-first, add immutable agent runtime manifest persistence with exact binding/definition/adapter/reference/budget pins, canonical digest, atomic failure, owner-scoped idempotency, private reads, and no credentials/conversations/outputs/effects/runtime state. Completed 2026-08-01 in #2091 with canonical and packaged manifest/store owners, source-truth revalidation, tamper refusal, concurrent single identity, and private bounded persistence; the capability remains dark and does not activate or execute the manifest.
- [x] 2.3 Test-first, add the governed component-adapter registry and deterministic exhaustive component compiler with explicit runtime modes, descriptive-only data, unsupported/type/confinement refusal, and complete deterministic diagnostics. Completed 2026-08-01 in #2095: 96 focused tests, Ruff/format, strict OpenSpec, packaged isolated import/mirror parity, and independent exact-head review passed at `0b643972`; merge commit `21bbde3e` is present on current main. Revalidated in the cloud-drain integration with the combined 685-test authority/runtime suite.
- [x] 2.4 Test-first, add governed plan-adapter compilation with adapter-declared topology/entry/coverage semantics and one bounded provider-turn plan/component pair; prove no universal DAG, single-entry, fixed taxonomy, or silent component omission. Completed 2026-08-01 in #2097: 108 focused tests, Ruff/format, strict OpenSpec, packaged isolated import/mirror parity, and independent exact-head review passed at `1b15b6ee`; merge commit `ecddf165` is present on current main. Revalidated in the cloud-drain integration with the combined 685-test authority/runtime suite.

## 3. Delegated Requester-Owned Execution

- [x] 3.1 Test-first, derive the immutable agent runtime principal and live-check current delegated capability/resource/provider grants on activation, invocation, and resume without accepting caller-authored actors or owner/maintainer/ambient authority. Completed 2026-08-03: the new dark activation composition accepts only an authenticated owner's manifest selection, re-resolves the exact immutable manifest plus every live grant class under the canonical activation write fence, derives the reserved subject/executor/time/lease server-side, and converges concurrent exact replays without minting replacement authority; revoked grants, foreign manifests, conflicting principals, unfenced store access, and lease failure all fail closed. Nine activation tests, the 82-test activation/principal/invocation/provider/continuation family, changed-file Ruff, strict OpenSpec, isolated packaged import, and 330-file mirror parity pass. Independent Claude Sonnet code/security review approved exact implementation head `861940a3` with no blocking finding; PR #2230 merged as `1a799ae8` after all required hosted checks passed. The broader change-level 5.2 gate remains open.
- [x] 3.2 Test-first, atomically consume a live authenticated provider-work binding draft into one linked `ProviderWorkBinding`, server-authored `AgentInvocationCommand`, and append-only invocation root; prove exact replay, changed-input conflict, concurrent single winner, missed-boundary/no-write behavior, bearer-free recovery provenance, and helper/dispatcher/queue bypass refusal. Accepted at reviewed repair head `578bcb4b`: the canonical aggregate is atomic, request-bound, server-sealed, replay-safe, and revalidated before every authority transition.
  - [x] 3.2d Reconcile the atomic writer and every downstream provider/continuation/health consumer onto the canonical #2155/#2160 record shapes; remove the duplicate command/root owner and repeat exact-head security/concurrency review. `9201bb4a` removed the duplicate shapes; `655f9331` repaired the four reproduced review failures with a server-HMAC-sealed admission witness, exact request nonce binding, and generation-fenced claim/reservation/continuation takeover; `578bcb4b` completed full command-anchored health lineage. Four scoped independent Codex reviews approved, 517 related authority/runtime tests pass, and all 325 canonical/plugin Python mirrors match.
- [x] 3.3 Test-first, admit only that command/invocation lineage into canonical provider-work reservation/claim/launch with requester-owned credential routing, conserved budgets, typed non-authoritative output, and unchanged Branch authority guards. Revalidated after 3.2d at `578bcb4b` by the 517-test authority/runtime/Branch family and separate admission/recovery reviews.
  - [x] 3.3a Closed receipt union — completed 2026-08-02 at `2c47251c`: `agent_invocation` carries an `agent_runtime_manifest` subject plus exact command/invocation lineage and forbids Branch fields; Branch kinds carry `branch_version` plus Branch lineage and forbid agent fields; persisted V1 Branch receipts remain digest-stable/readable. 101 focused provider/continuation tests, mirror parity, and independent opposite-provider review found no blocking defect.
  - [x] 3.3b Atomic agent receipt issuance — completed 2026-08-02 at `2a9d6363`: the canonical service resolves exact command/invocation integrity, manifest subject, activation, fresh transactional grants, original principal, current transactional provider assignment, binding/revocation/budgets, and receipt insertion under one `BEGIN IMMEDIATE`; time is sampled after lock acquisition. Eight writers converge on one receipt, stale/missing authority writes nothing, and raw/wrapped/legacy-generic agent receipt bypasses fail while Branch issuance remains unchanged. 128 combined authority/runtime/Branch/mirror tests, Ruff, strict OpenSpec, pre-commit mirror/import gates, and exact-head independent security re-review pass.
  - [x] 3.3c Fresh claim/reserve/launch wrappers — completed 2026-08-02 at `7ebe5b57`: each transition revalidates exact invocation, manifest, activation, grants, principal, provider assignment, binding, and budgets under `BEGIN IMMEDIATE`; one-shot process-local grants are consumed on every outcome and exception, launch authorization precedes receipt-scoped reservation observation, and one of eight launchers receives the non-serializable carrier while restart replay cannot remint it. Revoked/expired authority fails closed, one budget envelope is conserved, generic helpers cannot advance agent work, and no provider call occurs yet. 159 combined agent/provider tests, Ruff, strict OpenSpec, 312-file mirror parity, pre-commit gates, and exact-head independent security re-review pass.

  Concurrent foundations folded in 2026-08-02 from #2155 and #2160: the private read-only, tamper-detecting invocation-root evidence source and separate inert command source retain their stable contracts. PR #2145's atomic admission/provider owner remains the positive authority integration; the duplicate command shapes require exact reconciliation before sync/archive. See `docs/audits/2026-08-02-agent-runtime-{invocation-root,command}.md` and `docs/concerns/2026-08-02-cloud-agent-command-duplication.md`.

## 4. Single-Active Recovery and Health

- [x] 4.1 Test-first, compose agent invocation with canonical cloud continuation and restart reconciliation under the same subject/command/invocation/provider identities; prove no `BackgroundBranchAttempt`, replacement identity, graph/effect/app, or public-control path is reached. Revalidated after 3.2d at `578bcb4b`, including concurrent pre-launch recovery under the same claim/reservation/continuation identities.
  - [x] 4.1a Canonical prepared continuation — completed 2026-08-02 at `fe624bc9`: after fresh transactional authority validation, the canonical cloud-continuation owner persists one non-authorizing `agent_invocation` continuation binding the exact activation subject/epoch/lease, command, invocation, provider binding, receipt, claim, reservation, typed-input digest, and budgets. Eight preparers converge on one identity; restart and post-launch-arm reconciliation replay it; stale activation writes nothing; no Branch attempt table is created or read. 126 combined agent/provider/Branch-continuation tests, Ruff, strict OpenSpec, 316-file mirror parity, pre-commit gates, and exact-head independent architecture/security review pass.
  - [x] 4.1b Actual provider call and typed outcome — completed 2026-08-02 at `828908d6`: the governed single-provider adapter verifies the exact canonical typed-input digest, resolves the registered requester universe, consumes the one-shot carrier at `ProviderRouter`, and permits no ambient universe or fallback provider. The canonical outcome owner atomically CAS-finalizes the same launched reservation with one ≤32 KiB inert `provider_text` result or a typed failed/indeterminate blocker; provider identity mismatch, timeout, oversized output, and authority loss after the call retain no provider text. Exact replay performs no second call. 159 focused authority/router tests, Ruff, bytecode compilation, strict OpenSpec, 318-file mirror parity, pre-commit gates, and exact-head independent architecture/security review pass.
  - [x] 4.1c Restart reconciliation — completed 2026-08-02 at `1ff5918c`: fresh service instances resume admitted, reserved, and prepared work under the original invocation/reservation/continuation identities and make one call. Normal execution refuses `launch_started`; recovery waits for the exact original claim window to expire, then eight concurrent reconcilers converge on one indeterminate blocker and CAS-finalize the same reservation without a provider call, carrier remint, replacement identity, or spend. 163 focused tests, Ruff, strict OpenSpec, 318-file mirror parity, pre-commit gates, and corrected exact-head independent security review pass.
- [x] 4.2 Test-first, add the private useful-progress projection over canonical owner records with exact subject/epoch/lease/grant/budget revalidation, stale-executor fencing, and no-progress alarms. Accepted at `578bcb4b`: health now anchors the continuation to canonical command/event plus the complete receipt/claim/reservation chain; exact-head independent review approved and 9/9 health tests pass.

## 5. Dark Verification and Foldback

- [ ] 5.1 Run focused/regression/security/fault/type/lint/mirror suites, cross-process identity races, §14 production-shaped load, worker-restart recovery, existing Branch authority regressions, and dark deployment health using only requester-owned authority; verify seven public handles and app/workflow/effect routes remain unchanged.
  - [x] 5.1a Complete the explicitly shaped local concurrency/restart slice without claiming live-cloud proof. At reviewed head `dde16358`, 64 requests across eight spawned processes converge before and after worker replacement/claim expiry, and a separate 64-request spawned-process launch wave produces one filesystem-visible provider call, exact typed losers, and one terminal replay identity. The load review and authority-race re-review approved; see `docs/audits/2026-08-02-cloud-agent-production-shaped-load.md`. Dark deployment, live substrate, public canary, and environment evidence remain open under 5.1.
  - [x] 5.1c Extend the shaped local proof across the trusted activation composition without claiming live-cloud evidence. On 2026-08-03, 64 requests across eight spawned processes converged on one epoch and one filesystem-visible server lease mint; a fresh-process restart wave replayed the exact identity; and a post-revocation fresh-process wave returned 64 typed denials without changing the row or lease. The combined activation/provider/restart load suite passed 4 tests in 27.06s, 189 broader authority regressions passed, and SQLite integrity remained `ok`; see `docs/audits/2026-08-03-agent-runtime-activation-load.md`. Dark deployment, public-route/deployed-head proof, and environment evidence remain open under 5.1.
  - [x] 5.1b Accept the admission-seal dark deployment at merged revision `81c01fa7`: immutable request-key custody, host-pair distinctness, daemon and four-worker health, exact-seven surface, canonical public canary, Access gate, and terminal release receipt passed in production deploy run `30777056289` attempt 2; the immediately triggered uptime canary `30777346026` also passed. This does not claim a registered/claimable user workflow or satisfy rendered chatbot and 24-hour PC-off acceptance. See `docs/audits/2026-08-03-cloud-agent-admission-seal-dark-deploy.md`.
- [ ] 5.2 Obtain independent exact-head code/security review, sync `custom-agent-runtime-core` plus both modified owner deltas, archive this change, retire its STATUS claim, and hand immutable manifest/principal/activation/invocation/health seams to the separately admitted app, workflow, and control successors.
