## 1. Migration evidence gates

- [ ] 1.1 Land `reconcile-external-connector-manifests`; verify every current supported manifest and submission packet names canonical `/mcp` or is withdrawn, and none names a reviewed-directory surface.
- [ ] 1.2 Record the host-approved supported-client matrix and telemetry observation window in a durable retirement-evidence artifact before evaluating call data.
- [ ] 1.3 Collect hidden-name call telemetry for the full predeclared window; prove zero calls or identify and migrate every caller of `universe`, `community_change_context`, `extensions`, `goals`, `gates`, and `wiki`.
- [ ] 1.4 Complete and record a rendered chatbot conversation through canonical handles for every host in the approved client matrix.
- [ ] 1.5 Obtain independent interface/security review of the evidence and explicit host approval for the breaking removal.

## 2. Fresh ownership and caller inventory

- [x] 2.1 Refresh `origin/main`, run provider-context and worktree scans, and rerun file-collision checks before claiming runtime files; resolve or depend on the owners observed in PRs #1560, #1550, #1549, #1493, #1478, #1467, #1466, #1465, and #1464. — 2026-07-25 (re-verified in inventory v2): 5 of 9 CLOSED; 4 remain OPEN (#1493 `a4115dd3`, #1467 `5fb6d16a`, #1466 `4427d012`, #1465 `8eef71b8`) and all edit `universe_server.py`. Per "resolve **or depend**", these are recorded as **binding preconditions on task 4.1** in inventory §11, not advisory notes. `claim_check.py --check-files` CLEAR (this lane claims no runtime files). Caveat: raw scan output is not retained and the `origin/main` stamp decays — re-run the collision check at implementation-claim time. See `docs/ops/2026-07-25-legacy-mcp-tool-caller-inventory.md` §2, §11.
- [x] 2.2 Confirm PR #1561 remains limited to the separate legacy stdio server; exclude `tinyassets/mcp_server.py` from this change and never restore `tinyassets/directory_server.py` if the retirement change already removed it. — 2026-07-25 (PR state re-queried for inventory v2): #1561 `1a5d45af` OPEN draft, 3 files, stdio-only; `mcp_server.py` tool surface is disjoint from the six; `directory_server.py` already deleted by `60f7f9f1` (#1718). **Finding 2.2-A:** open PR #1467 still carries a `modified +30/-3` delta on `directory_server.py` and would restore it if merged unrebased. See inventory §3.
- [ ] 2.3 Inventory every repository import and direct Python caller of `universe`, `community_change_context`, `extensions`, `goals`, `gates`, and `wiki`; record a preserve-or-explicitly-migrate decision and focused coverage for each wrapper. — **REOPENED 2026-07-25** after an opposite-provider review returned `reject` on inventory v1. The **Python half is complete and independently reproduced** (57 imports/26 files + 119 module-attr references/41 files = the 176 *reference* count; 376 actual call expressions across 62 files; zero production-runtime Python callers; zero canonical→legacy delegation ⇒ PRESERVE the wrappers, confirming 4.1's default). The **wire half was materially wrong**: v1 claimed "6 wire callers" and "2 live scripts depend on `wiki action=list`"; the true figures are **42 literal wire sites across 10 files** (4 Python + **38 website JS/TS/Svelte across 8 files**, both deployable trees) plus a dynamic playground dispatcher and a registration-level test, and **5 wire sites + the public playground** depend on `wiki action=list`. v1's "176 … all under `tests/`" also omits the non-test `import *` at `fantasy_daemon/universe_server.py:2` and mislabelled 17 non-call introspection references as calls. Two further canonically-unreachable actions have live website callers (**2.3-D** `extensions action=stream_run`) and task 4.4's mirror rebuild breaks four packaged slash commands (**2.3-E**). **v3 2026-07-25** — a second opposite-provider review returned `adapt`, confirming every headline count and both new blockers, and requiring three corrections now folded: B2's 17 non-call references occupy **5** files not 4 (count 17 unchanged); `stream_run` is a **real implemented action** (`api/runs.py:924`, `_RUN_ACTIONS` `:1891`) so 2.3-D is *canonical unreachability*, not docstring-only; and 2.3-E's skill calls `read_premise` (not `get_premise`), **four** of five actions are unreachable (not three), and the breakage lands at **4.4** (mirror rebuild), not 4.1. New **2.3-F**: legacy tool names are load-bearing authorization state (`tinyassets/auth/provider.py:516-618`) — see task 4.2. Re-check only after the class E/F/G/K migration decisions in inventory §11 are recorded. See `docs/ops/2026-07-25-legacy-mcp-tool-caller-inventory.md` §4–§11.

## 3. Failing contract and execution proofs

- [ ] 3.1 Add a failing contract test that inspects `universe_server.mcp` with listing middleware bypassed and requires the registered tool set to equal exactly the seven canonical handles.
- [ ] 3.2 Add failing canary tests showing that either an extra handle or missing `get_status` produces exit code 4 and names the precise drift.
- [ ] 3.3 Add a failing authenticated in-process MCP test that runs a deterministic no-provider branch through registered `run_graph`, extracts its durable run identifier, and reads the same run back through MCP `read_graph(target="run")` without stubbing registration, run creation, or run read-back.
- [ ] 3.4 Mutation-check the three new proofs: reintroducing one hidden registration, permitting missing `get_status`, or bypassing durable run creation must make the corresponding test fail.

## 4. Live registration retirement

- [ ] 4.1 Remove exactly the six legacy `_register_structured_tool` registrations from `tinyassets/universe_server.py` while preserving wrapper functions unless task 2.3 proves and tests an explicit caller migration. — **Blocked on the binding preconditions in `docs/ops/2026-07-25-legacy-mcp-tool-caller-inventory.md` §11**: open PRs #1493/#1467/#1466/#1465 (all edit this file), the #1467 `directory_server.py` rebase, the three orphaned actions with live wire callers (`wiki action=list`, `community_change_context`, `extensions action=stream_run`), and migration of both website trees, the snapshot baker, the playground, and the four packaged plugin skills.
- [ ] 4.2 Remove `_DeprecatedToolVisibility`, the legacy-name set, and dead registration-only state; add no compatibility alias or alternate hidden dispatch path. — **Scope guard (Finding 2.3-F, inventory §4f):** the action-scope table at `tinyassets/auth/provider.py:516-618` is keyed by five legacy tool names (`universe`, `wiki`, `extensions`, `gates`, `goals`) and therefore *looks* like the "legacy-name set … dead registration-only state" this task removes. **It is not** — it is live authorization state carrying the write/costly/admin classification (including the money-write set), and the api-layer dispatchers still enforce against those names when reached through canonical handles (`api/universe.py:6235`, `api/wiki.py:2789`, `api/extensions.py:399`, `api/market.py:2595,3936`, `api/first_contact.py:50`). This task must **explicitly preserve it, or migrate it in lockstep with every `require_action_scope` call site, and state which it did**. Deleting it fails **open** on money-write actions.
- [ ] 4.3 Make the public canary compare against one exact seven-name set with required `get_status`; rename inaccurate internal `assert_five_handles*` identifiers without aliases while preserving the existing `--assert-handles` CLI.
- [ ] 4.4 Update the packaged runtime mirror and verify byte parity without restoring the retired `tinyassets/directory_server.py` surface.
- [ ] 4.5 Replace the canonical registered-tool metadata owner with the exact seven-row table; leave no retired-tool metadata residue.

## 5. Verification and review

- [ ] 5.1 Run the exact-registry, public-canary, authenticated MCP round-trip, structured-envelope, anonymous-challenge, and wrapper-caller focused suites on a clean current-main base.
- [ ] 5.2 Run plugin build/mirror parity, scoped Ruff, and strict validation of every active OpenSpec change.
- [ ] 5.3 Obtain an independent diff review that checks the exact six-name removal, absence of hidden dispatch alternatives, canonical behavior preservation, local-stdio boundaries, and continued absence of `/mcp-directory*`.
- [ ] 5.4 Run and freshness-stamp the applicable §14 concurrent-client/load proof against the seven-handle server; if the existing harness does not cover this MCP change, add bounded simultaneous `tools/list` plus run/read sessions and prove no registry drift, cross-session leakage, or duplicate run creation.

## 6. Deploy, acceptance, and spec sync

- [ ] 6.1 Deploy the reviewed build and run `scripts/mcp_public_canary.py --assert-handles` against `https://tinyassets.io/mcp`; record the exact seven-name result and deployed source SHA.
- [ ] 6.2 Complete final rendered-chatbot `ui-test` acceptance through the live connector and save the prompt/result plus trace or screenshot evidence.
- [ ] 6.3 Check for post-fix organic user evidence; if none exists, record that explicitly and leave a concise STATUS watch item instead of claiming proven clean use.
- [ ] 6.4 Verify rollback to the prior image is available; if rollback occurs, record that hidden registrations returned and reopen the retirement gate before claiming green.
- [ ] 6.5 After implementation and acceptance evidence land, sync this delta into `openspec/specs/live-mcp-connector-surface/spec.md`, archive the change, and retire its STATUS claim in the same landing lane.
