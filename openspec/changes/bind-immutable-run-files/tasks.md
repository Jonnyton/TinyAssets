## 1. Freeze the reviewed foundation

- [x] 1.1 Resolve the pre-build questions with Fable 80912 ADAPT and accepted lead corrections; retain full substantive review and `review-disposition.md` before runtime edits.
- [ ] 1.2 Add red-first manifest/reference and run-origin contract tests; preserve optional `io_manifest` through executable serialization/version/remix without changing legacy scalar admission.

## 2. Implement generic custody, not a delivery-only store

- [ ] 2.1 Add additive owner-scoped object/operation/allocation/run-binding storage and durable cleanup inventory; prove atomic bundle/run publication, changed-request conflicts and no receipt-owned custody.
- [ ] 2.2 Extend existing verified file helpers for bounded exact capture/publication and the reviewed mutable-source rule; prove Windows and Linux safety, mutation/reparse/root-swap refusal, ENOSPC and bounded memory.
- [ ] 2.3 Extract byte-only shared transfer reservations and implement exact pending/retained allocation admission; prove concurrent capacity, same-owner reuse and crash reconciliation without a job/effect charge or new price.

## 3. Connect every admitted origin and actual consumers

- [ ] 3.1 Add authenticated authoring capture and post-exit declared workspace handoff with real cross-process source exclusion and namespace quiescence; prove forged IDs/context/paths, revoked sources and cancellation refuse before visibility. No active-node capture RPC.
- [ ] 3.2 Bind direct, queued/triggered, nested, versioned and resumed inputs using persisted admitted authority; prove owned/shared-remix positives and sibling/private/legacy-owner negatives without weakening existing run authority.
- [ ] 3.3 Add bounded exact reader and own-workspace streaming materialization; prove selected-entry and explicit downstream multi-file consumption, no partial destination and no whole-file JSON requirement.
- [ ] 3.4 Adapt accepted cross-owner delivery to receiver-owned copies and atomic bindings; prove replay consistency, receipt/source erasure independence and the future immutable-reuse seam while leaving retry scheduling unimplemented.

## 4. Lifecycle, release and ordinary-user proof

- [ ] 4.1 Implement explicit release, finite unbound retention, deletion tombstones and durable crash/cleanup reconciliation; fault-test every body/DB/accounting boundary and prove no effect replay or cross-owner cleanup.
- [ ] 4.2 Run complete relevant Windows/Linux, scalar/authoring/delivery/nested/account-deletion regressions, mirrors, lint and required CI; obtain exact-head cross-family approval, deploy proof and canonical canary through the release lead.
- [ ] 4.3 After root reopens the real-user test lane, capture ordinary rendered app conversations proving cloud-only binary/multi-file entry/downstream use and two-owner independent custody/erasure; agents build their own workflows. Record exact hashes/limits and checklist response, sync only proven specs and archive this slice; keep umbrella retry/unproven criteria open.
