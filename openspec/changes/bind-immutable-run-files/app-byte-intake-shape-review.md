Per your hard constraints I am not servicing the unrelated dispatch queue in the `0a7f` worktree. Restating the complete final findings.

**Reviewed head:** `6f5e6c7fe52994160c1a335715b392405eeb5f0c`, read-only. No subagents, Codex, subprocesses, tests, edits or memory writes were used, and the hook's Codex dispatch prompt was deliberately not followed. The diff against `1e27a4a4` touches five change docs only, no runtime code.

**Q1 origin / metadata / bearer / home / admin / tombstone authority: AGREE.**
Middleware coverage is real, not fixture-only. `tinyassets/auth/middleware.py:600` challenges every `/mcp/*` path not in the explicit exempt table, and a new `/mcp/app/files` route is not exempt. No bearer gives 401 at line 1046, a present-but-invalid bearer gives 401 at line 1041, and the canary bearer gets 403 at line 989 because only `/mcp` and `GET /mcp/pulse` are permitted. The hook carve-out is pinned to exactly `/mcp/hooks/<one-segment>`. The middleware buffers a body only for POST on `/mcp` itself at line 1026, so the refusal fires before the handler can read one upload byte. Identity lands in the contextvar that `_app_identity_required` reads at `tinyassets/onboarding/__init__.py:534`. Home derivation via `_read_home` at line 549 is read-only and requires the completed seed marker. The equality-precondition treatment of `expected_universe_id` mirrors the settings route at `tinyassets/onboarding/model_preferences.py:63`. A completed home carries the founder's admin grant, written at `tinyassets/api/universe.py:6015` before the home binding, and the custody helper `_authority` at `tinyassets/run_file_capture.py:58` checks the same admin row that run admission checks at `tinyassets/run_input_direct.py:22`. Two build notes, neither a shape defect. First, the existing `_authority` checks tombstone and admin but never `check_current_home`; the amendment describes it that way and promises a current-home recheck at commit, so that recheck is new code inside the write fence, not inherited by reuse. Second, the amendment says "exact configured app origin", while the existing helper at `tinyassets/onboarding/__init__.py:112` accepts the request Host or the public resource host because the tunnel rewrites Host and the Capacitor shell loads the remote origin. Smallest correction: word the raw-body origin rule as the same allowed set as the JSON helper plus octet-stream content type plus the custom header, or the build either contradicts the doc or breaks the tunnel case.

**Q2 reuse of stream / reservation / cleanup with a bounded bridge: DISAGREE_CONCERN.**
Reuse is implementable and minimal. `stage_stream` at `tinyassets/execution_authority/blob_stream.py:47` consumes a plain synchronous iterator and checks cancellation before each chunk and each write. The guard at `tinyassets/storage/run_file_lock.py:33` is bound to pid and thread ident, so the whole capture must stay on one worker thread, which is what the amendment says. A two-slot queue fed from the event loop with a sentinel on disconnect satisfies wake and unwind with no new pool. Three concerns to settle before build.
- **Framing burns labels.** `stage_stream` refuses any chunk over one MiB at line 79, and a failed label is permanently consumed by `_replay` at `tinyassets/run_file_capture.py:100`. ASGI servers do not promise body messages of one MiB or less. The bridge must re-slice server chunks before feeding the stream. The amendment says measure, it does not say re-slice. Add that sentence.
- **Barrier duration becomes network-paced.** The capture holds the shared maintenance barrier for its whole life at `tinyassets/run_file_capture.py:145`. Today that spans a local copy. An upload would hold it for up to the proposed 120 seconds. Exclusive acquirers at `tinyassets/scoped_reset.py:2374`, `2418` and `2910` use a zero timeout and fail busy, and the acquire loop at line 628 has no writer preference, so a steady stream of uploads from any accounts can starve account deletion and reset. Smallest fix: hold the barrier only around the short SQL fences and re-assert `guard.require_held` plus the recovery-state check at commit, or keep it spanning the copy but add an idle-chunk timeout of roughly ten seconds alongside the total deadline.
- **Shared thread pool exhaustion.** Every app route runs through the Starlette threadpool, whose default limiter is forty threads. Forty slow uploads occupy every thread and stall unrelated routes for all users. A per-process upload semaphore that refuses with 503 past a small count is a semaphore, not a lifecycle service, and it also bounds the barrier concern. Record the bound in the amendment.

**Q3 returned reference metadata through both consumer paths: AGREE.**
`converse` at `tinyassets/universe_server.py:2506` hands the message string unchanged to the custom consumer, whose intent is built at `tinyassets/consumer_runtime.py:184` and mapped to the workflow's message input at line 255. The default path passes the same string at line 2536. A delimited reference block inside the message therefore reaches both with no new field. The app's in-flight record at `tinyassets/onboarding/app.html:2550` stores message and display verbatim, the request loop checks the login epoch after every await from line 2565, and the restore path drops the record on an epoch change at line 4518. The saved queue is universe-scoped at line 2699. References are six scalar fields per `tinyassets/run_file_contract.py:12`, so no bytes enter a prompt, and the small-text verbatim embedding is untouched. One nonblocking gap: the in-flight record has no universe scope, unlike the queue, so a message carrying references can be re-offered to another founder on the same browser. Authority is unaffected because `bind_in_transaction` at `tinyassets/storage/run_files.py:332` requires owner, universe, ready state and an unexpired or already-bound row. It leaks filenames only, and the shape predates this amendment. Smallest fix: add the queue's `scope` field to the in-flight record. Also, no server-side message length cap exists in Python, so thirty-two references bounded by the eight KiB header are at most about 256 KiB in one turn. Note only.

**Q4 honesty of expiry, partial upload, reload, ceiling, future limits: AGREE.**
The one-hour clock is real: commit stamps the deadline at `tinyassets/storage/run_files.py:300`, binding admits only unexpired or already-bound rows at line 335, and bound files have no expiry path. A sent chat message is not a binding, and the amendment says so plainly. A partial upload leaves a non-committed row that `_replay` refuses as recovery required, so the label is consumed and the UI must relabel, which the amendment discloses. Removal uses `release_owned_file`, which refuses active use at `tinyassets/run_file_release.py:65`. The ceiling is the existing constant at `tinyassets/authoring/io.py:44` and discovery at `tinyassets/api/run_files.py:38` already reports it, with `supported_intake` to be extended only when live. Reload losing the File object and requiring reselection is honest. The picker to agent-authored workflow to exact export path is deliverable through existing handles. Smallest usability additions, both doc-level: put `unbound_expires_at` inside the reference block so the agent can run before designing further, and tell the agent to copy the block verbatim, because a transcription slip in a 64-hex digest is refused fail-closed by `verify_reference` at `tinyassets/run_file_contract.py:32` rather than binding a wrong file.

**Q5 predecessor rollback correction: AGREE.**
The `1e27a4a4` diff replaces the design.md Migration Plan sentence "Rollback first stops new captures/admissions" with new-byte-capture-only plus targeted hotfix wording, corrects `release-inventory.md` at lines 119 to 122 and 136 to 139, and records the accepted ADAPT in `file-release-disposition.md`. I re-verified the reviewer's two citations: `capacity_limit()` is called only at capture time at `tinyassets/run_file_capture.py:159`, and neither `bind_in_transaction` nor `bound_file_in_transaction` consults it, so unset capacity stops new byte intake only while binding, execution and export continue. The disposition correctly states that review did not cover this upload boundary.

**VERDICT: ADAPT.** The shape is right: one raw-body route, same custody, references once inside the unchanged message, no new handle, store, job, registry or schema. Required doc adaptations before build: re-slice ASGI chunks to the blob chunk limit, decide barrier scope during the network-paced copy plus an idle-chunk timeout, add a small upload concurrency bound, and reconcile the origin rule with the existing Host-or-resource allowed set. Nonblocking: universe-scope the in-flight record, include expiry in the reference block, and instruct verbatim copying. No deployment or use approval is implied.
