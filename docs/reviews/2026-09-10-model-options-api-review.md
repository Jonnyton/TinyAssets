# Shared owned model catalogue: independent review

September10 2026. Claude peer76384 reviewed exact e8d69c3b against6edbf66c,
read-only, exit0 after428s: **APPROVE**, no required corrections. It independently
ran tests/test_model_options_api.py:17 passed7.05s, Windows Python3.14.
Full final response is output/model-options-api-review.md; this is the durable
verdict and disposition, not an assertion of live deployment.

AGREE: authenticated named owner/current-home/admin fences; exact serving lookup
and global race refusal; independent source freshness/revocation demotion;
credential-blind fields; full legacy native chain; complete structured catalogue;
no read activation; display-only guard; separate source/model failures; existing
read annotations preserved except truthful openWorldHint.

Five DISAGREE_CONCERN observations are non-gating and remain follow-ups:

1. Legacy HTTP without a manifest has no serving-source marker/default model in
   the catalogue, although legacy_single_provider is truthful. Do not label its
   empty order as a failed actual turn. A legacy HTTP inventory test is still needed.
2. Agent/assignment mismatch returns not_found rather than a repair catalogue.
3. Deposited/unadopted native and explicit-saved legacy refusal wording can be
   more precise; both safely exclude execution.
4. sources.reasons and source_failures duplicate source facts; consumer must not
   mistake them for independent failures or newly invented model rows.
5. Synchronous discovery has no single-flight; this was already disclosed.

The review did not cover the subsequently uncommitted app picker. That consumer,
its exact-home preference target fence and rendered behavior need their own
implementation review/proof. Native explicit discovery and all-universe controls
remain required; no approval here completes those capabilities.
