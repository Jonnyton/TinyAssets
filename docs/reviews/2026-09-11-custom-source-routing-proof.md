# Captured source routing — local verification, not deployed

September11 2026 UTC. Runtime614d1709071eaa4dbd1412b3c3bffa9a9fe9870d
against9ea3db96. Independent exact-commit APPROVE135s, terminal exit0, no required
corrections. No push or rollout.

Fresh discovery now supplies the same immutable contract to selected-model
validation, per-source ranking, actual request encoding, aggregate quantity
reservation, usage decoding and capacity fallback. Legacy snapshots/fixtures
retain existing registry fallback and legacy cost arithmetic. custom-http is
only a conservative scope, never a registry alias or remote account identity.

Atomic affordability receives a finite request output limit and preserves the
old per-token cost floor. A final reserved-cost check refuses over-budget inserts.
Configured-source candidates retain owner_filtered=false, server-derived
availability_basis and an explicit visible picker caveat. Metadata is not an
inference/spending grant or independently verified remote behavior.

13 new integration cases use authenticated publication, real scoped discovery
and inference broker, real writer/router/adapter/tool journal and synthetic
network/tool service. No provider registry mutation or fake discovery snapshot.
They prove actual reported model and unknown/exact usage, declared418 model-only
fallback without tool replay, conservative unknown capacity, paid refusal,
quantity capture/current-source recheck, eight-output aggregate reservation,
final inconsistent-affordability refusal and revoked source before tool dispatch.
Arithmetic tests explicitly substitute an internal selected value; they are not
proof of a live paid grant. A new JS/DOM picker case verifies the visible caveat.

WindowsPython3.14:398 passed41.28s, zero skips, one FastMCP deprecation warning.
Actual LinuxPython3.11.16/git2.47.3/bwrap0.12.0:398 passed27.42s, zero skips.
Same test group for both:

```text
python -m pytest -q tests/test_custom_source_execution.py tests/test_app_model_picker.py tests/test_interactive_http_agent.py tests/test_model_options_composition.py tests/test_model_options.py tests/test_model_policy.py tests/test_selected_model_authority.py tests/test_agent_protocol_capability.py tests/test_discovery_contract.py tests/test_discovery_snapshot.py tests/test_custom_discovery_publication.py tests/test_model_options_api.py tests/test_discovery_quantities.py --tb=short --show-capture=no -rs
```

Linux uses scripts/linux_oracle.py with the same pytest arguments under WSL Ubuntu,
GIT_DIR=/mnt/c/Users/Jonathan/Projects/TinyAssets/.git/worktrees/TinyAssets6,
GIT_WORK_TREE=/mnt/c/Users/Jonathan/.codex/worktrees/select-agent-models/TinyAssets.
An earlier398-pass Linux run noticed mirror regeneration during tar; it was
repeated above after files settled, without the archive warning.
Ruff on changed Python files passes. Plugin build/import423files passes;
precommit verifies10canonical mirror pairs, whitespace and path-resolver checks.

Reviewer independently reproduced13 integration cases5.94s on Windows with
PYTHONDONTWRITEBYTECODE=1 and `python -m pytest -p no:cacheprovider -q
tests/test_custom_source_execution.py`. Available-family fallback, not cross-family
evidence. The reviewer did not locate the primary limit receipt; lead reverified
it at C:/Users/Jonathan/.codex/worktrees/0a7f/TinyAssets/docs/reviews/2026-09-11-review-provider-limit.md
on08:07UTC, tracked by4042b933. It records04:41–04:42UTC terminal monthly-limit
API429/zero-token evidence and the permitted quality-gate fallback. No renewed
provider probes, account changes or review reruns were made.

Four earlier neutrality loci still fail check_channel_agnostic.py; no suppression.
Public authoring/schema discovery, legacy full-price migration, native explicit
models/all-universe controls and live acceptance remain open. Fresh08:06UTC PR3832
is draft/open remotebd93e239, auto-merge null. Existing live app still ends with
Sep10 21:31PDT original checklist allpassed; approved CI-log302 blocker remains
separate. No new prompt, approval, workflow edit or feedback-PR takeover.
