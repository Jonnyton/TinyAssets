# App model controls: local consumer proof

September10 2026, feature worktree after e8d69c3b. Built the actual native dialog,
composer model button, current override, saved default and fallback move/remove
controls. Actual answer identity is separate from configured/next-message choice;
no reported model is invented. All catalogue choices remain visible with reasons.
Refresh failures/expiry label retained rows stale and disable new changes. Saves
use the existing CAS route and an explicit expected-home query fence. Ambiguous
saves are not replayed. Sign-out/home changes fence late responses and clear the
transient choice; already captured queued/retried/spoken choices are untouched.

No picker operation grants access. Existing accepted manifest models can be
selected; legacy/unpowered Automatic can be saved. Expanded model-access setup
for unaccepted sources is still missing from the UI, so this is NOT completion
of task3.1 or a ready-to-deploy claim. Explicit native discovery, current-attempt
telemetry, persisted answer receipts and non-home controls also remain open.

Twenty executed Node cases use the actual picker object with synthetic DOM and
transports, including delayed-save JSON after another login. Two new actual route
cases cover explicit target mismatch after home rebind and unchanged CAS. The
final37-file group also exercises real policy/writer/router/HTTP/MCP composition:

```text
python -m pytest -q tests/test_agent_inference.py tests/test_interactive_http_agent.py tests/test_selected_model_authority.py tests/test_discovery_snapshot.py tests/test_api_key_http_provider.py tests/test_http_inference_lifecycle.py tests/test_writer_execution_receipt.py tests/test_universe_intelligence.py tests/test_agent_turn_journal.py tests/test_agent_chat_codec.py tests/test_agent_chat_portable_history.py tests/test_agent_price_guard.py tests/test_mirror_parity_gate.py tests/test_model_capacity.py tests/test_model_policy.py tests/test_provider_router_diagnostics.py tests/test_provider_served_router.py tests/test_native_model_authority.py tests/test_model_preferences.py tests/test_model_preference_store.py tests/test_onboarding_model_preferences.py tests/test_model_capacity_transport.py tests/test_served_model_preferences.py tests/test_provider_serving_binding.py tests/test_provider_assignment_manifest.py tests/test_converse_handle.py tests/test_universe_server_mcp_structured_results.py tests/test_model_options.py tests/test_model_options_composition.py tests/test_compute_connection.py tests/test_discovery_http.py tests/test_model_catalogue_collection.py tests/test_model_options_api.py tests/test_app_model_choice.py tests/test_app_model_picker.py tests/test_onboarding_app.py tests/test_brand_parity.py --tb=short --show-capture=no -rs
1053 passed, 3 skipped, 7 warnings in97.51s (Windows Python3.14)
```

Actual Docker Linux oracle, same37files/flags, Python3.11.16/git2.47.3/bwrap0.12.0:
1055 passed,1skipped,1warning in87.80s. Linux skip is unconfigured real Codex account;
Windows additionally skips POSIX shared admission and bwrap, covered on Linux.
The known scoped-reset inventory failures remain excluded and unresolved.
Ruff,419mirrors/import, regenerated app checksum and diff checks pass.

Rendered local check: python output/preview_model_picker.py, September10 near
19:33UTC. Real Chromium at390px and1280px, real app markup/script with only boot
replaced by an explicitly synthetic catalogue; all network calls disabled.
Selection, fallback add/reorder, post-move keyboard focus, use-current, Escape
and focus return passed with no script errors. Screenshots
output/model-picker-390.png and output/model-picker-1280.png were inspected;
the dialog scrolls on both heights with readable wrapping. This is layout/input
proof, NOT live account, provider, cross-client connector or organic-user proof.
The initial preview used an opaque about:blank origin and a malformed synthetic
arrow expression; fixture origin and expression were corrected without changing
app runtime code. No production credentials were used.

The preceding shared API review independently APPROVE428s. This new consumer
still requires independent exact implementation review, access opt-in completion,
both rendered MCP clients, CI, deployment, protected SHA/canary and the ordinary
app retest. The real app's new approved HTTP302 download blocker is separately
recorded in the primary goal/concern; its feedback PR remains agent-owned.
