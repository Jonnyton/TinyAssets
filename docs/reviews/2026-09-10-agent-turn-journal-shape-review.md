Checked. Both FINISHED files are already folded into durable state at the reviewed SHA: the protocol-shape ADAPT is recorded in `agent-chat-protocol.md` (the "Applied shape review ADAPT300s" section) and the codec-implementation APPROVE in `tasks.md` under task 2.4. The three vanished entries belong to the answering-model release that `tasks.md` records as merged and deployed. This session is the running `agent-turn-journal-shape-result.md` dispatch itself and is bound to read-only, no-dispatch constraints, so there is nothing further it may advance. Full verdict restated below so the final output stands alone.

Reviewed SHA: `8480ec1004467a71f92d43c0aae4500dea87bc20`, `openspec/changes/select-agent-models/agent-turn-journal.md`. Read-only review, no subagents, no tests run.

**Overall:** the shape is right. Three tables are the minimum that preserves a per-call `started` CAS row, a per-inference reservation provenance row, and one turn frontier. The journal grants nothing, mints nothing, caps nothing. Four concrete contradictions with cited code must be fixed in the contract before code, plus two record/operation gaps.

## Required fixes (gating)

1. **Non-text results cannot reach storage through the codec.** DISAGREE_EVIDENCE. The proposal says to use existing codec records and also to retain image/audio/resource blocks. The only codec path to a stored result is `tool_outcome`, whose projection raises on any non-text block at `tinyassets/providers/agent_chat_codec.py:276`. Fix: the finish-tool operation takes the raw MCP call result and stores the journal's own projection. Blocks are dumped with the MCP model excluding only the meta field and validated against the MCP content-block union. Storage computes a `content_kind` column (`text_only` or `non_text`) from the stored JSON, never from the caller. The codec's text projection runs on the read side only, so a `non_text` row holds the turn as `held_unsupported_result` with the result intact.

2. **Idempotent finalization contradicts the generation CAS as written.** DISAGREE_CONCERN. A retried finalize after its own commit always sees a moved generation, so it can never be idempotent under "mismatch returns conflict". Fix: finish-inference and finish-tool return one of `applied`, `already_applied`, `conflict`. `already_applied` is returned only when the target row is already terminal with byte-identical call id, result JSON, error flag and outcome. It mutates nothing, leaves the generation untouched, and is evaluated before the generation check. Any other mismatch or differing payload is `conflict`.

3. **Account deletion will fail on the proposed foreign keys.** DISAGREE_EVIDENCE. The deletion plan issues one DELETE per table in sorted name order at `tinyassets/account_deletion.py:276` and `tinyassets/account_deletion.py:514`. That order is rounds, then tools, then turns, and rounds is the parent of tools. With foreign keys on, as the shared connection sets at `tinyassets/storage/provider_work_authority.py:1505`, the rounds delete raises and the root-rows phase is recorded as failed at `tinyassets/account_deletion.py:866`. Fix: both child FKs declare `ON DELETE CASCADE`, every child table carries its own owner and universe columns, and all three tables are added to the person-keyed exception map at `tinyassets/account_deletion.py:112` so former-home rows go with the owner. The coverage test at `tests/test_account_deletion.py:642` passes automatically once a universe column exists, so add a test that writes owner A in universe B and asserts A's deletion removes it.

4. **The tables are not inert for the operator reset.** DISAGREE_EVIDENCE. Any main-database table missing from the classification map makes scoped reset raise at `tinyassets/scoped_reset.py:1294`. Creating the three tables hard-blocks every operator reset until classified. Fix: classify all three in `MAIN_DB_TABLE_CLASSIFICATIONS`, with exact column facts in the resettable-schema map if they reset by universe, in the same commit, with the reset-scope test updated.

5. **Schema creation must be an explicit non-committing operation.** The shared connection creates its own schema on every open at `tinyassets/storage/provider_work_authority.py:1511`, but the journal is not in that script. Follow the served-budget precedent at `tinyassets/provider_assignment.py:151`, called before BEGIN IMMEDIATE. The journal's ensure-schema must use per-statement execute, never `executescript` (which implicitly commits an open transaction), and must raise if called inside a transaction.

6. **Complete the state tables and provenance columns.** Tool row states: `planned`, `started`, `completed`, `not_sent`, `unknown`; the last two are terminal and each holds the turn in a named state. The client's error carries `not_sent` only pre-dispatch and `unknown` after the call at `tinyassets/engine_tool_client.py:159`; a cancellation re-raises with no outcome and maps to `unknown`. Turn states: `ready`, `inference_started`, `tools_pending`, `completed`, `held_refusal`, `held_truncated`, `held_filter`, `held_unknown_stop`, `held_transport`, `held_tool_not_sent`, `held_tool_unknown`, `held_unsupported_result`, `corrupt`. Round provenance must include `binding_id` alongside reservation id, binding generation and digest. The reservation record carries id and generation at `tinyassets/provider_assignment.py:105`; the digest comes from the served authority record. Usage columns stay nullable and are never backfilled from the reservation estimate.

## Nongating integration notes

- **Three tables: AGREE.** Folding tools into a round JSON column loses the row-level CAS that step 4 needs before each dispatch.
- **No FK to budget rows: AGREE.** Settled-history pruning deletes reservation rows at `tinyassets/provider_assignment.py:259`; provenance copied by value survives it.
- **Ordinal primary identity, no unique call id: AGREE.** The request builder already refuses a call id reused across rounds at `tinyassets/providers/agent_chat_codec.py:340`; storage must not add a second definition of that rule.
- **Reply snapshot needs a serializer.** The reply record has no document form. Add a versioned snapshot and loader in the journal that re-validates via the argument parser and the continuation JSON. Keep the approved codec unchanged, or add a pure pair there under its own small review.
- **Single active round:** add a partial unique index on turn id where state is `inference_started` as a belt to the CAS.
- **Foreign-owner rows block deletion** through the ownership keys at `tinyassets/account_deletion.py:196`, exactly as for the model-preferences table. Existing policy, not new.
- Use the store's Z-suffixed timestamp helper so one format exists, as `tinyassets/storage/model_preferences.py:141` does.

VERDICT: ADAPT
