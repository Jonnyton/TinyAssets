# Request-local answering-model receipt verification

September10,2026 UTC, working tree over95f610a0. No deployment or picker claim.

Independent Claude shape review12667 APPROVE231s also inspected internal
observer/collector code. Public integration followed that approved optional
envelope shape. Existing public input/decorator and direct str returns unchanged.
Full verdict in interactive-model-receipt-review.md; no whole-feature exact-head
review or cross-client rendered acceptance is implied.

The bridge captures the completed writer response on the caller's thread before
text reduction. It never observes extract_learning, uses no shared last-call
state, and swallows observer failures without discarding an earned answer or
retrying inference. ProviderResponse.reported_model is separate from legacy
requested/default labels; HTTP supplies validated evidence, other adapters
remain unknown until their structured reporting is implemented.

Successful converse JSON carries optional execution provider/model/model_status.
Held/error paths omit it, and requests without evidence cannot inherit a prior
receipt. The projection contains only bounded printable labels. No authority,
grant, preference, storage schema, credentials or tool-loop changes.

Final Windows Python3.14:252passed,0skipped in17.07s (10558 exit0). Three existing
FastMCP asyncio deprecation warnings. Actual Docker Linux Python3.11.16,
Git2.47.3,bwrap0.12.0:252passed,0skipped in10.94s (41821 exit0).

Command:

`python -m pytest -q tests/test_writer_execution_receipt.py tests/test_universe_intelligence.py tests/test_api_key_http_provider.py tests/test_converse_handle.py tests/test_providers.py tests/test_selected_model_authority.py tests/test_mirror_parity_gate.py tests/test_universe_server_mcp_structured_results.py tests/test_onboarding_app.py::test_optional_execution_receipt_does_not_change_reply_delivery --tb=short -rs`

The same arguments ran via scripts/linux_oracle.py in native Ubuntu Docker with
the process-local translated GIT_DIR/GIT_WORK_TREE documented in the prior
linux-oracle-wsl-proof.404-file runtime mirror/import, Ruff and diff checks pass.

Coverage includes interleaved requests, first-write receipt, malformed/absent
reported model, legacy positional constructors, failed/mock/degraded paths,
observer failure without retry, actual writer-versus-learning wiring, direct
converse JSON and both MCP response channels, plus real app JavaScript receiving
the optional field without duplicate send or losing the reply. All provider
traffic is synthetic. App JS compatibility is not a completed model control.

Owned Chrome-extension app conversation was re-read at00:01 UTC and remained at
Sept9,16:43 PDT export completion. No new prompt, workflow edit or approval.
This new local receipt bridge has no rendered live acceptance yet. UI source-name
mapping, active/attempting states, CLI model evidence, choice/default/fallback
controls and full HTTP tool execution remain in the unchanged completion scope.
