# Scoped model-capacity continuation: local proof

September 10, 2026. Feature worktree codex/select-agent-models,
implementation based on e5d93e84. Not deployed or publicly activated.

The real writer/router/HTTP adapter/journal/engine-client composition now consumes
an internal advisory model plan. Confirmed pre-generation capacity refusals carry
typed scope, release unspent reservations, and select the next eligible candidate
through the existing policy kernel. Each attempt rechecks current authority.
Completed tool results stay in the same journal and are not replayed. Unknown
transport or tool effects hold. Account/unknown scope excludes sibling models;
model-only refusal does not cool the entire provider. Explicit empty tails remain
empty. Authentication, malformed responses and generic server errors do not cycle.

Pre-build independent review1878 finished in341s: correction8cb8d991 APPROVE;
capacity shape ADAPT. All three required shape adjustments were incorporated
before code (reservation settlement, dedicated scope-aware catch, conservative
unknown-to-account fold). This is not an implementation approval of the new code.

Verification command (same arguments on Windows and scripts/linux_oracle.py):

```text
python -m pytest -q tests/test_agent_inference.py tests/test_interactive_http_agent.py tests/test_selected_model_authority.py tests/test_discovery_snapshot.py tests/test_api_key_http_provider.py tests/test_http_inference_lifecycle.py tests/test_writer_execution_receipt.py tests/test_universe_intelligence.py tests/test_agent_turn_journal.py tests/test_agent_chat_codec.py tests/test_agent_chat_portable_history.py tests/test_agent_price_guard.py tests/test_mirror_parity_gate.py tests/test_model_capacity.py tests/test_model_policy.py tests/test_provider_router_diagnostics.py --tb=short -rs
```

- Windows:515 passed, zero skips,52.15s.
- Actual WSL Docker Linux:515 passed, zero skips,34.04s; image
  tinyassets-linux-oracle:ce0e83fb15a8, Python3.11.16/git2.47.3/bubblewrap0.12.0.
- Focused composition/capacity:52 passed14.83s.
- Ruff changed runtime/tests passed. Plugin generator mirrored416 files and
  import probe passed; mirror parity is included above.

Synthetic network seams exercise revoked access before replacement, newly paid
replacement exclusion, free-only bounds on every POST, cross-owner plan refusal,
known-result continuation, shared limits, and no replay after uncertain outcomes.
No actual provider calls, remote tool effects or real CLI integration ran here.

Separate known failure, excluded from this group and NOT resolved: the two new
test_agent_turn_reset_integration cases stop at the existing five-table scoped
reset inventory mismatch. See concern2026-09-10-scoped-reset-inventory-misses-provider-state.
No live reset or classification whitelist change was performed.

Still required: exact implementation review, public current/saved policy
consumption, native/subscription model support, clickable controls, CI, deploy,
authenticated SHA/canary, and ordinary rendered app acceptance.

## Supplemental legacy and serving-authority coverage

Exact runtime964c58f2, September10 2026. Same nine-file command documented in
interactive-http-agent-proof.md under Supplemental legacy/authority regression
group: Windows173 passed/3 skipped21.57s; actual Linux175 passed/1 skipped8.62s.
Linux covers shared-reader concurrency and bubblewrap skipped by Windows.
The separately configured real-Codex integration remains skipped on both; no
claim that a real CLI turn ran. No canonical runtime changed during review.
