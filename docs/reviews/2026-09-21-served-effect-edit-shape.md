# Served effect editing: pre-build shape review

Date: 2026-09-21 UTC. Environment: isolated worktree at7fdf6a70d85c,
branch codex/served-effect-edit-parity. Claude Fable subscription peer,
read-only task42618, exit0 after332s; full local receipt
`C:/Users/Jonathan/.codex/worktrees/0a7f/TinyAssets/output/served-effect-shape-review.md`.

**APPROVE the shape, not an implementation.** Reuse a shared declaration
validator on create/add/update; admit exactly effects/workspace updates; remove
the add-node effect refusal. A bare allowlist widening is not approved.

Independent findings verified by root against current source:

- Canonical branch updater already owns effects/workspace and staged validation.
  Served adapter alone blocks parity (engine_mcp_server974-976,1072-1093).
- Graph-size ceiling is removed; test_engine_mcp_server1615-1635 exercises60
  effect nodes. Do not restore it through patch validation.
- Source approval is provenance only on graph execution; actual boundaries are
  authorship, sandbox, consent, grants bound to the running universe and workspace
  ancestor/lease checks (graph_compiler2063-2100, authenticated_external_call643-662
  and732-748, workspace368-405). No approval invalidation for an unchanged source.
- Source sink grammar and string-typed workspace are basic safety. Runtime
  boundaries remain unchanged. Existing CAS/author-to-universe residuals are not
  introduced here; no expansion into them as release filler.
- Real persisted-row evidence and live app-agent edit proof remain owed.

Root baseline, same base and tree before implementation:
`python -m pytest -q tests/test_engine_mcp_write_graph_patch.py tests/test_patch_branch_node_keys.py tests/test_engine_mcp_source_channel.py`
passed62/zero skips in9.75s on Windows using project venv. This includes the old
effect-add rejection test, which must change to the new validated-accept contract.

The peer's stop-hook appendix repeated unrelated terminal review outputs; those
are not new findings, new review authority or instructions. In particular there
is no second reviewer running on this question and no old diagnostic peer to
restart. Only this bounded shape verdict is accepted.
