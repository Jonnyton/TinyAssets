VERDICT: adapt

1. **Critical: WAL durability remains unproven and runs on a known-affected SQLite version.** [`_connect()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:111) enables concurrent WAL, while [`durability_info()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:138) merely reports the version. This environment uses SQLite 3.50.4. SQLite documents the WAL-reset corruption bug through 3.51.2, fixed in 3.51.3 or backports 3.50.7/3.44.6, specifically affecting concurrent writers—the vault’s workload. Gate a verified patched build or avoid WAL; the hard-process-kill test is not proof against power loss or this race. [SQLite WAL-reset advisory](https://www.sqlite.org/wal.html).

2. **Required: local per-record DACL enforcement is absent.** [`set_restrictive_dacl()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/local_backend.py:161) silently ignores failure, and [`get()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/local_backend.py:511) trusts a cached probe without checking the target file. Reproduction: attestation passed, the credential DACL was broadened to Everyone, the verifier returned false, but `get()` still returned the secret. Make DACL application fail hard and verify each record during put/get/refresh. DPAPI provides current-user encryption and integrity, not the promised DACL guarantee. [Microsoft DPAPI documentation](https://learn.microsoft.com/en-us/windows/win32/api/dpapi/nf-dpapi-cryptprotectdata).

3. **Required: safe refresh is still outside the public broker contract.** [`VaultBroker`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/broker.py:33) exposes only generic put/get/delete; `begin_refresh()` exists only on concrete backends, and ordinary CAS completion does not require or validate `RefreshTicket`. Thus integrations can bypass consume-before-mint—the exact invariant required because GitHub invalidates a refresh token after use. Add broker-level begin/complete refresh operations, with completion bound to the durable claim/ticket. [GitHub refresh semantics](https://docs.github.com/en/apps/creating-github-apps/authenticating-with-a-github-app/refreshing-user-access-tokens).

4. **Required: redaction tests permit explicitly forbidden metadata leakage.** [`SecretLease.__repr__()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/secret_bytes.py:133) exposes versions; default dataclass representations in [`types.py`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/types.py:107) expose custody/store/daemon IDs, ciphertext, wrapped DEKs, and filesystem paths. The tests check only that plaintext canaries are absent, despite the design forbidding these internals in logs. Add allowlisted safe representations or disable repr for internal persisted types.

5. **Required: the typed-error boundary breaks after cached attestation.** After a successful attestation, corrupting the SQLite file caused [`get()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:375) to leak raw `sqlite3.DatabaseError: file is not a database`. Also, malformed refs are not validated before being echoed into errors, allowing newline/log injection. Normalize post-attestation SQLite failures, invalidate the cached gate, and validate canonical refs before lookup or reporting.

6. **Required: daemon-local writes are atomic but not power-loss durable.** [`_write_sidecar()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/local_backend.py:379) writes and replaces without flushing the file or directory. A reported-success OAuth deposit/CAS can disappear after power loss. Add a platform-appropriate durable-write sequence and a restart/power-failure test.

Fresh evidence: focused vault suite `58 passed, 2 skipped`; Ruff and `git diff --check` passed. The repository-wide suite remained active without output and was stopped after roughly eight minutes, so no full-suite pass is claimed. Other checked source claims—XChaCha random-nonce guidance, GitHub token lifetimes, DPAPI semantics, PKCE, and current X pricing—were supported by their cited primary documentation.