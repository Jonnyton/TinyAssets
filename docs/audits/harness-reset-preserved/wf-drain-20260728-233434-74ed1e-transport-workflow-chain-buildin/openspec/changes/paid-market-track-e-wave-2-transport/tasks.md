## 1. Review Gate and Baseline

- [x] 1.1 Obtain opposite-provider review of this proposal/design, the complete realtime request/bid/match/claim/delivery plus transaction `paid-market-economy` delta, the quote→bid composition amendments in `paid-market-live-price-discovery`, and both task lists. Claude Sonnet approved the corrected contract on 2026-07-22; verdict and resolved blockers are recorded in `docs/audits/2026-07-22-paid-market-full-workflow-opposite-provider-review.md`.
- [x] 1.2 Run and record the pre-change baselines: `python -m pytest tests/test_paid_market_core.py tests/test_match_scale.py -q --noconftest`, `openspec validate --specs --strict`, and `openspec validate paid-market-track-e-wave-2-transport --strict`. Baseline 2026-07-24 PT on Windows: 180 tests passed in 2.15s; 26 specs passed strict validation; this change passed strict validation.

## 2. Fixture Replay and Production Baseline Gate

- [x] 2.1 Write failing fixture tests in `tests/test_paid_market_migrations.py` for unique/gap-free IDs, exact-byte SHA-256 drift, fresh application, verified populated-fixture baselining, failure rollback/resume, bounded advisory-lock timeout, two concurrent serialized runners, migration-role-only history DML, and prior-app compatibility after populated upgrade. RED began with the missing runner; GREEN 2026-07-24 PT: 12 PostgreSQL-backed runner tests passed inside the final 227-test gate, including full-catalog baseline drift, atomic recovery, alternate-runner ACL normalization, and fresh concurrent runners.
- [x] 2.2 Add a local runner under `prototype/full-platform-v0/` with fixture-scoped psycopg dependencies and `schema_migrations(version, name, sha256, applied_at)`; make each SQL file plus history row one transaction and keep public/application roles unable to mutate history. The runner uses a bounded session advisory lock, exact-byte hashes, per-file transactions, a non-login history owner, and fail-closed drift/baseline checks.
- [x] 2.3 Renumber and dependency-correct the v0 files in place as `001_core_tables`, `002_flags`, `003_rls`, `004_indexes`, `005_seed`, `006_discover_nodes`, `007_token_normalization`, `008_forwards`, `009_market_ledger`; ensure discovery establishes/checks pgvector and forwards adds a monotonic offer `version`. Do not copy this SQL into a production migration home. The corrected discovery migration also supplies the schema/RLS fields its existing RPC and tests require.
- [x] 2.4 Update the prototype compose, tests, and README to use only its fixture runner; prove fresh install and fixture upgrade use the same path and the gateway does not start before migration success. Evidence 2026-07-24 PT: fresh Compose migration exited 0; PostgreSQL and gateway were healthy; all 18 prototype tests passed in 3.46s, with RLS assertions running as the real fixture-app role; replay exited cleanly.
- [ ] 2.5 Produce a read-only inventory of the actual deployed Supabase schemas, extensions, auth, policies, functions, roles, vector dimensions/indexes, migration history, and deployment mechanism. Stop for host approval of the production baseline and migration home before authoring separately reviewed production-native SQL. **BLOCKED 2026-07-24:** requires host-provided live Supabase access and an explicit production-baseline/migration-home decision; fixture credentials were not treated as live authority.
- [ ] 2.6 After approval, author production-native migrations from the recorded baseline; add exact dry-run, role/policy, populated-upgrade, rollback/forward-fix, checksum/history, and prior-app compatibility tests; obtain independent migration/security review while keeping live application explicitly unapproved. **BLOCKED 2026-07-24:** depends on 2.5 and host approval; only fixture-native 001–009 work proceeded.

## 3. Pure Spot Adapter and Ledger SQL Boundary

- [x] 3.1 Add failing unit tests in `tests/test_paid_market_core.py` for a pure spot-settlement posting adapter, exact conservation, integer-only inputs, and positive/negative `Ledger.assert_drained` behavior. RED evidence 2026-07-24 PT: the focused test failed because the adapter contract was absent.
- [x] 3.2 Implement only the missing pure spot adapter in `tinyassets/paid_market/ledger.py`, export it through `tinyassets/paid_market/__init__.py`, and keep the package free of I/O/environment/database imports. GREEN evidence 2026-07-24 PT: 187 paid-core/matcher tests passed; focused ruff clean.
- [x] 3.3 Add failing fixture PostgreSQL tests in `tests/test_paid_market_postgres.py` for role/DML/RPC denial, fixed-search-path resistance, non-login ownership, exact account provenance, posting/key/memo/account/body bounds, 100 concurrent identical replays, and same-key/different-body conflict. RED began with the missing bounded RPC/role; GREEN 2026-07-24 PT: 8 PostgreSQL boundary tests passed, including NULL/empty private-primitive rejection, 100 simultaneous replay callers, stale-snapshot safety, and Python-to-SQL canonical-body parity.
- [x] 3.4 Harden fixture `009_market_ledger.sql`: store `request_sha256`, explicitly revoke function/table/sequence privileges from public/user-facing roles, grant only a dedicated internal settlement role, fix the trusted search path, validate Wave 2 bounds, reject `external:*`/`pool:*` and caller-supplied treasury accounts, and make raw apply/drain helpers internal. Repeat only in later production-native SQL authored from the approved live baseline. The fixture now exposes only `apply_settlement(bytea,text)` to the settlement role and recomputes SHA-256 over exact canonical bytes.
- [x] 3.5 Add one internal logical-accounting wrapper that atomically performs business-state/version CAS, actor/account authorization, adapter postings, `market.apply_tx`, every required logical reservation/collateral drain assertion, and audit-state commit; prove any database failure rolls back all effects and that success never serves as wallet-funding or chain-effect authority. GREEN 2026-07-25 PT after Opus 5 fold: migration 013 revokes raw `market.apply_settlement` execution from `tinyassets_fixture_settlement`; live PostgreSQL privilege and invocation tests prove that role can call only the bounded wrapper while its non-login definer alone can call the raw function. Removing the revoke made both source and live-role mutation probes fail. Existing amount/cap/version/authority/drain/rollback and fee-on-every-settlement proofs remain green.
- [x] 3.6 Differential-test randomized persistent transactions against pure `Ledger`, including overdrafts, repeated accounts, reverse-order first-touch accounts, residual logical reservation accounts, identical replays, changed-body conflicts, and the canonical fee on every settlement including same-owner and external-supply settlement. GREEN 2026-07-25 PT: randomized persistent fixture transactions were compared account-for-account with pure `Ledger`; residual escrows, replay/conflict behavior, and a positive treasury fee on same-owner, native, and external-supply paths were verified.
- [ ] 3.7 Add adversarial tests for verified-request tenant derivation, mixed-tenant rejection, signed/bounded/revoked on-behalf grants, server-recomputed canonical hashes, hash mismatch, composite tenant keys, duplicate-account coalescing, and the global cross-family lock order. Prove exact actor equality, linked-but-not-identical actors, caller fields, grants, and external routes cannot remove the fee from a settlement, and database accounting rows alone confer no wallet or chain authority. **PARTIAL 2026-07-25:** fixture tests cover verified tenant/actor derivation, mixed-tenant and forged/revoked/bounded grants, server hashes, exact bid-version binding, fee invariance, composite keys, and account coalescing. A production-shaped cross-family concurrency/deadlock proof is not available, so this remains unchecked.

## 4. Realtime Inbox, Bids, Atomic Claim, Delivery, and Dark Transport

**Premise verification 2026-07-25:** the delta spec itself fully enumerates the non-delivery lifecycle and released the circular 4.1 gate. Landed quote/capacity and `best_execution` types released 4.5/4.6. This lane implements the dark request/bid/match/claim/realtime spine and fixture migration 013; delivery/acceptance/dispute remain gated on exact `lease_fence` and `accepted_result_sha256` authority and are intentionally absent.

- [ ] 4.1 Add failing lifecycle tests in `tests/test_paid_market_workflow.py` for body-bound idempotent submission, changed-body conflict, every allowed/forbidden state edge, actor/grant authority, cancellation/claim races, bounded fan-out slots, append-only history, and delegation from the existing canonical API routers with no standalone advertised MCP action. **PARTIAL 2026-07-29:** the dark core proves idempotency, graph enforcement, authority, cancellation/claim races, bounded slots, and append-only history. The typed, injected `tinyassets/api/market_workflow.py` boundary now delegates every pre-delivery command unchanged, rejects action/type/clock mismatches, exposes no delivery or settlement action, and remains unregistered/default-dark; 10 focused routing tests and the 41-test workflow/bid/claim/routing gate passed on Windows. Wiring authenticated canonical routers and delivery-fenced later transitions still depends on the activation and external fence/hash owners, so this remains unchecked.
- [ ] 4.2 Define immutable request/bid/match/claim/delivery commands and typed applied/replayed/conflict/contention results in `tinyassets/payments/market_workflow.py`; define injected `MarketRequestStore`, `MarketRealtimeBus`, `MarketDeliveryStore`, and `MarketLedgerRpc` protocols while keeping API modules delegation-only and application core free of psycopg/Supabase imports. **PARTIAL 2026-07-25:** immutable non-delivery commands/results and the four injection boundaries are defined without database imports. A delivery command would require the externally owned fence/hash authority and was not stubbed, so this remains unchecked.
- [ ] 4.3 Add prototype/integration-only PostgreSQL adapters and `prototype/full-platform-v0/migrations/013_paid_market_workflow.sql` plus `tests/test_paid_market_workflow_postgres.py` for versioned request, bid, match, fan-out-slot, transition-event, durable per-shard outbox, delivery-receipt, dispute, and acceptance rows. Prove direct DML denial, fixed-search-path/non-login-owner command functions, independent locked-row authority rechecks, RLS-scoped least-privilege reads, hostile-search-path resistance, and exact constraints; defer production-native SQL and clients until the Supabase baseline and migration home are approved. **PARTIAL 2026-07-25:** fixture migration 013 and PostgreSQL tests cover the request/bid/match/slot/event/outbox spine, command ownership, authority rechecks, RLS, hostile paths, replay, and exact constraints. Delivery receipt, dispute, and acceptance rows are deliberately absent pending the external fence/hash owner, so this remains unchecked.
- [x] 4.4 Add failing `tests/test_paid_market_realtime.py` coverage for same-transaction outbox append, post-commit capability announcement, privacy-minimal envelopes, eligibility-bound subscription/detail fetch, subscribe-and-buffer → repeatable-read snapshot/watermark → durable outbox catch-up → live-tail ordering, compacted-watermark fresh-snapshot fallback, duplicate/out-of-order frames, superseded-version coalescing, configured positive page/retry/subscription bounds, per-principal/shard fairness, degraded freshness, and prohibition of native-replay reliance or global inbox polling; implement the injected notification adapter in `tinyassets/payments/market_realtime.py`. GREEN 2026-07-25 PT: bounded multi-page catch-up reaches the captured head or refuses live-tail entry on exhaustion; cancellation tombstone invalidation, original-result replay, and all listed dark-adapter ordering, fallback, privacy, bounds, fairness, and degraded-freshness cases pass without Supabase imports or global polling.
- [x] 4.5 Add failing `tests/test_paid_market_bids.py` coverage for host/grant authority, immutable versions, quote-id/version/digest binding, one persistent bid→one pure `BookOffer` mapping, replacement/cancellation/expiry/revocation, capacity fences, deterministic equal-bid tie-break, exact pure-oracle snapshot and rejection receipt, cross-lane discovery versus intra-request allocation boundaries, and no hidden maintainer/provider/payment authority; persist the immutable match decision before claim. GREEN 2026-07-25 PT after Opus 5 fold: before the window only requester authority may record an early-close match; afterward eligible hosts may invoke the deterministic matcher only within the requester spend cap; no match records at/after deadline. The 999,000× price repro is refused. Removing cap or time guards makes the focused mutation probes fail.
- [x] 4.6 Add failing `tests/test_paid_market_claims.py` integration coverage for single-request and bounded fan-out narrow claims plus multi-bid matching: call `best_execution` over bid-derived `BookOffer` values, lock request/slot/selected bid IDs in canonical order, compare recorded monotonic versions, reject stale selections atomically, retry at most three times with jitter, and return honest insufficient-supply/contention results; implement without cross-lane reranking, greedy matching, partial claims, poll-all, or changes to the repo-file node-bid domain. GREEN 2026-07-25 PT after Opus 5 fold: claim rechecks spend cap, requester early-close/window receipt, deadline, exact versions, and the canonical runtime transition guard before mutation. Opening all 169 transition pairs enables the previously forbidden behavioral command and deleting `bidding -> claimed` blocks claim, proving the table controls runtime behavior.
- [ ] 4.7 Add failing `tests/test_paid_market_delivery.py` coverage for exact `job_id:lease_fence:accepted_result_sha256` authority, domain-owned semantic verdict and acceptance-class binding, machine-gate-only bounty auto-accept with no requester veto, evidence/gate-rerun-only disputes for that class, human/inspection review only for domains that declare it, immutable artifact receipt/ACL/gate evidence, progress-not-completion, response-loss replay, changed-deliverable conflict, policy-bound auto-accept, `disputed -> running -> completed` fenced correction, refund resolution, and cross-tenant/private-delivery denial; implement the workflow handoff in `tinyassets/payments/market_delivery.py`.
- [x] 4.8 Write failing tests in `tests/test_paid_market_transport.py` proving verified subject+tenant authority is required, environment/caller-selected tenants grant no authority, a signed target/action/account/amount/time-bounded host grant records grant+host+target identities, the default-off feature flag blocks mutation, pure entries are serialized unchanged, v1 YAML/`public.ledger` stay byte-stable, and transport exposes no direct balance/table writer; implement immutable settlement commands plus injected `MarketLedgerRpc` in `tinyassets/payments/market_transport.py`. RED began with the missing module; GREEN 2026-07-24 PT: 16 transport tests and 191 pure-core/matcher tests passed inside the final gate. No legacy writer is imported or changed.
- [x] 4.9 Record the `docs/exec-plans/active/2026-07-18-distributed-execution-platform.md` S14/B36 dependency on exact future chain-effect identity `job_id:lease_fence:accepted_result_sha256` and the required separately reviewed hybrid chain-settlement successor from `docs/design-notes/2026-04-18-full-platform-architecture.md` §18.6; do not add provisional chain states, signer, resubmission, reconciliation worker, or alarm behavior in Wave 2. **SKIPPED-LANDED:** the exact identity and dependencies were already recorded throughout proposal/design/delta; this lane preserved them and added no chain-effect code.

## 5. Fault, Concurrency, and Uptime Proof

**Premise verification 2026-07-25:** the dark workflow now permits bounded local fault/concurrency and zero-host proofs. Production-shaped Supabase/load, external delivery authority, and live-environment proofs remain gated; local fixture evidence is not represented as production/load proof.

- [ ] 5.1 Add `tests/test_paid_market_faults.py` fault injection before/after request commit, outbox append, notification emission, bid replacement, match recording, claim CAS, completion receipt, acceptance/dispute transition, ledger apply, drain assertion, database commit, and response delivery; prove recovery yields zero or one committed effect at each boundary, never two. **PARTIAL 2026-07-25:** request/outbox/notification/bid/match/claim and fixture accounting/drain/database/replay boundaries have zero-or-one proofs. Completion, acceptance/dispute, and delivery-response boundaries require the externally owned fence/hash authority and were not stubbed, so this remains unchecked.
- [x] 5.2 Run 100 concurrent buyers against one versioned bid book; prove no bid/capacity slot sells twice and each committed selection equals `best_execution` over the bid-derived `BookOffer` values for its valid snapshot. GREEN 2026-07-25 PT: 100 in-process selected-host claim attempts produced one applied result, consumed capacity once, and selected exactly the landed pure oracle result for the valid snapshot; a separate two-request race proves one capacity grant cannot double-sell. This is local code evidence, not a production-shaped §14 load claim.
- [ ] 5.3 Run the §14 capability-sharded scenario through production-shaped Supabase Realtime without mocked delivery, with 500 synthetic daemons and 1,000 requests over five minutes plus repeated disconnect/reconnect for 20% of daemons and one saturated shard; prove bounded snapshot-tail convergence, no private cross-shard disclosure, zero lost current requests or duplicate bid/claim/delivery effects, no poll-all storm, fair bounded catch-up, and event-to-visible plus claim latency p99 below three seconds.
- [ ] 5.4 On an isolated Supabase test project matching launch region/compute, run at least 64 writers through at least one million overlapping transfers. Require at least 5,000 committed transactions/second aggregate, p99 below 250 ms, zero conservation drift/negative internal balance/deadlock/timeout, and sustained CPU/pool occupancy below 80%; record environment plus p50/p95/p99.
- [ ] 5.5 Run 500 simultaneous terminal attempts on one logical reservation account; prove one accounting settlement succeeds and every other caller gets the prior result or a clean state/idempotency conflict, with the logical reservation drained exactly once.
- [x] 5.6 Stop every tray/daemon host and prove market reads/durable state remain available, work remains honestly pending, no settlement is fabricated, and the surface reports settlement unavailable. Defer hosted retry/alarm proof to the cited distributed-execution exec-plan S14/B36 rollout. GREEN 2026-07-25 PT: the database-owned status read reports `dark_pending`, preserves the pending request, and reports settlement unavailable with no daemon process or fabricated settlement.
- [ ] 5.7 Race duplicate completion, explicit acceptance, auto-accept expiry, dispute, correction, failure, and refund commands on one delivered request; prove one append-only lifecycle, one authoritative settlement/refund disposition, timely dispute freeze, no artifact/ACL leak, and body-bound replay or clean conflict for every loser.

## 6. Verification, Mirror, and Handoff

- [x] 6.1 Rebuild the Claude plugin runtime mirror and run its parity/import probe after the new pure/payment modules land; do not hand-edit generated mirror files. Generated 264 runtime files; build import probe and an explicit mirror-first import of `MarketTransport`/`spot_settlement_entries` passed 2026-07-24 PT.
- [ ] 6.2 Run focused workflow/Realtime/bid/match/claim/delivery/transport tests, the full relevant PostgreSQL integration suite, `python -m ruff check` on changed Python, both strict OpenSpec validations, and the pre-change 180-test pure-core/matcher suite; attach dated environment and raw latency/failure evidence. **BLOCKED 2026-07-24 (standalone gates green):** Windows/Docker PostgreSQL 15 evidence is 227 transport/core/matcher/migration/SQL tests in 9.99s after final review fixes, 18 prototype tests in 3.46s, fresh Compose migration exit 0 + replay exit 0 + healthy gateway/PostgreSQL, changed-Python ruff clean, and both strict validations green. Workflow/Realtime/bid/claim/delivery suites do not exist because 4.1–4.7 are dependency-blocked.
- [x] 6.3 Obtain independent implementation review across correctness, security, migration safety, concurrency, non-custodial boundaries, and diff simplicity; resolve every Critical/Important finding and re-run affected evidence. Two Claude-family review passes found migration-history privilege, fee-oracle, RLS, SECURITY DEFINER, exact-baseline, NULL-invariant, and runner-specific ACL fingerprint defects; commits through `bc732ab0` resolved them. The final narrow re-review APPROVED after reproducing the targeted PostgreSQL tests; see `docs/audits/2026-07-24-paid-market-wave2-transport-{implementation-review,followup-review,final-rereview}.md`. A subsequent Opus 5 cross-family verification confirmed the code sound and returned ADAPT only for claim accuracy: the lane guarantee is limited to the four paid-market settlement oracles, while the sibling `paid-market-live-price-discovery` change still requires the orchestrator-routed no-zero-fee wording recorded in `LANE_REPORT.md` before either change lands. The earlier APPROVE trail is Claude-family-only; its Codex review dispatch never returned a verdict.
- [ ] 6.4 Before advertisement, complete a real rendered chatbot request conversation and paired tray-host flow through the live connector/control plane: submit, capability notification, bid, deterministic match, claim, fenced completion, private deliverable read, acceptance or dispute, and linked settlement/refund status. Capture trace/screenshot evidence and freshness-stamped post-fix organic clean-user use, or leave a STATUS watch item when none exists. **BLOCKED 2026-07-24:** advertisement/live activation is forbidden while 4.1–4.7, S14/B36, migration approval, and cutover remain incomplete.
- [x] 6.5 Keep migrations unapplied, workflow/transport unregistered, on-chain effects absent, and `TINYASSETS_PAID_MARKET` off; require the reviewed hybrid chain-settlement successor from `docs/design-notes/2026-04-18-full-platform-architecture.md` §18.6 and record explicit dependencies on `docs/exec-plans/active/2026-07-18-distributed-execution-platform.md` S14/B36 plus host decisions for migration, cutover, enablement, wallet authority, and chain-effect deployment. Premise verification found the new transport unreachable outside tests/generated mirror, the feature default-off, no chain code, and all gates preserved.
- [ ] 6.6 After implementation lands, sync the delta into `openspec/specs/`, validate idempotently, archive this change in the same lane, and remove the STATUS row in the landing commit. **BLOCKED 2026-07-24:** this builder branch has not landed and must receive cross-family review before any PR; sync/archive/STATUS retirement would falsely claim completion.
