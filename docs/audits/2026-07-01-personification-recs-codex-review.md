VERDICT: adapt

Required: default MCP routing still uses and mutates root-global `.active_universe`, violating the imported universe-creation spec. `_action_create_universe` writes the marker at [tinyassets/api/universe.py](C:/Users/Jonathan/Projects/TinyAssets/.claude/worktrees/permissions-fail-closed/tinyassets/api/universe.py:4650), and `_default_universe()` reads it before env/default fallback at [tinyassets/api/helpers.py](C:/Users/Jonathan/Projects/TinyAssets/.claude/worktrees/permissions-fail-closed/tinyassets/api/helpers.py:70). `get_status()` falls back to that path for anonymous/read-only authenticated callers at [tinyassets/api/status.py](C:/Users/Jonathan/Projects/TinyAssets/.claude/worktrees/permissions-fail-closed/tinyassets/api/status.py:694), while the OpenSpec says first contact must not route through a root-global marker and explicit selection must not change global state at [spec.md](C:/Users/Jonathan/Projects/TinyAssets/.claude/worktrees/permissions-fail-closed/openspec/changes/universe-creation/specs/universe-creation/spec.md:99). I reproduced it: founder A first `get_status()` creates home `u-*` and writes `.active_universe`; founder B with read-only scope and no home then calls `get_status()` with no `universe_id` and receives founder A’s `universe_id` with no error. Implement identity-bound omitted-scope resolution and stop MCP first-contact/create from changing global active state; add the still-unchecked OpenSpec tests in [tasks.md](C:/Users/Jonathan/Projects/TinyAssets/.claude/worktrees/permissions-fail-closed/openspec/changes/universe-creation/tasks.md:16).

Required before treating WorkOS rollout docs as durable: clean up contradictory auth guidance. The new provider correctly uses `https://<authkit_domain>/oauth2/jwks`, which matches current WorkOS docs, but the cited local recipe still tells readers to use `WorkOSClient(...).user_management.get_jwks_url(client_id)` / `api.workos.com/sso/jwks/<client_id>` at [docs/reference/workos-authkit-integration.md](C:/Users/Jonathan/Projects/TinyAssets/.claude/worktrees/permissions-fail-closed/docs/reference/workos-authkit-integration.md:47), contradicting its own resolved note at line 135. Also reconcile the ratified design’s stale `workflow.{tool}.{effect}` scope text with the actual `tinyassets.{tool}.{effect}` implementation at [tinyassets/auth/provider.py](C:/Users/Jonathan/Projects/TinyAssets/.claude/worktrees/permissions-fail-closed/tinyassets/auth/provider.py:452).

Source checks: WorkOS’s current AuthKit MCP docs support AuthKit as AS, `https://authkit_domain/oauth2/jwks`, PRM, and `resource_metadata`; their resource-indicator guidance supports strict `aud` validation. The MCP server-instructions blog supports the branch’s warning that server instructions are not reliable for changing model personality. Focused tests passed: 92 auth/ACL/query tests and 75 persona/reset/bundle tests.