VERDICT: adapt

1. **Critical: refresh remains double-redeemable after TTL.** [`still_held()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/leases.py:123) is a TOCTOU check; fencing only rejects the stale commit. I reproduced both A and B entering the provider-call section after A’s lease expired; B committed and A later failed. GitHub invalidates a refresh token after one use, while the cited advisory fixes the race by atomically consuming state before minting—not by fencing afterward. Add a protocol that cannot issue a second redemption after ambiguous/expired ownership, plus a TTL-overrun test counting actual provider calls. [GitHub token semantics](https://docs.github.com/en/apps/creating-github-apps/authenticating-with-a-github-app/refreshing-user-access-tokens), [cited advisory](https://github.com/better-auth/better-auth/security/advisories/GHSA-392p-2q2v-4372).

2. **Critical: non-finite expiry bypasses lifecycle enforcement.** [`check_lifecycle()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/record.py:76) accepts `NaN`; `NaN <= now` is false. I stored `expires_at=float("nan")` and successfully read the credential. Reject non-finite numeric metadata, encode JSON with `allow_nan=False`, and convert malformed sealed metadata to `CORRUPT_RECORD`.

3. **Required: implementation still conflicts with canonical PLAN.** [PLAN.md](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/PLAN.md:57) explicitly forbids platform-resident private data even when encrypted. The design proposes a replacement, but the Claude review says host approval is still required. Obtain that approval and amend PLAN before merging platform custody; the design note alone cannot override it.

4. **Required: KEK rotation can report false success.** [`rotate_kek()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:418) does not require `new_key_id` to be active. I rotated to `k2` while `k1` remained active: existing rows moved to `k2`, future writes continued using `k1`, and rotation returned success. Require active-key agreement and verify all live rows use and decrypt under the new key before success.

5. **Required: attestation overclaims custody.** [`_evidence_ok()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/attestation.py:72) ignores `dacl_current_user_only`; explicit false evidence still produced `ok=True`. The current `icacls` heuristic also does not prove “daemon SID + SYSTEM only.” Either make ACL verification a hard gate using Windows security APIs or keep local attestation false until deployment integration supplies that proof. Microsoft DPAPI proves current-user encryption and integrity, not the required DACL. [Microsoft DPAPI documentation](https://learn.microsoft.com/en-us/windows/win32/api/dpapi/nf-dpapi-cryptprotectdata).

6. **Required: attestation does not consistently fail with typed errors.** A backend read failure after probe creation escapes [`attest_store()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/attestation.py:90) as raw `RuntimeError`; `_must_fail()` also counts any exception as successful scope isolation. Convert unexpected failures to a failed result/`BACKEND_UNAVAILABLE`, and require the expected typed wrong-scope failure.

7. **Required: public projections violate the design allowlist.** [`AttestationResult.public_projection()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/attestation.py:52) exposes internal `store_id` and custody; [`SecretDescriptor.public_projection()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/types.py:148) exposes version/state despite the design specifying ref/kind/scope/timestamps only. Tighten both projections and their tests.

8. **Required hardening:** [`FileKeyProvider`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/crypto.py:135) performs `lstat()` followed by path-based `read_bytes()`, leaving a symlink-swap window. Open with `O_NOFOLLOW`, verify the opened descriptor with `fstat`, and validate the key directory/active marker too.

Verification: focused tests passed `44 passed, 2 skipped`; Ruff and `git diff --check` passed. PyNaCl is genuine and installed. The green suite does not cover the reproduced TTL double-call, `NaN` expiry, inactive-key rotation, or false DACL attestation.