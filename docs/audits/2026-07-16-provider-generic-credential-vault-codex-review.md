VERDICT: adapt

1. **Critical: custody and lifecycle metadata are not authenticated.** [`canonical_aad`](C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/crypto.py:137) binds scope/ref/kind/version, but omits `store_id`, custody, daemon identity, state, and expiry. The backends trust those plaintext fields during reads. I changed only `store_id` and decrypted through another store; clearing `expires_at` reactivated an expired credential without the KEK. Bind immutable store identity into AAD and authenticate mutable lifecycle metadata separately. Add tamper/swap tests.

2. **Critical: forged bindings can delete valid credentials.** Platform deletion omits kind/custody verification in [`platform_backend.py`](C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:332). Local deletion checks only scope, while [`_ref_filename`](C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/local_backend.py:132) discards the ref prefix. I reproduced wrong-kind platform deletion and wrong-kind/cross-store local deletion. Validate canonical refs and the complete protected descriptor before every replace/delete.

3. **Critical: the exclusive refresh lease is not exclusive after TTL.** [`_try_acquire_lease`](C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:392) deletes expired leases without heartbeat or fencing; the local lock has the same problem. I reproduced two holders overlapping when the first operation exceeded TTL. This does not close the cited refresh-race class. Use atomic token-version CAS/fencing—matching the [Better Auth advisory’s exactly-one-winner fix](https://github.com/better-auth/better-auth/security/advisories/GHSA-392p-2q2v-4372)—and test refreshes exceeding TTL plus crash/reacquisition.

4. **Required: daemon-local CAS is non-atomic.** [`DpapiVaultBackend._put`](C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/local_backend.py:297) performs an unlocked read/check/write, so concurrent writers can both accept the same expected version. The DPAPI CAS test is sequential; all multiprocess CAS coverage targets SQLite. Add a Windows multiprocess exactly-one-winner test and lock/fence the entire mutation.

5. **Required: attestation and production-custody claims exceed what is implemented.** Local evidence only proves that the current process can decrypt; it does not inspect the required DACL in [`inspect_persisted`](C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/local_backend.py:454). `FileKeyProvider` does not verify root ownership, mode, or symlink safety. No separate broker process, UID/capability drop, signed capability validation, or socket boundary exists. Microsoft’s source supports current-user DPAPI and integrity, but not the branch’s DACL/process claims. Either implement these gates or explicitly keep the PR library-only and block production integration on them. [Microsoft DPAPI documentation](https://learn.microsoft.com/en-us/windows/win32/api/dpapi/nf-dpapi-cryptprotectdata)

6. **Required: KEK rotation does not perform the promised verification.** [`rotate_kek`](C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:432) unwraps DEKs but never authenticates payload ciphertext or complete row identity before reporting success, and it bypasses attestation. Verify every payload and protected descriptor inside the transaction; add corruption and crash-injection tests.

7. **Required documentation correction:** XChaCha is a sound choice because its 192-bit nonce permits safe random nonces. It is not stronger than AES-GCM “because it supports AAD”; AES-GCM also authenticates AAD. Correct the rationale in the review artifact. [Libsodium XChaCha guidance](https://doc.libsodium.org/secret-key_cryptography/aead/chacha20-poly1305), [Libsodium AES-GCM documentation](https://doc.libsodium.org/doc/secret-key_cryptography/aead/aes-256-gcm)

Verification: the branch’s focused suite passed `31 passed in 4.60s`; Ruff and `git diff --check` passed. Those tests do not cover the reproduced isolation, lifecycle-tampering, deletion, or lease-expiry failures. GitHub’s cited 8-hour/6-month rotation semantics and one-use refresh behavior were confirmed in [current GitHub documentation](https://docs.github.com/en/apps/creating-github-apps/authenticating-with-a-github-app/refreshing-user-access-tokens).