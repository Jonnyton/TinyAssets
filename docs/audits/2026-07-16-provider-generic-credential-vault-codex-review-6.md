VERDICT: adapt

1. **Critical: short writes can destroy the existing local credential while reporting success.** [`_write_sidecar()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/local_backend.py:433) calls `os.write()` once and ignores its returned byte count. Reproduction: a half-write returned a successful version-2 descriptor, replaced the old blob, then `get()` failed with `CORRUPT_RECORD`. Python documents that `os.write()` returns the number actually written. Loop until all bytes are written and add a short-write regression proving the old credential survives failure. [Python `os.write` documentation](https://docs.python.org/3/library/os.html#os.write)

2. **Critical: the refresh bearer capability remains observable.** [`RefreshTicket.secret`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/leases.py:236) is public ordinary `bytes`. Both `dataclasses.asdict(ticket)` and `vars(ticket)` exposed it in my reproduction, bypassing the repr/pickle protections. Anyone obtaining that capability can authorize replacement of a one-use refresh-token record. Use a private, slotted, non-dataclass secret container and test `asdict`, `vars`, copy, pickle, repr, and formatting. GitHub confirms using a refresh token invalidates both it and the old access token, so this authorization path is security-critical. [GitHub refresh semantics](https://docs.github.com/en/apps/creating-github-apps/authenticating-with-a-github-app/refreshing-user-access-tokens)

3. **Required: corrupted rotation metadata escapes the typed-error boundary.** [`rotate_kek()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:594) constructs `Custody`, `SecretKind`, and numeric values directly from unauthenticated SQLite hints. Changing `custody` to `"forged"` produced raw `ValueError`, contradicting the broker’s `CredentialUnavailable` contract. Normalize malformed row shapes to `CORRUPT_RECORD`, suppress unsafe causes, invalidate attestation, and test every enum/numeric hint.

4. **Required: empty credentials are accepted and returned.** [`SecretBytes.__init__()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/secret_bytes.py:36) permits zero-length values; `put(..., SecretBytes(b""))` successfully round-tripped `b""`. Reject empty—and preferably unbounded—credential payloads at the broker boundary, including refresh completion, while preserving the previous value after a rejected CAS.

5. **Required: durability documentation still contradicts the implementation and evidence.** The design says “SQLite WAL now” at [`provider-generic-credential-vault.md`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/docs/design-notes/2026-07-16-provider-generic-credential-vault.md:17), while the backend uses `TRUNCATE + EXTRA`; package and test comments also still claim WAL/FULL or call `os._exit` a power-loss proof. Update all artifacts and the draft PR to distinguish process-crash evidence from the still-pending power-cut/VM-reset release gate. [SQLite durability semantics](https://www.sqlite.org/pragma.html#pragma_synchronous)

Fresh verification at `0a5bc6be`: focused suite `71 passed, 2 skipped`; Ruff, `compileall`, and `git diff --check` passed. PyNaCl 1.6.2 is genuine and Apache-2.0 licensed. No repository-wide suite pass is claimed.