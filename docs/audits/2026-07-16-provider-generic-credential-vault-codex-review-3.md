VERDICT: adapt

1. **Critical: refresh claims are not power-loss durable.** [`PlatformVaultBackend._connect()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:111) sets `synchronous=NORMAL`. SQLite explicitly says WAL commits may roll back after power loss in this mode. A lost claim plus the old vault record permits a second one-time-token redemption. Use `FULL` and add a hard-crash/restart test—not an injected Python exception. Also pin or gate a patched SQLite: this environment uses 3.50.4, within SQLite’s documented WAL-reset affected range; the pinned production image provides no vault-specific version proof. [SQLite durability documentation](https://www.sqlite.org/pragma.html#pragma_synchronous), [WAL-reset advisory](https://www.sqlite.org/wal.html).

2. **Required: local attestation still approves custody without the required DACL.** [`_evidence_ok()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/attestation.py:74) deliberately omits ACL evidence, while the test explicitly requires `result.ok is True` with `dacl_gate="deferred-to-deployment"`. I reproduced `ok=True` on Windows. This contradicts the design’s restricted-DACL requirement and the prior review adaptation. DPAPI proves current-user encryption and integrity, not file ACL isolation. Implement a Windows security-descriptor verifier or keep local attestation false—and public CRUD blocked—until deployment supplies it. [Microsoft DPAPI documentation](https://learn.microsoft.com/en-us/windows/win32/api/dpapi/nf-dpapi-cryptprotectdata).

3. **Required: the public broker contract does not enforce safe mutation or refresh.** [`VaultBroker`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/broker.py:33) omits refresh coordination, while both backends accept `replace` without `expected_version`. I reproduced successive unguarded replacements `v1 → v2 → v3`. I also pre-claimed nonexistent version 99, permanently preventing its legitimate future claim. Require `expected_version` whenever `replace` is supplied, reject the inverse pairing, and expose one broker-level refresh operation that atomically validates the authenticated current record before consuming its redemption right. GitHub confirms refresh tokens become unusable after one use; the cited advisory’s safe pattern is an atomic CAS over the actual current token row. [GitHub token semantics](https://docs.github.com/en/apps/creating-github-apps/authenticating-with-a-github-app/refreshing-user-access-tokens), [Better Auth advisory](https://github.com/better-auth/better-auth/security/advisories/GHSA-392p-2q2v-4372).

4. **Required: the custody discriminant admits an invalid platform store.** [`VaultStore.__post_init__()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/types.py:118) requires `daemon_id` for local custody but does not forbid it for platform custody. The platform backend accepts such a store; I wrote and read it successfully, then `rotate_kek()` failed with `CORRUPT_RECORD` because rotation reconstructs the store without that unpersisted daemon ID. Enforce `daemon_id is None` for platform stores and validate the invariant again at the backend boundary.

5. **Required: the promised typed-error boundary is incomplete.** Tampering the plaintext `version` hint to a non-integer makes [`_decrypt_row()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:207) leak a raw `ValueError`, contradicting “every failure is `CredentialUnavailable`.” Several filesystem wrappers also chain raw `OSError` causes, exposing backend paths in tracebacks. Normalize corrupt metadata and post-attestation I/O failures to the documented codes and suppress unsafe exception causes.

Fresh Windows verification, 2026-07-16: focused suite `53 passed, 2 skipped in 11.13s`; Ruff and `git diff --check` passed. The green tests do not cover the reproduced failures above.