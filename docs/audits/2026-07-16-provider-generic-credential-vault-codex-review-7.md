VERDICT: adapt

1. **Critical: claimed power-loss durability is unsupported.** Both backends use `TRUNCATE + synchronous=EXTRA` ([platform](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:127), [local](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/local_backend.py:327)). SQLite documents EXTRA’s additional sync for **DELETE mode**, while TRUNCATE commits without a following `fsync()` and is only “usually” power-loss safe. A rolled-back refresh claim permits the same one-use token version to be claimed again. Use `DELETE + EXTRA` or equivalent proven storage, verify the effective pragmas, and add the pending VM-reset/power-cut test. Avoiding WAL on SQLite 3.50.4 is justified—the WAL-reset bug was fixed in 3.51.3—but TRUNCATE is not the durable substitute claimed. [SQLite synchronous semantics](https://www.sqlite.org/pragma.html#pragma_synchronous), [atomic-commit documentation](https://sqlite.org/atomiccommit.html), [SQLite release history](https://www.sqlite.org/changes.html).

2. **Critical: local deletion can reopen a consumed refresh token.** [`DpapiVaultBackend._delete()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/local_backend.py:630) removes the sidecar and retires the high-water claim in separate filesystem/SQLite persistence domains. If the sidecar survives, reappears after power loss, or is restored without the control DB row, version 1 becomes claimable again. I reproduced that state transition. Keep an irreversible local tombstone/high-water row, or design a recovery-safe authoritative deletion state; test crashes at every ordering boundary.

3. **Required: the typed-error contract remains false.** Changing any persisted envelope BLOB to SQLite TEXT made public `get()` raise raw `TypeError` at [`_envelope_from_row()`](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:264). Simulated storage-open failure exposed raw `OperationalError`; malformed `at_version` exposed raw `ValueError`/`TypeError`. This contradicts [`VaultBroker`’s “every failure is CredentialUnavailable” contract](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/broker.py:17). Validate row shapes and inputs, normalize all SQLite/I/O failures, invalidate cached attestation on corruption, and add adversarial tests. Either type caller-validation failures too or narrow the documented contract explicitly.

4. **Required: durable design truth is stale.** The design still labels itself “design only,” says Claude review is pending, and its broker protocol omits `begin_refresh`/`complete_refresh`. Update it to describe the implemented high-water capability protocol and the unresolved power-loss/deletion limitations before merge.

The cited libsodium, DPAPI, GitHub token lifetime/rotation, and X OAuth/pricing claims were independently rechecked and remain consistent with their primary sources. Fresh evidence at `932ee1f6`: focused suite `78 passed, 2 skipped`; Ruff, `compileall`, and `git diff --check` passed. The repository-wide suite timed out after five minutes, so no full-suite pass is claimed.