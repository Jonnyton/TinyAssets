VERDICT: adapt

1. **Critical — refresh claims are not proven power-loss durable.** Both SQLite stores use rollback `TRUNCATE` with `synchronous=FULL` ([platform_backend.py](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:111)). SQLite explicitly says rollback-journal `FULL` may lose the last transaction after power loss; `EXTRA` is the ACID setting. The test uses `os._exit`, which proves process-crash—not power-loss—durability. A lost claim can permit a second one-use refresh redemption. Use `EXTRA`, assert the effective mode, and add production-filesystem power-cut/VM-reset proof. [SQLite durability documentation](https://www.sqlite.org/pragma.html#pragma_synchronous)

2. **Critical — failed local CAS can delete the existing credential.** `_write_sidecar()` replaces the old blob before applying the DACL, then deletes the new path if DACL enforcement fails ([local_backend.py](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/local_backend.py:401)). Reproduction returned `old_blob_survives_failed_replace False`. Apply and verify the DACL on the temporary file before replacement, preserving the old blob on every failure.

3. **Critical — consume-before-mint remains bypassable and tickets are forgeable.** Ordinary public `put(replace=..., expected_version=...)` advances refresh-token records without a ticket; ticket validation only runs when a ticket happens to be supplied ([platform_backend.py](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:338)). An existing test explicitly uses this bypass ([test_credential_vault_hardening.py](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tests/test_credential_vault_hardening.py:371)). `RefreshTicket` is also a constructible dataclass authenticated only by caller-chosen `holder`; a reconstructed ticket with matching fields succeeded in my repro. Require a broker-minted random claim capability and reject generic replacement for rotating-token kinds. GitHub confirms each refresh token becomes unusable after one use. [GitHub refresh semantics](https://docs.github.com/en/apps/creating-github-apps/authenticating-with-a-github-app/refreshing-user-access-tokens)

4. **Required — attestation accepts the wrong failure reason.** `_must_fail()` treats every `CredentialUnavailable` as proof of scope isolation ([attestation.py](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/attestation.py:167)). A fake backend returning `NOT_FOUND` for the wrong-scope probe received `ok=True`. Require the explicitly permitted scope/authentication error code.

5. **Required — typed-error and malformed-reference guarantees still break.** Scope comparison occurs before reference validation, so a newline-bearing malformed ref is echoed in `SCOPE_MISMATCH` errors ([platform_backend.py](/C:/Users/Jonathan/.claude/jobs/aaaa5b09/tmp/wt-vault-review/tinyassets/credentials/platform_backend.py:414)). A valid JSON sidecar containing `[]` leaks raw `AttributeError` from the local backend. Validate bindings and decoded sidecar schemas before all use, then normalize failures to `CredentialUnavailable`.

6. **Required — local power-loss durability is still unsupported.** `_fsync_dir()` is a no-op on Windows and `os.replace()` is used without a write-through operation. The hard-process-kill test does not simulate power loss. Use a documented durable Windows replacement path and power-interruption evidence; Microsoft exposes `MOVEFILE_WRITE_THROUGH` specifically for disk-flushed moves. [Microsoft MoveFileEx documentation](https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-movefileexw)

7. **Required — redaction and lifecycle cleanup are incomplete.** Default reprs for `AttestationResult`, `RefreshTicket`, and `RefreshLease` expose store/custody/boot/holder/version/fence metadata; the “every vault type” test omits them. Refresh claims are never pruned, including after secret deletion, so the table grows indefinitely. Add redacted reprs and transactionally retire claims once their version advances or the ref is deleted.

Fresh verification on Windows at `72535b38`: focused suite `65 passed, 2 skipped`; Ruff and `git diff --check` passed. Those green tests do not cover the reproduced failures above. The draft [PR #1469](https://github.com/Jonnyton/TinyAssets/pull/1469) also still describes WAL and stale test counts.