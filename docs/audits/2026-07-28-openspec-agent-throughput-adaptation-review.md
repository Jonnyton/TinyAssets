Both blocking findings are fully folded, verified against the current worktree, and nothing regressed. No remaining blocking issues.

**Finding 1 — all-matching-row WIP counting (fixed and proven).** `_active_owners` (`scripts/openspec_flow.py:63-70`) now unions the exact provider identity from *every* matching claimed/in-flight row, and `build_report` credits the change to each of those owners (`scripts/openspec_flow.py:225-227`) — first-row-wins is gone. The new test `test_all_matching_claimed_owners_count_toward_wip` reproduces the exact bypass scenario from the review (a `claimed:provider-b` row mentioning the change in its Depends cell placed *above* the true owner's row) and asserts both providers carry the change in provider WIP and that the true owner is rejected for a second change. Live confirmation on the real repo: `codex-gpt5-desktop-throughput` passes the self-check for its own change but is BLOCKED for `universe-visibility` with the owned change named and global WIP (11) reported. Design.md §3 (lines 71-76) now documents this counting semantics verbatim, so code and design agree. The remaining over-attribution direction (a Depends mention still credits the mentioning provider) is the conservative fail-closed choice the review itself proposed, and it's documented as such in AGENTS.md/skill text ("every matching claimed row counts conservatively") and design.md's risk table.

**Finding 2 — invocation pinning (fixed on all seven surfaces).** AGENTS.md:226-228, `openspec/config.yaml:35`, both SKILL.md files (byte-identical — same git blob `7b1dfe1`), proposal.md:21-23, design.md:108-110, and the delta spec (spec.md:12-13 and 47-48, now as SHALL requirements) all say admission runs **after scaffolding and before claiming or building**, with audit at dispatch/triage time and an explicit "not a mandatory session-start gate." A grep for "before creating" across all these surfaces returns nothing. The unknown-change path still fails closed with exit 2, which is now consistent since no canonical text directs a pre-creation invocation.

**Tests and git window.** 11/11 focused tests pass (a strict superset of the prior 8 — exact-identity rejection, self-exclusion, suffix warning, 13-task ceiling, umbrella warning-only, read-only audit, JSON parity, finish-first ranking, and CLI exit 2 all retained). The rewritten `_git_flow` is history-based (`git log --format= --name-only --diff-filter=A --no-renames`), which closes the endpoint-diff gaps: admitted-and-archived-within-window is counted (tested), pre-existing changes gaining new files are excluded via the baseline tree check, archive moves surface as A lines under `--no-renames`, and re-archives dedup against `baseline_archived`. The option-shaped revision guard rejects `-`-prefixed values before they reach git's argv (line 96-97, tested with `--output=unsafe`). Ruff is clean on both files.

**Prior non-blocking corrections confirmed.** The Claude review doc's first line no longer points at the nonexistent `docs/reviews/` path — it now states "This file is the durable review artifact." The delta scenario is retitled "Incomplete active change is absent from live coordination" with the "unchecked tasks" condition (spec.md:22-27), matching the complete-but-unarchived precedence. Bonus: the review's suggestion 7 (missing `tasks.md` invisibility) was also folded — such changes now classify as `invalid-artifacts`, count toward WIP, and emit a triage warning, with a test.

Two non-blocking observations for the lane owner, both pre-existing behavior the earlier review already blessed: the umbrella warning still fires on this change's own proposal (warning-only, by design), and a complete-but-unarchived change doesn't hold its provider's WIP slot (finish-first surfaces it as the top recommendation instead — the direction finding 4's scenario fix endorsed).

Codex-dispatch note: skipped deliberately — this review *is* the opposite-provider gate on Codex-built work; routing it back to Codex would collapse the cross-family independence.

VERDICT: APPROVE
