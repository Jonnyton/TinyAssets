# Self-Marketing Archetype — The Platform Pitches Itself (2026-07-09)

**Standing goal:** "Keep pitch assets current with the specs; publish the weekly clip." The founder's marketing pipeline as a commons workflow — the dogfooding thesis extended to distribution.

Nodes (all existing primitives): deck generation (the deck's generator script as a code node) -> narration transcript (text node) -> **voice synthesis via the founder's voice-clone connection** -> mux (ffmpeg node) -> **publish effector** (TikTok/YouTube/X adapters).

HARD RULES:
1. **Voice credentials are ledger grants** — auth-injected at the boundary; adapters NEVER hold the voice key (a stolen voice outranks a stolen API key). Voice cloning uses the founder's OWN voice for the founder's OWN content, with the clone credential scoped to this universe.
2. **Publishing is a consequential effector under action caps** — auto or hold-for-confirm per the autonomy envelope; every upload journaled with idempotency keys (no double-posts).
3. **Synthetic-media disclosure ON by default** in the publish effector (platform policies require it; honest anyway).

Assets in this bundle: TinyAssets-Pitch.pptx (updated: meta-market thesis, Bloomberg-of-compute moat, opens-at-zero), NARRATION-SCRIPT.md (investor 3:10 + TikTok 60s, pre-timed), TinyAssets-Pitch-video.mp4 (16:9, timed), TinyAssets-TikTok.mp4 (9:16 cards, caption-first for muted viewing). Record the voice clone from NARRATION-SCRIPT, lay audio over the pre-timed videos, done — then hand the whole pipeline to the archetype.
