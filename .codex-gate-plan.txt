1. Critical: The gate has no usable baseline: `.github/known-failing-tests.txt` contains zero entries, while run `29884182377` reported 200 failures. Generate a fresh current-main ledger and prove a green main run before changing protection.
2. Critical: Reviews being unrequired leaves the gate self-bypassable. A declared `infra-change` PR can replace the command, rewrite the ledger, or add `if: false`; GitHub treats skipped jobs as successful. Protect these files with a required workflow/ruleset or targeted non-author approval. [GitHub status-check behavior](https://docs.github.com/en/pull-requests/reference/status-checks)
3. Critical: `ci_required_tests.py:116-118,245` excludes skipped tests from both execution and stale-ledger checks. Skipping every test—or selecting one passing test—returns green; only literal zero collection fails via exit 5. Require a baseline inventory/count and reject unexpected skips, deselections, deletions, and renames.
4. Required: The only complete serial run took 26:27, including 25:48 for tests and 28 seconds for installation—not the ADR’s claimed three minutes. Current main adds 85,821 test lines across 223 files since that run, so the 30-minute timeout is unproven and the <10-minute merge property is already violated. Fix parallel isolation or define a measured fast suite; obtain several fresh sub-10-minute runs.
5. Required: The flip strands existing work: currently 10 PRs are open and the sole non-draft PR, #1492, has no `required-tests` result. `auto-enroll-merge.yml` consumes `synchronize` but never generates it, while strict protection only waits for an update. Backfill every eligible PR and add a trusted updater/reconciler; otherwise active drain PRs can also become permanently behind during a 26-minute run. [GitHub strict-check behavior](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches)
6. Required: The merge result imports and its focused tests pass (`13 passed`), but it is already stale again: HEAD merged `ce35784f`, while `origin/main` is `61da7f21` and the branch is one commit behind. The delta is six commits and ten files, not four; rebuild deliberately from current main or merge it again and rerun current-tree evidence.
7. Required: #1503 is not red proof for the shipping aggregator. Its run used the older direct-pytest workflow and was already red with 106 failures and 14 errors; the deliberate failure merely appeared among them. Prove the current quarantine aggregator green, then inject exactly one novel failure and observe green→red.
8. Required: Ubuntu-only execution permanently skips the three newly Windows-only resolver tests, while tray tests cannot collect headlessly. Either add Windows coverage or split these unrelated coverage reductions from the gate change.
9. Required: The workflow/ADR claim that renaming `required-tests` “goes green on nothing” is wrong; the old required context remains Expected and blocks indefinitely. Correct the documentation and test renamed/skipped/no-report context behavior before relying on the recovery instructions.
VERDICT: adapt