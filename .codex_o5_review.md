VERDICT: adapt

1. **Critical: settlement scope is caller-selected, not re-derived.** [`join_paid_observation`](https://github.com/Jonnyton/TinyAssets/blob/claude/o5-live-price-runtime/tinyassets/paid_market/price_surface.py#L151) accepts class/revision/scope as arguments; none of the three receipts bind them. This permits post-hoc rebucketing, directly violating the spec. Bind exact descriptor/class/scope into all receipts and reject mismatches.

2. **Critical: the full aggregate key is not enforced.** [`NativeAsk`, `ReferenceRequest`, and `ReferenceQuote`](https://github.com/Jonnyton/TinyAssets/blob/claude/o5-live-price-runtime/tinyassets/paid_market/price_surface.py#L86) lack scope revision/dimensions. Aggregation filters observations by the triple but native asks and references only by `market_class_id`, allowing one scope’s price to enter or clamp another. Add the full key and cross-scope exclusion tests.

3. **Critical: quote scope validation proves shape, not provenance.** [`quote_signing_bytes`](https://github.com/Jonnyton/TinyAssets/blob/claude/o5-live-price-runtime/tinyassets/paid_market/quotes.py#L184) accepts any nonempty revision and any canonical identifier-valued JSON object. It never resolves a trusted `ScopeRevision` or re-derives dimensions from terms. A seller can therefore sign an invented bucket. Require a trusted revision lookup/projector during quote creation and validation.

4. **Required: `ScopeRevision` is neither immutable nor reliably bound.** [`_validate_revision`](https://github.com/Jonnyton/TinyAssets/blob/claude/o5-live-price-runtime/tinyassets/paid_market/scope.py#L158) accepts arbitrary revision identifiers, mutable nested mappings, and projections from facets outside `_MARKET_FACETS`. The same revision can produce different keys after mutation, and private/arbitrary facets can be promoted into public scope. Canonically hash immutable revision content, constrain projection sources, and enforce the 64-member bound.

5. **Required: malformed demand can fragment market identity.** [`_compare`](https://github.com/Jonnyton/TinyAssets/blob/claude/o5-live-price-runtime/tinyassets/paid_market/descriptors.py#L586) deduplicates required sets for compatibility, while [`_public_requirements`](https://github.com/Jonnyton/TinyAssets/blob/claude/o5-live-price-runtime/tinyassets/paid_market/descriptors.py#L745) preserves duplicates. Thus `["text"]` and `["text","text"]` can both match but derive different class IDs. Validate and canonicalize demand identifiers, duplicates, integer bounds, and structures before matching/projection.

6. **Required: the guarded-parser claim is false.** [`json.loads`](https://github.com/Jonnyton/TinyAssets/blob/claude/o5-live-price-runtime/tinyassets/paid_market/descriptors.py#L489) materializes the tree before `_enforce_node_limits` checks depth, arrays, and scalar count. Implement genuine parse-time limits or amend the normative spec and lane claims.

7. **Required before re-review:** restack onto current `main`; the branch is 4 commits behind and both sides changed `LANE_REPORT.md`. Add red/green regressions for every case above and run the full suite—currently recorded as `PENDING_FULL`. I could not independently rerun tests because the local PowerShell host crashed before every command.

The cited legal framing is appropriately cautious: the CFTC interpretation is fact-specific and limited to embedded volumetric optionality, while BIS describes knowledge/end-user/end-use conditions that may trigger controls. [CFTC](https://www.cftc.gov/LawRegulation/FederalRegister/finalrules/2015-11946.html), [BIS](https://www.bis.gov/media/documents/ai-policy-statement-training-ai-models-may-13-2025).